# AGENTS.md — نقطة دخول أي وكيل ذكاء اصطناعي

**الحالة:** نافذ · **آخر تحديث:** 2026-09-04 · **القرار الحاكم:** ق-93

هذا الملف ليس مصدر حقيقة، بل باب. وظيفته أن يستلم أي وكيل — أيًّا كانت
واجهته — المشروع من المستودع وحده بلا محادثة سابقة، وأن يمنع بقاء
معلومة حاكمة في المحادثة وحدها.

## 1. اقرأ بهذا الترتيب

1. هذا الملف.
2. `docs/memory/AI_HANDOFF_PROTOCOL.md` — ترتيب القراءة الكامل، وعقد
   التوثيق الواجب لكل دفعة عمل.
3. `docs/memory/RESUME_POINT.md` — **المصدر الوحيد** لما يُفعل الآن.
   لا تستنتج الخطوة التالية من آخر Commit ولا من `PROGRESS.md`.
4. ثم ما يحدده `AI_HANDOFF_PROTOCOL.md` بحسب موضوع الجولة.

## 2. عقد العمل: من ينفّذ ماذا

- **المالك** ينفّذ كل ما يمسّ القاعدة أو الشبكة أو تاريخ المستودع:
  `npm run db:reset`، `npm run db:test`، `npm run db:index`،
  `npm run cloud:verify`، وأوامر Docker، و`git commit` و`git push`،
  وفتح طلبات الدمج ودمجها. الوكيل **يكتب الكود ويسلّم كتلة أوامر
  جاهزة**، ولا ينفّذ شيئًا من ذلك بنفسه.
- الوكيل يشغّل بنفسه فحوص الهاتف وحدها: `flutter analyze` و
  `flutter test` داخل `apps/mobile`.
- كل سطر أمر يُسلَّم للمالك: **أقصر من ٦٠ محرفًا**، بلا اعتماد على
  تقسيم كلمات لمتغير غير مُقوَّس، وبلا كتلة شفرة داخل كتلة. التفصيل في
  `docs/memory/TERMINAL_COMMAND_PROTOCOL.md`.

## 3. القاعدة السحابية — حقائق تخالف التوقع

- القناة العاملة الوحيدة: `aws-0-ap-south-1.pooler.supabase.com:6543`
  (Supavisor transaction mode). المنفذ 5432 والاتصال المباشر عبر IPv6
  لا يصلان، و`supabase db push` يفشل دائمًا من هنا.
- **لا تُطبع كلمة المرور ولا رابط اتصال غير مُقنَّع، في أي مخرَج.**
- النشر ليس خطوة يدوية: **دمج `main` = نشر على الإنتاج** عبر تكامل
  GitHub. وفرع `main` محميّ بضابط نافذ، فالطريق الوحيد: فرع ← طلب دمج
  ← دمج مضغوط.
- `supabase/tests` **لا تُشغَّل سحابيًا** أبدًا؛ فالمُثبَت سحابيًا وجود
  الأهداف لا نجاح تحققاتها. والخطة المجانية بلا نسخ احتياطية وبلا فروع
  معاينة: التراجع يكون بهجرة جديدة، لا بتعديل هجرة منشورة.

## 4. حدود لا تُخترق

- **ق-120 نافذة**: لا شاشة ولا ميزة جديدة قبل إغلاق طابور التدقيق، ولا
  `W2-02d`. أي استثناء يكون بطلب صريح من المالك ويُسجَّل معلنًا.
- الهجرات **071–097 مختومة**، وأي تغيير جديد في القاعدة يبدأ من
  **098**، مع **اختبار دائم واحد** في `supabase/tests` لكل هجرة.
- ق-78: `api` هو السطح المكشوف وحده. ق-79: لا كتابة إلا عبر عقد `api`.
  ق-99: لا حساب مال ولا اختراع رقم في العميل. ق-113: لا نجاح كاذب —
  وهي تسري على **أدوات التحقق نفسها** (الثابت 699 في
  `docs/technical/INVARIANTS.md`).
- لا تقرأ ملفات الهجرات كاملة لإثبات وجود عمود أو قيد أو دالة: استعمل
  الفهرس المولَّد `docs/technical/db/*.txt` (يُبنى بـ`npm run db:index`
  ولا يُحرَّر يدويًا).

## 5. إيقاع الجلسة

مهمة واحدة لكل جلسة. خطة قبل الكود. وقياس بعد الكود لا قبله: كل رقم
تكتبه في وثيقة يجب أن يكون قد شُغِّل فعلًا، والفرق بين
Adopted / Documented / Implemented / Verified مُعرَّف في
`AI_HANDOFF_PROTOCOL.md` §7 ولا يُخلط. وإذا حُجب إجراء على المستخدم،
فاقرأ صلاحية الخادم ووسائطه أولًا: منع عملٍ يقبله الخادم **فشل كاذب**،
وهو مرآة النجاح الكاذب لا نقيضه.

## 6. الإخراج العربي

وثائق المشروع وجميع المخرجات النصية الموجّهة للمستخدم عربية، بما في ذلك
التحليل، والخطط، وشرح القرارات، وملخصات التنفيذ، وحالة التقدم، ونتائج
الاختبارات، وتفسير الأخطاء والنتائج. لا تستخدم الإنجليزية في التواصل
التفسيري مع المستخدم إلا عند الحاجة التقنية.

تُترك بصيغتها الأصلية أسماء الملفات والمسارات، والأوامر، وأسماء المتغيرات
والدوال، والكود، وSQL، وأسماء الحزم والواجهات البرمجية، ورسائل الأخطاء
النصية التي ينتجها النظام أو الأدوات.

أما قواعد عزل اتجاه النص (`RLM`/`LRM`) فخاصة بواجهة كل وكيل ولا تُنسخ
بينها: بعض الواجهات تُظهر `LRI`/`PDI` مربعات وبعضها يعرضها صحيحة.
يأخذها كل وكيل من ملف تهيئته عند المستخدم، لا من هذا الملف.

## 7. عقد الجولة — الاستهلاك والإيقاع

اعتُمد 2026-09-03 بـق-121. الغرض: أقصر طريق إلى نتيجة صحيحة، بأقل قراءة
مكرّرة. التوفير يأتي من **عدم التكرار** لا من تحقق أقل.

- **ابدأ بـ`npm run c:state`** لا بقراءة ملفات. وبقيّة الفحوص:
  `npm run c:app` و`npm run c:db` و`npm run cloud:verify`. كلها تطبع
  ملخّصًا بأسطر معدودة وتكتب سجلها الكامل في
  `${TMPDIR:-/tmp}/wi-checks/`، فالمالك يلصق أسطر `STEP` و`RESULT` و
  `NODE_BIN` و`DONE` وحدها — لا السجل. وعند الفشل السطور القليلة تحتها.
  وإن عجز `git` عن العمل (مجلد عمل محجوب) فحقوله كلها تُطبع `BLOCKED` و
  النتيجة `RESULT=BLOCKED_GIT`: **لا تُقرأ `0` ولا تُستنتج نظافة الشجرة**.
- **عمق التفكير يُعلَن قبل الجولة.** عالٍ: تشخيص سبب مجهول، أو مسّ
  الصلاحيات أو المال أو القاعدة. متوسط: إصلاح بند معروف في ملف معروف
  (الافتراضي). منخفض: تنفيذ قرار متَّخذ، أو نصّ، أو أدوات، أو إجابة سؤال.
  وإن لم يُعلَن، يختار الوكيل ويُعلن الكلمة في أول سطر من ردّه.
- **شكل التقرير للمالك: أربعة عناوين بلا مصطلحات برمجية** — ما الحال الآن
  (بتجربة مستخدم التطبيق) / ما سنفعله / ما سيتغيّر بعد التنفيذ / ما
  أحتاجه منك. الأرقام والتفاصيل التقنية تُكتب في وثائق المستودع لا في
  الرد. وقبل أي أمر يُسلَّم: أربعة أسطر — وضع المالك الآن، وما سيتغيّر،
  ولماذا، وترجمة أي مصطلح.
- **لا إعادة قراءة** ملف قُرئ في الجلسة، ولا قراءة ملف بعد تعديله
  للتأكد، ولا قراءة ملف هجرة كامل لإثبات وجود عمود أو قيد أو دالة.
- **لا وكلاء فرعيين** إلا بطلب صريح من المالك.
- ق-113 وقاعدة «رقم مُشغَّل لا مكتوب» نافذتان على الجولة السريعة كما على
  البطيئة. الاختصار في القراءة والسرد، لا في الإثبات.
