# الهجرات

**آخر تحديث:** 2026-08-12

سجل ملفات هجرة قاعدة البيانات، وحالة كل ملف: هل كُتب؟ وهل **طُبّق فعليًا**؟ وهما أمران مختلفان تمامًا.

---

## الملفات الموجودة

### 001 — الإضافات والمخططات

* **الملف:** `supabase/migrations/20260805203100_001_extensions_and_schemas.sql`
* **الحجم:** نحو 3.3 كيلوبايت
* **المحتوى:** تفعيل الإضافات وإنشاء المخططات التسعة
* **حالة التطبيق:** غير مُثبتة. لا دليل على تشغيلها على قاعدة بيانات حية.

### 002 — المستأجرون والاشتراكات

* **الملف:** `supabase/migrations/20260805204500_002_tenants_and_subscriptions.sql`
* **الحجم:** نحو 13 كيلوبايت، 533 سطرًا
* **المحتوى:** جداول المستأجرين والاشتراكات
* **حالة التطبيق:** غير مُثبتة.

---

## تحذير مهم — الملفان أقدم من القرارات

كُتب الملفان في 2026-08-05، أي **قبل** حسم القرارات الـ 47 في 2026-08-12. لذلك هما لا يعكسان على الأرجح:

* وحدة المال الجديدة جزء من ألف — ق-14.
* تخزين النسب بمليون جزء — ق-21.
* إلغاء التقريب — ق-01 وق-12.
* موقع جداول الاشتراكات في `billing` وليس `core` — ق-26.

**الترجيح:** إعادة كتابتهما من الصفر وفق الوثائق المرجعية المُعدّلة، لأن قاعدة البيانات فارغة ولا توجد بيانات حقيقية يُخشى عليها. انظر م-04.

---

## قواعد الهجرات

1. كل هجرة لها رقم تسلسلي وطابع زمني في اسمها.
2. **لا تُعدّل هجرة طُبّقت أبدًا**؛ يُكتب ملف جديد يُصححها.
3. بعد تطبيق أي هجرة يُحدّث هذا الملف فورًا مع **دليل التطبيق**.
4. `PROGRESS.md` يسجل تاريخ التطبيق ونتيجته.
