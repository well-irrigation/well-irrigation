# سجل تغييرات الوثائق

**آخر تحديث:** 2026-09-03

يُوثّق هنا كل تغيير يطرأ على الوثائق المرجعية، حتى يُعرف لماذا اختلف النص عن الأصل.

---

## 2026-09-03 — ق-121: قصّ نقطة الاستئناف وعقد الجولة

- **الملفات:** `docs/memory/RESUME_POINT.md` (قُصّ)،
  `docs/memory/RESUME_HISTORY.md` (جديد)،
  `docs/memory/AI_HANDOFF_PROTOCOL.md` (§2 و§3)، `docs/PROJECT_MAP.md`
  («أين توقف العمل»)، `AGENTS.md` (§7 جديد)،
  `docs/memory/DECISIONS.md` (ق-121)، `docs/memory/PROGRESS.md`،
  `docs/memory/OPEN_ISSUES.md`. وأدوات: `scripts/check_state.sh` و
  `scripts/check_app.sh` و`scripts/check_db.sh` (جديدة) و
  `scripts/db_index.sh` (إصلاح ترتيب) و`package.json`.
- **ماذا تغيّر:** `RESUME_POINT.md` من **1383 → 140 سطرًا**؛ صار رأسًا
  يحمل الحالة والتالي والحدود الحيّة وما لا يجوز إعادة عمله والمؤجَّل.
  والسرد الزمني كله انتقل إلى `RESUME_HISTORY.md`. و`AI_HANDOFF` صار
  يسمّي الأرشيف في ترتيب القراءة (§2 البند 8) وفي قاعدة نقطة التوقف
  (§3)، ويضيفه إلى قائمة «ما لا يُستنتج منه NEXT». و`PROJECT_MAP` كذلك.
  و`AGENTS.md` كسب §7: عقد الجولة — الأوامر المختصرة، وعمق التفكير
  المُعلَن، وشكل التقرير الرباعي، وقواعد عدم إعادة القراءة.
- **لماذا:** الملف كان يُقرأ كاملًا في كل جلسة بحكم §3 وينمو كل جولة،
  فصار أكبر بند استهلاك متكرّر في المشروع بلا أن يزيد المعرفة المطلوبة
  للجولة. ولأن ق-93 §10 تمنع بقاء معلومة حاكمة في المحادثة وحدها، نُقل
  عقد العمل المتفق عليه إلى `AGENTS.md` لا إلى ذاكرة وكيل.
- **القرار المبرّر:** ق-121، وق-93 §10، والثابت 699 (لا نجاح كاذب في
  أدوات التحقق نفسها).
- **ما لم يُفقد:** جسم `RESUME_HISTORY.md` مطابق للأصل **بايتًا ببايت**
  (أُثبت بـ`cmp`، لا بالدعوى). والقواعد الحيّة والتحذيرات والمؤجَّلات
  **لم تُنقل** إلى الأرشيف بشرط صريح في ق-121: قاعدةٌ حيّة في أرشيف لا
  يقرأها أحد.
- **ما لم يتغيّر:** لا هجرة (السقف 071–093، والتالي 094)، ولا عقد `api`،
  ولا سلوك في التطبيق، ولا تخفيف لأي بوابة. ق-120 تبقى نافذة.

## 2026-09-02 — م-41D7: سلطة قراءة التسعيرة للمشغل (Migration 093)

- **الملفات:** قاعدة — `supabase/migrations/20260902020001_093_price_read_for_operators.sql`
  (جديد)، `supabase/tests/20260902_093_price_read_for_operators.test.sql`
  (جديد)، و`supabase/tests/20260819_080_permission_authority_foundation.test.sql`
  و`supabase/tests/20260822_081_permission_enforcement_money.test.sql`
  (أرقام الحرس فقط، بتعليق يسمّي 093).
  وثائق — `MIGRATIONS.md`، `INVARIANTS.md` (694–698)،
  `API_ARCHITECTURE.md`، `OPEN_ISSUES.md`، `PROGRESS.md`،
  `RESUME_POINT.md`، وهذا الملف. **لا تغيير في Flutter.**
- **نوع التغيير:** تنفيذي (هجرة سلطة) + توثيقي. **استثناء مالكي معلَن
  من ق-120**: هجرة لا شاشة جديدة ولا ميزة جديدة.
- **ما كان:** `api.get_active_price_schedule` (091) يفحص `price.manage`،
  وهي لحزمة `tenant_owner` وحدها (080). فالمشغل — وهو من يبدأ الجلسة —
  يتلقى `42501` ويرى لافتة صلاحية بلا سعر ساعة. وكان مسجَّلًا كقرار
  مفتوح في آخر جولة، فحسمه المالك: **يجب أن يرى المشغل سعر الساعة**.
- **ما صار:** صلاحية اطلاع مستقلة `price.read` ممنوحة لـ`tenant_owner`
  و`well_manager` و`operator`؛ وقارئ داخلي
  `ops.read_active_price_schedule(uuid, timestamptz)` — `stable` +
  `security definer` + `search_path` مثبت — يحمل الفحص المسمّى؛ وغلاف
  `api` يبقى `INVOKER` رقيقًا (جلسة `28000`، مدخل `22023`، رؤية البئر
  عبر RLS 079 `42501`) ثم يفوّض بلا تكرار قرار الصلاحية.
- **لماذا هجرة لا سطر:** المنع طبقتان. سياسات 031 تحصر `SELECT` على
  `ops.price_schedules` و`ops.price_rules` بالمالك، فتخفيف الفحص
  المسمّى وحده داخل عقد `INVOKER` يُعيد صفر صفوف → `schedule: null`،
  أي **غياب كاذب** يقول «لا تسعيرة» والتسعيرة موجودة. الطبقتان
  تتحركان معًا: منح للقراءة + تجاوز RLS في نقطة واحدة مُراجَعة، مع
  إبقاء الجدولين مغلقين على المالك.
- **القرارات المبرِّرة:** ق-78 (حدّ `api`)، ق-79 (لا كتابة من
  `INVOKER`، ومنه نمط الزوج)، ق-82، ق-99، واختبار 084 PASS 7 (الغلاف
  لا يكرّر قرار الصلاحية).
- **القياس:** كتالوج `iam.permissions` **41 ← 42**،
  و`iam.role_permissions` **75 ← 78** (مالك 42، مدير 14، مشغل 22)؛
  اختبار جديد بـ**15 تحققًا**؛ والحزمة ✅ **مثبتة 2026-09-02**:
  `FILES=31 PASS=500 FAIL=0 ERROR=0` (485 + 15)، وهدف 093 = 15/15،
  و`db:index` أعاد التوليد (`functions.txt` 437 سطرًا). ولا تشغيل على
  جهاز حقيقي.
- **تحذير النشر:** دمج الهجرة في `main` ينشرها على الإنتاج تلقائيًا،
  وأثرها هناك **توسيع اطلاع**: كل مشغل ومدير مُعيَّن نشط يرى تسعيرة
  بئره بعد الدمج.
- **النشر والتحقق السحابي (تحديث 2026-09-02):** دُمج `PR #20` مضغوطًا
  في `main` عند `787eee0`، و`npm run cloud:verify` أعطى
  `MIGRATIONS_LOCAL=92` / `MIGRATIONS_CLOUD=92` / `MISSING_IN_CLOUD=0`،
  و`FUNCTIONS_INDEX=424` / `FUNCTIONS_CLOUD=426` /
  `FUNCTIONS_MISSING_IN_CLOUD=0`، و`IAM_PERMISSIONS=42` /
  `IAM_ROLE_PERMISSIONS=78`. فالمُثبت سحابيًا ليس وجود الملف وحده بل
  وجود القارئ الجديد والعقد المستبدل، ووصول المنح على مستوى البيانات.

## 2026-09-02 — باب دخول موحَّد للوكلاء: `AGENTS.md`

- **الملفات:** `AGENTS.md` (جديد، جذر المستودع)، `CLAUDE.md` (جديد،
  مؤشِّر من ثلاثة أسطر)، `docs/memory/AI_HANDOFF_PROTOCOL.md`،
  `docs/PROJECT_MAP.md`.
- **السبب:** المالك يشغّل وكيلًا آخر على الجهاز نفسه ويريد استئنافًا بلا
  انقطاع. والفحص أظهر أن المستودع بلا أي ملف تعليمات في الجذر: كل وكيل
  جديد يبدأ أعمى إلى أن يُلصق له سياق يدويًا. و`AGENTS.md` هو الاصطلاح
  الذي تقرأه معظم الواجهات تلقائيًا، و`CLAUDE.md` لعائلة Claude.
- **ما نُقل من المحادثة إلى المستودع (ق-93 §10):** عقد «من ينفّذ ماذا»
  (المالك يشغّل القاعدة وDocker وGit والدمج؛ الوكيل يكتب الكود ويشغّل
  `flutter analyze`/`test` وحدهما)، وحدّ **٦٠ محرفًا** لسطر الأمر،
  وحقيقة أن القناة السحابية الوحيدة هي المنفذ 6543 وأن
  `supabase db push` يفشل، وأن **دمج `main` = نشر إنتاج** وأن الفرع
  محميّ بضابط نافذ، وأن `supabase/tests` لا تُشغَّل سحابيًا، وأن الخطة
  المجانية بلا نسخ احتياطية فالتراجع بهجرة جديدة، ومنع طبع كلمة المرور
  أو رابط اتصال غير مُقنَّع.
- **تصحيح تقادم في `AI_HANDOFF_PROTOCOL.md`:** «لا تعدّل 071–087» و«يبدأ
  الجديد من 088+» صارتا **071–093** و**094+**؛ و«Push إلى `main`» صارت
  فرعًا فطلب دمج فدمجًا مضغوطًا؛ وقائمة طابور التدقيق المنسوخة داخله
  حُذفت لأنها كانت تتقادم كل جولة، واستُبدلت بإحالة إلى
  `RESUME_POINT.md` مع حالة مختصرة بتاريخها.
- **القرار المبرِّر:** ق-93 (لا معلومة حاكمة تبقى في المحادثة وحدها)
  وق-96 (بروتوكول أوامر الطرفية).
- **ما لم يُوضع فيه:** قواعد عزل اتجاه النص العربي (`RLM`/`LRM`) بقيت
  في تهيئة كل وكيل عند المستخدم، لأن الواجهات تختلف: بعضها يُظهر
  `LRI`/`PDI` مربعات وبعضها يعرضها صحيحة، فتوحيدها في المستودع كان
  سيكسر أحد الطرفين.



- **الملف:** `scripts/cloud_verify.sh` (+76/−20). لا هجرة ولا تغيير في
  القاعدة ولا في `Flutter`. دُمج مضغوطًا في `PR #21` عند `8123448`.
- **نوع التغيير:** إصلاح عطب في أداة التحقق نفسها.
- **قبل:** متغير `CONTRACTS` يحمل أسماء عقود هجرة 092 الخمسة **مكتوبة
  يدويًا**، فيفحصها إلى الأبد ويمرّ على كل جولة بعدها بصمت. أول تشغيل
  بعد نشر 093 طبع `CONTRACT_OK` خمس مرات ثم «نجح التحقق السحابي: القرص
  والسحابة متطابقان» بلا أن ينظر إلى شيء من 093.
- **بعد:** القائمة اليدوية نُزعت. الفحص يشتق كل `schema.function` من
  الفهرس المولَّد `docs/technical/db/functions.txt` (424 اسمًا فريدًا،
  52 في `api`) ويقارنها بـ`pg_proc` السحابية بنفس مُرشِّح مخططات
  التطبيق المستخدَم في `scripts/db_index.sh`. الناقص سحابيًا يُفشل
  الفحص، والزائد يُطبع كانحراف بلا إفشال (الفهرس محلي وقد يتقادم إن
  نُسي `db:index`). وأُضيف عدّ `iam.permissions` و`iam.role_permissions`
  السحابيين **كقياس لا كحُكم**.
- **لماذا لا إضافة أسماء 093 إلى القائمة:** ذلك يؤجّل العطب جولةً
  واحدة. العطب في **شكل** التوقع لا في محتواه: المكتوب يدويًا يتقادم،
  والمشتقّ من مُخرَج يُعاد توليده لا يتقادم. ولذلك أيضًا لم تُثبَّت
  الأرقام 42/78 في السكربت.
- **القرار المبرِّر:** ق-113 (لا نجاح كاذب) تنطبق على أدوات التحقق كما
  تنطبق على الواجهة — بل أَولى، لأن الأداة تُصادق على البقية.
- **مؤجَّل بقصد:** استثناء الدوال المملوكة لإضافة عبر `pg_depend` في
  السكربت وفي المولِّد معًا، لإسكات `public.max` / `public.min`
  الزائدتين سحابيًا. مُسجَّل في `OPEN_ISSUES.md`.

## 2026-09-02 — م-41D6: تسعيرة شاشة التشغيل من `api.get_active_price_schedule`

- **الملفات:** كود — `apps/mobile/lib/features/operations/operations_screen.dart`،
  `apps/mobile/lib/core/session/offline_session_coordinator.dart`،
  `apps/mobile/lib/core/api/operations_repository.dart`
  (`kSessionEnergySources`).
  اختبارات — `test/core/api/data_api_boundary_test.dart` (مقياس التسعيرة
  مقلوبًا)، `test/features/operations/operations_pricing_test.dart` (جديد).
  وثائق — `OPEN_ISSUES.md`، `PROGRESS.md`، `RESUME_POINT.md`، وهذا الملف.
- **نوع التغيير:** تنفيذي (إصلاح تدقيق داخل ق-120) + توثيقي. **لا**
  Migration ولا عقد `api` جديد — `api.get_active_price_schedule` موجود
  منذ 091 — ولا شاشة جديدة.
- **ما كان:** زرّان ثابتان في الشاشة، «طاقة شمسية ☀️ — 3,500 ريال /
  ساعة» و«ديزل شامل ⛽ — 5,000 ريال / ساعة»، والمستحق الحيّ وسند القبض
  يُحسبان منهما. و«ديزل شامل» تجمع `well_diesel` و`farmer_diesel`
  المختلفَي السعر، والنص العربي يُرسل حرفيًّا في `p_energy_source` وهجرة
  066 (السطر 72) ترفض كل ما ليس الرموز الثلاثة بـ`22023` — فالكتابة
  مرفوضة خادميًّا والرقم المعروض بلا سند في الطرفين. والمنسّق يحمل
  الرقمين نفسهما في `PricingResolver` الافتراضي فيُسعّر أي مقطع بـ3500.
- **ما صار:** السعر وحده صار من العقد، وخيارات المصدر من
  `kSessionEnergySources` — مصادر هجرة 066 الثلاثة — لأن اختيار المصدر
  قرار تشغيلي. قاعدة بلا `hourly_rate_minor` تُعرض «التسعيرة غير
  متوفرة»؛ غياب الجدول أو فشل القراءة يُعلَن في لافتة فوق الأزرار مع
  «إعادة المحاولة» بلا أي سعر؛ رفض `42501` يُعرض كحالة صلاحية بلا زرّ
  إعادة محاولة؛ المستحق الحيّ بلا سعر = نصّ القرار 341 لا «0 ريال»؛
  ونهاية الجلسة بلا سعر تُسجَّل الجلسة ويُقال إن لا سند من هنا. والمنسّق
  بلا أسعار افتراضية (`PricingResolver.none()`) ومعه `updatePricing`
  تُحلّ اللقطات المقروءة وتُعيد بناء المُسقط.
- **تصحيح داخل الجولة نفسها:** أول تنفيذ اشتقّ **أزرار المصدر** من
  قواعد السعر ومنع البدء بـ«لا مصدر طاقة مُسعَّر لهذا البئر». وقراءة
  هجرتَي 080 و091 تُظهر أن `price.manage` لحزمة `tenant_owner` وحدها
  بينما `operator` يُمنح `session.start`، وأن
  `ops.start_irrigation_session` لا تأخذ سعرًا وتُثبّت السعر خادميًّا في
  `ops.create_priced_session_segment`. أي أن ذلك المنع كان يحجب عن
  المشغل عملًا يقبله الخادم — فشل كاذب يقابل النجاح الكاذب المُزال —
  فأُزيل، وبقي حرس واحد: «يرجى تحديد مصدر الطاقة قبل بدء السقي».
- **القرارات المبرِّرة:** ق-99 (لا حساب مال في العميل)، القرار 341 (نصّ
  بانتظار المزامنة **بدل** الرقم)، ق-113، ق-120.
- **القياس:** الحرس **22/22** ومقياس التسعيرة انقلب من إثبات وجود
  `_clientSideRateFor` إلى إثبات غيابه، ومعه مقياس «التسعيرة لا تحجب
  التشغيل»؛ الحزمة **294 ← 300 PASS** (+6 اختبارات تسعيرة)؛
  `flutter analyze` = No issues found. لا تشغيل على جهاز حقيقي.

## 2026-09-02 — م-41D5: إغلاق البند 8 (تخصيص الرصيد المقدَّم) + المقياس السادس

- **الملفات:** كود — `apps/mobile/lib/features/finance/farmer_financial_account_screen.dart`،
  `apps/mobile/lib/core/api/finance_repository.dart` (تعليق فقط).
  اختبارات — `test/core/api/data_api_boundary_test.dart` (المقياس السادس)،
  `test/features/finance/farmer_financial_account_screen_test.dart`.
  وثائق — `OPEN_ISSUES.md`، `API_ARCHITECTURE.md`، `PROGRESS.md`،
  `RESUME_POINT.md`، وهذا الملف.
- **نوع التغيير:** تنفيذي (إصلاح تدقيق داخل ق-120) + توثيقي. **لا**
  Migration، ولا عقد `api` جديد، ولا شاشة جديدة.
- **ما كان:** زر «استخدام الرصيد المقدم» يفتح نافذة تقول «سيتم استخدام
  الرصيد لتسديد أقدم الفواتير المستحقة التالية»، ثم يرسل
  `allocateAdvance(paymentId: 'mock-advance-pay', allocations: [])` —
  معرّف ليس `uuid` وقائمة فارغة — ويعلن «تم استخدام الرصيد المقدم في
  تسديد الفواتير بنجاح ✅». ثلاث كذبات في نافذة واحدة: الفواتير
  المعروضة لم تكن مقصودة بالتخصيص، و«أقدم» والعقد يعيد الأحدث،
  والرسالة تعلن ما لم يُرسل.
- **ما صار:** الزر «الرصيد المقدم (غير متاح)»، والنافذة
  `_AdvanceUnavailableDialog` — `StatelessWidget` بلا مستودع ولا زر
  تأكيد — تعرض الرصيد كما أعاده العقد، وتصرّح بأن التسديد غير متاح في
  هذا الإصدار وأن **لا أمر أُرسل** وأن الرصيد والدين لم يتغيّرا،
  والفواتير موسومة «عرض فقط».
- **لماذا لم يُوصَّل التخصيص:** عقد الكتابة موجود
  (`api.allocate_payment`) والمستودع يناديه بصدق، لكن القراءة تعيد
  الرصيد المقدَّم رقمًا **مُجمَّعًا** والسندات بأرقام فواتيرها بلا مبالغ،
  فلا يُعرف أي سند بقي فيه رصيد ولا كم. اختيار السند في العميل قرار
  مالي يخترعه العميل — ق-99 تمنعه، والزناد
  `billing.split_overpayment_to_advance` يؤكد أن المنطق مملوك للخادم.
- **القرارات المبرِّرة:** ق-99 (لا تلفيق مال)، ق-113 (لا نجاح كاذب)،
  ق-120. السابقة الحاكمة: م-41B3B.
- **القياس:** دين المعرّفات المُلفَّقة **1 ← 0**؛ الحرس **21 ← 22**؛
  اختبارات الشاشة **4 ← 5** (فيها جاسوس يُثبت أن عدّاد نداءات التخصيص
  = 0)؛ `flutter analyze` نظيف؛ الحزمة **292 ← 294 PASS**.

---

## 2026-09-02 — م-41D4: إصلاح النجاح الكاذب في مسار التشغيل + المقياس الخامس

- **الملفات:** كود — `apps/mobile/lib/core/api/operations_repository.dart`،
  `apps/mobile/lib/features/operations/operations_screen.dart`،
  `apps/mobile/lib/features/operations/widgets/payment_receipt_dialog.dart`،
  `apps/mobile/lib/core/widgets/top_well_selector.dart`.
  اختبارات — `test/core/api/data_api_boundary_test.dart` (المقياس الخامس)،
  `test/core/api/operations_write_contract_test.dart` (جديد)،
  `test/features/operations/operations_failure_paths_test.dart` (جديد)،
  `test/features/operations/payment_receipt_dialog_test.dart`. وثائق —
  `OPEN_ISSUES.md`، `PROGRESS.md`، `RESUME_POINT.md`، وهذا الملف.
- **نوع التغيير:** تنفيذي (إصلاح تدقيق داخل ق-120) + توثيقي. **لا**
  Migration، ولا عقد `api` جديد، ولا شاشة جديدة، ولا `W2-02d`.
- **النطاق المُعلن كان ثلاثة مواضع (5، 6، 7)، والمُصلَح خمسة عشر:**
  القياس أثناء التنفيذ كشف أن العائلة نفسها تمتد على كامل مسار الجلسة،
  لا على ثلاثة أسطر. إصلاح ثلثها كان سيُبقي المسار كاذبًا: يُرفض بدء
  الجلسة صراحةً ثم يُقبل إنهاؤها بفاتورة مُلفَّقة. التفصيل في
  `OPEN_ISSUES.md`.
- **ما تغيّر:** كتابات الجلسة الخمس في المستودع ترفع
  `StateError('Supabase client is unavailable')` بلا عميل — نفس استثناء
  القراءات في الملف نفسه، فلا اصطلاح جديد — بدل معرّف جلسة مُلفَّق
  وأربع عودات صامتة وفاتورة `10500` لم تُحسب في القاعدة. وفي الشاشة
  زالت مصائد الاستثناء الفارغة: كل كتابة تفشل تُعرض في `SnackBar`
  و**لا** تُغيَّر الحالة المعروضة (العداد لا يبدأ، الزر لا ينقلب، السند
  لا يُفتح). وحوارا إضافة المزارع والأرض يرفضان العمل بلا بئر نشط بدل
  تلفيق كيان وإغلاق النافذة كأن الحفظ نجح.
- **موضعان خارج القائمة المُعلنة:** الشريط العلوي كان يبني `WellSummary`
  بمستأجر وأدوار مُفترضة، ويسمّي بئر العرض التجريبي عند غياب البئر
  النشط؛ صار يقول «لا بئر مختار». وزر الطباعة الحرارية كان ينتظر
  `600ms` ثم يعلن «تم إرسال أمر الطباعة» بلا أي تكامل بلوتوث؛ صار يعلن
  عدم التوفر صراحةً.
- **القرارات المبرِّرة:** ق-113 (لا نجاح كاذب)، ق-114، ق-90 بند 20،
  ق-120 (لا توسيع وظيفي). السابقة الحاكمة: م-41B3B — تُزال الكذبة
  وتُعرض حالة صريحة، ولا يُختلق خادم.
- **دين مُعلَن جديد (لا يُغلق هنا):** تسعيرة الساعة ما زالت محسوبة في
  العميل (3500 / 5000) بدل `api.get_active_price_schedule`. كانت ثلاثة
  أرقام متفرقة في الشاشة؛ جُمعت في ثابتين مُسمّيين ودالة واحدة
  `_clientSideRateFor`، ومُثبتة في الحرس بعدّ واحد لكل رقم حتى لا تنمو.
  توصيل عقد التسعيرة جولة مستقلة.
- **المقياس الخامس:** الحرس 17/17 ← **21/21**. الأربعة الجديدة تقفل:
  كتابات المستودع (لا عودة صامتة، ولا معرّف مُلفَّق، ولا فاتورة
  مكتوبة، وعدد الحراسات = عدد الاستثناءات ≥ 12، والعقود الخمس موجودة)،
  وشاشة التشغيل (لا مصيدة فارغة، ولا كيان مُلفَّق، مع سبع رسائل فشل
  صريحة)، والسند (لا ادعاء طباعة)، والشريط العلوي (لا اسم بئر مُلفَّق).
  ودين المعرّفات المُلفَّقة انكمش **3 ← 1**، ودين `'well-1'` انكمش
  **47/15 ← 45/14**.
- **اختبارات سلوكية لا نصية فقط:** ستة اختبارات وحدة تُثبت أن الكتابات
  الخمس ترفع الاستثناء فعلًا (والسادس يُثبت أن القراءة ترفع نفسه)،
  وثلاثة اختبارات واجهة بمنسق يفشل في الإيقاف والإنهاء تُثبت أن الرسالة
  تظهر وأن الحالة المعروضة لم تتغيّر ولم يُفتح سند.
- **التحقق:** `flutter analyze` = نظيف؛ الحزمة **292/292 PASS**
  (من 278). لا تشغيل على جهاز حقيقي.

---

## 2026-09-02 — م-41D3: إصلاح النجاح الكاذب في `AccountRepository` + الحرس الرابع

- **الملفات:** كود — `apps/mobile/lib/core/api/account_repository.dart`،
  `apps/mobile/lib/core/session/offline_session_coordinator.dart`،
  `apps/mobile/lib/features/settings/device_sync_screen.dart`،
  `apps/mobile/lib/features/settings/more_settings_screen.dart`،
  `apps/mobile/lib/features/settings/profile_security_screen.dart`.
  اختبارات — `test/core/api/data_api_boundary_test.dart` (الحرس الرابع)،
  `test/core/api/account_repository_explicit_state_test.dart` (جديد)،
  وثلاثة ملفات اختبار لشاشات الإعدادات. وثائق — `OPEN_ISSUES.md`،
  `PROGRESS.md`، `RESUME_POINT.md`، وهذا الملف.
- **نوع التغيير:** تنفيذي (إصلاح تدقيق داخل ق-120) + توثيقي. **لا**
  Migration، ولا عقد `api` جديد، ولا شاشة جديدة، ولا `W2-02d`.
- **ما تغيّر:** البنود 1–4 من قائمة النجاح الكاذب. كلمة المرور لم تعد
  تبتلع الاستثناء ولا تعلن نجاحًا بلا جلسة؛ تشخيص الجهاز يُقرأ من
  الطابور بدل ثوابت مكتوبة، وغير المقيس يُكتب «غير مقيس» لا «يعمل»؛
  المزامنة اليدوية تُعلن عدم توفرها بدل تأخير `100ms` ثم نجاح؛ وحرس
  الخروج يفشل مغلقًا بدل أن يعيد `0`.
- **القرارات المبرِّرة:** ق-113 (لا نجاح كاذب)، ق-118، القرار 541،
  القرار 578، ق-90 بند 20 (الجاهزية تُقاس ولا تُدَّعى)، ق-120 (لا
  توسيع وظيفي). السابقة الحاكمة: م-41B3B.
- **لماذا لم يُوصل قياس حقيقي للاتصال والمزامنة الخلفية:** ذلك عمل
  `W2-02d` وتمنعه ق-120. البديل الصادق: `null` = غير مقيس، يُعرض
  للمستخدم بهذه الصفة، ولا يُترجم إلى «متصل».
- **الحرس الرابع:** 13/13 ← **17/17**. القوائم الثلاث تقيس باب القاعدة؛
  والأربعة الجديدة تقيس الصدق: دين المعرّفات المُلفَّقة = 3، ودين
  `'well-1'` = 47 موضعًا في 15 ملفًا (قياس جديد أكبر مما كان موصوفًا)،
  وقفل نصّي على `AccountRepository` وعلى شاشات الإعدادات الثلاث.
- **التحقق:** `flutter analyze` نظيف؛ الحزمة **278/278 PASS** (من 265).
  لا تشغيل على جهاز حقيقي.

---

## 2026-09-02 — قياس أرقام الدين بعد 092 + فهرس المخطط

- **الملفات:** `docs/memory/OPEN_ISSUES.md`، `docs/memory/PROGRESS.md`،
  `docs/memory/RESUME_POINT.md`، وهذا الملف. وأُنشئ `docs/technical/db/`
  (أربعة ملفات مولَّدة: `columns.txt` / `constraints.txt` /
  `functions.txt` / `triggers.txt`) بأمر `npm run db:index`.
- **القياس:** الدين المُعلَن الثلاثي = **0** بمقياسين مستقلين تطابقا
  (مسح `python3` على كامل نص 76 ملف Dart، + حرس الحد `13/13`).
  عقود `api` المستعملة = 38؛ `flutter analyze` نظيف؛ الحزمة 265/265.
- **الإضافة الجوهرية:** قسم جديد في `OPEN_ISSUES.md` يسجّل **ثمانية
  مواضع «نجاح كاذب»** لا تراها القوائم الثلاث لأنها لا تخاطب القاعدة
  أصلًا، بمواضعها الدقيقة وقرارها الحاكم وما يغلقها. أربعة منها في
  `AccountRepository`، وواحد كان معروفًا من قبل (`'mock-advance-pay'`).
- **لماذا:** الاكتفاء بالأرقام الثلاثة كان سيُعلن إغلاق ق-120 وهي مفتوحة.
  المقياس يقيس الباب لا الصدق، فلزم إعلان حدّه بدل توسيع دلالته.
- **لا تغيير كود:** جولة قياس وتوثيق فقط؛ صفر سطر في `apps/mobile/lib`.

---

- **الملفات:** `docs/technical/MIGRATIONS.md`، `docs/memory/RESUME_POINT.md`،
  `docs/memory/PROGRESS.md`، `docs/memory/OPEN_ISSUES.md`، وهذا الملف،
  و`scripts/cloud_verify.sh` و`package.json`.
- **الدمج:** الطلب `#13` دُمج مضغوطًا في `main` بالالتزام `87a0529`. سبقه
  دمج `origin/main` في الفرع (`f03e8c9`) لحل تعارض في ثلاث وثائق ذاكرة،
  سببه أن الطلب `#12` دُمج مضغوطًا فوصل نص م-41D1 مرتين بهُويّتين
  مختلفتين: نسخة `main` قبل تصحيح 2026-09-01 ونسختنا بعده. حُفظت نسختنا
  لأنها أشمل، وصُحّحت معها ثلاث حقائق تجاوزها الزمن.
- **النشر:** الدمج في `main` نشر 092 على قاعدة الإنتاج تلقائيًا عبر تكامل
  `GitHub` (`Deploy to production` = ON). النشر ليس خطوة منفصلة.
- **أداة جديدة:** `scripts/cloud_verify.sh` + `npm run cloud:verify`.
  قراءة فقط عبر القناة 6543، بلا `create` ولا `insert` ولا `drop`. تُصدر
  حكمًا صريحًا على كلمة المرور (`PASSWORD=OK` أو `PASSWORD=WRONG` أو
  `PASSWORD=UNTESTED`) قبل أي فحص، ثم تقارن ملفات `supabase/migrations`
  بصفوف `supabase_migrations.schema_migrations` وتسمّي الناقص، ثم تتحقق
  من وجود عقود api الخمسة.
- **النتيجة:** `PASSWORD=OK`، و`MIGRATIONS_LOCAL=91` مقابل
  `MIGRATIONS_CLOUD=91`، و`MISSING_IN_CLOUD=0`، وخمسة `CONTRACT_OK`.
- **الحد الباقي:** ملفات `supabase/tests` لا تُشغَّل سحابيًا، فحالة السحابة
  = مطبَّقة ومُثبتة الوجود، غير مُتحقَّقة سلوكيًا.
- NEXT = إعادة قياس أرقام الدين بعد 092، ثم تقرير إغلاق ق-120.

## 2026-09-02 — تحقق 092 محليًا + فهرس مخطط القاعدة

- **الملفات:** `MIGRATIONS.md`، `PROGRESS.md`، `OPEN_ISSUES.md`،
  `RESUME_POINT.md`، `DOC_CHANGELOG.md`.
- **التحقق:** `db:reset` + `db:test` نجحا: Target 092 = 37 PASS،
  والحزمة 30 files / 485 PASS / 0 FAIL / 0 ERROR. خط الأساس انتقل
  من 448 إلى 485، وحالة 092 محليًا صارت **مطبَّقة ومُتحقَّقة**.
- **تصحيح موثَّق:** ملف اختبار 092 نفسه حمل ستة عيوب في بيانات
  التهيئة (أعمدة أسقطتها 075 و051، وثلاثة أزندة مالية، وخزان ديزل
  برصيد صفر). سُجِّل الدرس في `MIGRATIONS.md`: التطبيق الناجح لهجرة
  لا يُثبت صحة أسماء الأعمدة داخل عقودها.
- **أداة جديدة:** `scripts/db_index.sh` + `npm run db:index` تولّد
  `docs/technical/db/{columns,constraints,triggers,functions}.txt`
  من القاعدة الحقيقية، فيصير إثبات وجود عمود أو زناد بحثًا في سطر
  واحد بدل قراءة ملف هجرة كامل. الهدف: تقليل الاستهلاك في كل جولة.

---

## 2026-09-02 — م-41D2: عقود القراءة المالية وعقد التقارير (Migration 092)

- **الملفات:** `MIGRATIONS.md`، `API_ARCHITECTURE.md`،
  `INVARIANTS.md`، `DECISION_IMPLEMENTATION_MATRIX.md`،
  `OPEN_ISSUES.md`، `PROGRESS.md`، `RESUME_POINT.md`،
  `DOC_CHANGELOG.md`.
- **نوع التغيير:** تنفيذي + توثيقي. Migration جديدة **092** بخمسة
  عقود قراءة في `api`: `list_well_expenses` و`list_well_partners`
  و`list_well_profit_cycles` و`get_farmer_account` و
  `get_reports_summary`. الخمسة INVOKER + STABLE +
  `search_path = pg_catalog, pg_temp`، fail-closed
  `28000`/`22023`/`42501`، حدود مقصوصة، ترتيب حتمي داخليًا
  وخارجيًا، anon محجوب، ولا كائن علائقي في `api`.
- **القرارات المبرِّرة:** ق-82 (حدّ `api` وحده)، ق-88، ق-98،
  ق-99 (لا حساب مال في عقد قراءة)، ق-113 (لا نجاح كاذب)، ق-120
  (لا توسيع وظيفي).
- **لماذا تغيّر:** الشاشات المالية لم تقرأ بيانات حقيقية ولا مرة.
  خمس قراءات `from('schema.table')` على مخططات غير مكشوفة، كل
  واحدة ملفوفة بـ`catch(_)` يعيد بيانات تجريبية، فالفشل يُعرض
  نجاحًا ومالٌ ثابت في العميل يُعرض كأنه حساب.
- **الدين كان أكبر من الموثَّق:** ثلاثة مما كانت تخاطبه لا وجود له
  أصلًا — `iam.well_memberships` (جدول)، و`public.farms` (مخطط
  وجدول)، و`billing.invoices.issue_date` (عمود؛ الحقيقي
  `invoice_date`). فلم تكن قراءات تفشل شبكيًا بل قراءات لأشياء
  وهمية.
- **والعلاج كان أصغر:** مخطط `reporting` يحسب هذه الأرقام منذ 060
  (`partner_account_summary`، `farmer_account_balances`)، والمدفوع
  للشريك عمود حقيقي `paid_minor` من 068. فالعقود توصيل لا اختراع.
- **استثناء ق-99 واحد معلَن:** `remaining_minor` في سطر توزيع
  الشريك بتعريف 068 نفسه (`net_payable_minor − paid_minor`). أما
  «صافي التدفق» فتحسبه الشاشة من رقمين مُعادين لا العقد.
- **اختبار دائم جديد:**
  `20260902_092_finance_read_contracts.test.sql` (**37 تحققًا**).
- **Flutter:** مولّدات المحاكاة الأربع أُزيلت، ومعها هوية المزارع
  ورصيده المقدَّم الثابتان ومال الشركاء المحشور وأرقام التقارير
  الخمسة و`_getMockReportSummary`. لا `catch` في المستودعات؛ ست
  شاشات تُظهر فشل العقد نصًّا عربيًا. كشف حساب الشريك الواحد صار
  تصفية محلية لقائمة الشركاء، فلا عقد ثانٍ ولا نداء لكل شريك.
- **إصلاح تخطيط:** ترويسة كرت دورة الأرباح كانت تفيض 101 بكسل عند
  الحالة `calculated`؛ أُصلحت بمقاس مرن + `ellipsis`. لم تكن ظاهرة
  وقت المحاكاة لأن المحاكاة لم تُنتج تلك الحالة.
- **الدين المعلَن كله = 0:** Bare RPC **1 → 0**، Dotted-from
  **5 → 0**، Internal-schema **0**. القوائم الثلاث في
  `data_api_boundary_test.dart` فارغة، ومعها ثلاثة فحوص إيجابية
  تثبّت التوجيه عبر `api` وغياب المحاكاة وحضور رسائل الفشل الست.
- **الإثبات:** `flutter analyze` = **No issues found**؛ Full
  Flutter = **265/265 PASS** (كان 258؛ +7).
- **خط الأساس:** **لم يتغيّر** — `db:reset` + `db:test` لم تُشغَّلا
  بعد، فحالة 092 على القاعدة = مكتوبة لا مُتحقَّقة. المتوقع إن
  نجحت: Target 092 = 37، والحزمة 30 files / 485 PASS.
- **بند مفتوح جديد:** `'mock-advance-pay'` — معرّف دفعة مُلفَّق في
  زر «استخدام الرصيد المقدم» (مسار كتابة). إصلاحه يحتاج عقدًا
  وواجهة، وكلاهما توسيع تمنعه ق-120.
- **تحذير نشر مسجَّل:** دمج 092 في `main` ينشرها على قاعدة الإنتاج
  تلقائيًا عبر تكامل `GitHub` بلا اختبارات هناك؛ فحماية الفرع يجب
  أن تكون محفوظة وفعّالة قبل الدمج.
- **ثوابت جديدة:** 689–693 في `INVARIANTS.md` — وحدات الأساس
  والتحويل عرضيٌّ فقط، المستودع لا يبتلع الفشل، أسبوع التقارير
  يبدأ السبت ويُعلَن في `week_starts_on`، لا حساب مال في عقد قراءة
  إلا الاستثناء المعلَن، ولا نداء لعقد قبل إثبات وجوده.
- **تصحيح ادعاءين قديمين:** ترويسة `API_ARCHITECTURE.md` كانت ما
  زالت تقول «فجوة مؤكدة م-41 — Repair Now» وتعلن 9/20/5، وصف م-41
  في `DECISION_IMPLEMENTATION_MATRIX.md` كان يعلن 7 internal + 9
  bare + 5 dotted وNEXT = م-41C. كلاهما صار 0/0/0.
- **بند مفتوح جديد ثانٍ:** تناقض سقف الهجرات المختومة — الوثائق
  تعلن `071–084` في 15 ملفًا و`071–087` في 4 و`071–088` في 2،
  والواقع على القرص `071–091` والتالي 092. سُجِّل في
  `OPEN_ISSUES.md` ولم يُوحَّد لأن تمريرة على 15 ملفًا خارج نطاق
  هذه الجولة وتُفقد أثر التغيير لو خُلطت بها.
- NEXT = تشغيل `db:reset` + `db:test`، ثم حماية `main`، ثم الدمج.

## 2026-09-01 — تصحيح: السحابة متزامنة تلقائيًا عبر تكامل GitHub

- **الملفات:** `MIGRATIONS.md`، `RESUME_POINT.md`،
  `OPEN_ISSUES.md`، و`PROGRESS.md`.
- **الخطأ المصحَّح:** كانت الوثائق تقول إن 089 و090 و091 «غير
  منشورة سحابيًا» وإن النشر خطوة يدوية معلَّقة. هذا خطأ.
- **الدليل:** صفحة `Database → Migrations` في لوحة المشروع تُظهر
  **90 صفًا** مقابل **90 ملفًا** في المستودع، والفرق **صفر**،
  ومنها 089 و090 و091. وصفحة `Settings → Integrations` تُظهر
  تكامل `GitHub` موصولًا بـ`well-irrigation/well-irrigation`،
  و`Deploy to production` = **ON**، وفرع الإنتاج = **`main`**.
- **الأثر الحاكم:** **كل دفع أو دمج في `main` يطبَّق على قاعدة
  الإنتاج تلقائيًا**، بلا تشغيل `supabase/tests` هناك. فالحالة
  السحابية الأدق = **مطبَّقة لا مُتحقَّقة**، وسكربت `psql` اليدوي
  صار مسارًا احتياطيًا لا أساسيًا.
- **خطر مسجَّل:** `ba4176c` دُفع إلى `main` مباشرة، فطبّق تغييرًا
  على قاعدة الإنتاج بلا مراجعة. حماية فرع `main` صارت ضابطًا
  أمنيًا. والخطة `FREE` بلا نسخ احتياطي ولا فروع معاينة (كلاهما
  `Pro`)، والمالك ينوي الترقية.
- **تصحيح عدد:** الملفات **90** لا 91؛ الرقم 067 غير مستخدم
  (066 → 068).
- **Baseline:** لم يتغير — لا كود ولا `migration` في هذا التحديث.

---

## 2026-09-01 — م-41D1: عقود إدارة البئر (Migration 091)


- **الملفات:** `MIGRATIONS.md`، `API_ARCHITECTURE.md`،
  `RESUME_POINT.md`، `OPEN_ISSUES.md`، و`PROGRESS.md`.
- **التغيير:** توثيق Migration 091 — أربعة عقود قراءة وثلاثة أزواج
  كتابة لإدارة البئر، وثلاثة أعمدة جديدة على `core.wells`، ورمزا
  صلاحية جديدان لـ`tenant_owner`. وتحديث أرقام الدين على حدّ
  Data API: Bare RPC من **9 إلى 1**، Internal-schema **0**،
  Dotted-from **5** كما هي.
- **إفصاحان مهمّان:**
  1. **ثلاثة حرّاس عدد عُدِّلت عن قصد** في اختبارَي الحقبة المقفلة
     080 و081 (فهرس الصلاحيات 39→41، الإجمالي 73→75). السبب رمزا
     الصلاحية الجديدان، وهو مذكور في نص التعديل.
  2. **`recordFuelPurchase` ضاق سلوكه**: العقد القائم
     `api.purchase_fuel` يأخذ البئر لا الخزان، ولا يقبل اسم مورّد
     ولا ملاحظة؛ فحُذف الحقلان من الشاشة وشُرح السبب فيها بدل
     الوعد بحفظٍ لا يحدث.
- **السبب:** ق-82 — لا وصول من العميل إلا عبر `api`؛ وق-99 — لا
  وحدات ولا أسماء مخترعة في العميل.
- **الطبيعة:** توثيق مصاحب لتنفيذ فعلي (SQL + Flutter).
- **Baseline:** Flutter **258/258 PASS**، `analyze` نظيف، وقاعدة
  البيانات محليًا **29 files / 448 PASS / 0 FAIL** ومنها 091 بـ34
  تحققًا. الحالة = **Verified local**؛ لا نشر سحابي لـ089/090/091.
- **ملاحظة حوكمية:** `commit ba4176c` دُفع إلى `main` مباشرة بلا
  `PR`، فلا مراجعة مسجَّلة لـم-41D1.

---

## 2026-08-30 — ق-120: بوابة التدقيق والتثبيت قبل أي توسع وظيفي

- **الملفات:** `DECISIONS.md`، `OPEN_ISSUES.md`، `RESUME_POINT.md`،
  `PROJECT_MAP.md`، `AI_HANDOFF_PROTOCOL.md`،
  `AI_COLLABORATION_PROTOCOL.md`، و`DECISION_IMPLEMENTATION_MATRIX.md`.
- **التغيير:** توثيق حالة Pre-Production ومرحلة Audit → Inspection →
  Evaluation → Repair → Gap Closing، وتأجيل أي شاشة أو Feature جديدة
  حتى اجتياز بوابة التثبيت.
- **الفجوات المؤكدة:** م-38 لصلاحيات `api.setup_well_full`، م-39
  لخطأ السعر ×100، وم-40 لـFalse Offline Success.
- **السبب:** ترتيب العمل حول إصلاح العيوب المؤكدة ثم Regression Testing؛
  لا إلغاء لأي UX أو Architecture معتمد.
- **الطبيعة:** توثيقي/حوكمي فقط؛ لم يتغير كود التطبيق أو Supabase أو
  migrations أو packages، ولم تُسجل أي فجوة كـImplemented أو Verified.
- **Baseline:** لم يتغير.

---

## 2026-08-25 — تنفيذ شاشات الحساب، الملف الشخصي، الفريق، الجهاز والمزامنة، والتفضيلات والدعم الفني (UX-16A / 527–600 / ق-101)

- **الشاشات والمكونات المنفذة:**
  - `MoreSettingsScreen` (UX-16A / 527): الشاشة المركزية للمزيد والإعدادات مع رأس الحساب الشخصي والوصول السريع إلى الأقسام الأربعة، وتسجيل الخروج الآمن بعد فحص العمليات المعلقة.
  - `ProfileSecurityScreen` (UX-16A / 528–545): إدارة الملف الشخصي، تعديل الاسم، بطاقة رقم الهاتف غير القابل للتكرار (ق-84)، وتغيير كلمة المرور وتفريغها الفوري الآمن.
  - `TeamPermissionsScreen` (UX-16A / 546–555): إدارة الفريق والمشغلين في البئر، حوار إضافة عضو جديد، وضوابط عدم تعطيل مشغل أثناء جلسة نشطة وحماية المالك الوحيد والسجلات التاريخية.
  - `DeviceSyncScreen` (UX-16A / 556–570): تشخيص صحة الجهاز والتخزين المحلي، العمليات المعلقة في الطابور المتين (Durable Outbox)، زر المزامنة اليدوية، وتفاصيل العمليات بلغة الأعمال.
  - `AppSettingsScreen` (UX-16A / 568–590): المظهر الميداني (فاتح/داكن/تلقائي)، إعدادات الطابعة الحرارية الميدانية 58mm/80mm عبر البلوتوث، تفضيلات الإشعارات، وتأكيد معايير الأرقام الإنجليزية.
  - `HelpSupportScreen` (UX-16A / 574–577): أرقام الدعم الفني وواتساب، سياق التشخيص الفني والتقني الآمن دون كشف أسرار، الشروط والخصوصية، ومعلومات عن التطبيق.
  - `AccountRepository`: مستودع إدارة الحساب والملف الشخصي والفريق وتشخيص المزامنة وفحص العمليات المعلقة قبل تسجيل الخروج ق-578.
  - ربط الشاشات في `HomeScreen` و `WellIrrigationApp`.
- **حالة الاختبارات والتحقق:**
  - `flutter test`: **220 PASS / 0 FAIL**.
  - `flutter analyze`: **No issues found! (0 issues)**.
  - `db:test`: **354 PASS / 0 FAIL**.

---

## 2026-08-25 — تنفيذ شاشات إدارة البئر والمضخات والوقود والتسعير والتقارير V1 (UX-15 / 460–526 / ق-100)

- **الشاشات والمكونات المنفذة:**
  - `WellManagementHubScreen` (UX-15 / 460–461): المدخل المركزي الموحد لإدارة البئر يجمع إعدادات البئر، المضخات، تعرفة الأسعار، والوقود مع مبدل الآبار.
  - `WellSettingsScreen` (UX-15 / 460–465): بيانات البئر الأساسية والعمق والموقع والحفظ الصريح، وضوابط منع الحذف التاريخي والتعطيل أثناء الجلسات الجارية.
  - `PumpsManagementScreen` (UX-15 / 466–472): قائمة المضخات بحالاتها المعتمدة (تعمل، صيانة، احتياط، خارج الخدمة)، تفاصيل القدرة والتدفق والوقود، وحوار إضافة وتعديل مضخة.
  - `PricingTariffScreen` (UX-15 / 491–497): إدارة تعرفة ساعة السقي لمصادر الطاقة (شمسي، ديزل البئر الشامل، ديزل المزارع)، وتحديث جدول الأسعار الزمني دون المساس بالسجلات التاريخية.
  - `FuelInventoryScreen` (UX-15 / 478–490): رصيد ديزل البئر الإجمالي، خزانات الوقود، حوار شراء ديزل، وحوار الجرد الفعلي وتسوية الفروقات المدققة مع الفصل عن ديزل المزارع.
  - `ReportsAnalyticsScreen` (UX-15 / 498–521): التقارير العامة والمؤشرات الرئيسية والرسوم البيانية البسيطة V1 (ساعات السقي اليومية، توزيع الطاقة، والتحصيل مقابل المصروفات) وروابط التتبع (Drill-down).
  - `WellManagementRepository`: مستودع العمليات وإدارة البيانات والتقارير مع Fallback محلي متين للعمل دون اتصال.
  - ربط الشاشات في `HomeScreen` و `WellIrrigationApp`.
- **حالة الاختبارات والتحقق:**
  - `flutter test`: **207 PASS / 0 FAIL**.
  - `flutter analyze`: **No issues found! (0 issues)**.
  - `db:test`: **354 PASS / 0 FAIL**.

---

## 2026-08-25 — تنفيذ شاشات الحسابات والمالية والمصروفات وحسابات الشركاء وتوزيع الأرباح (UX-14 / 408–459 / ق-99)

- **الشاشات والمكونات المنفذة:**
  - `ExpensesScreen` (UX-14 / 425–431): إدارة المصروفات التشغيلية والمالية مع 3 تبويبات (اليوم، بانتظار الاعتماد، السجل)، تسجيل مصروف جديد مع فئة ومصدر السداد (صندوق البئر / الشريك الدافع) وإرفاق السند أو ذكر سبب التخطي ق-114، نافذة قرار المالك (اعتماد / رفض).
  - `PartnersScreen` (UX-14 / 432–438): هيكل الشركاء ونسب الملكية والأرباح، إجمالي المستحق المتبقي، والتأكد من إجمالي 100%.
  - `PartnerDetailFinancialScreen` (UX-14 / 432–438): كشف حساب الشريك المالي المفصل مع تفكيك المعادلة المعتمدة (الحصة + المصروفات الخاصة - سقي أرضه) وصرف الأرباح.
  - `ProfitDistributionScreen` (UX-14 / 439–447): دورات توزيع الأرباح، معالج احتساب دورة جديدة مع احتياطي الصيانة، تفكيك المعادلة الشفافة، وتأكيد واعتماد الدورة وقفل الفترة نهائياً.
  - `FarmerFinancialAccountScreen` (UX-14 / 408–424): الحساب المالي للمزارع مع الفصل التام بين الديون المستحقة والرصيد المقدم ق-99 (No Silent Netting)، وسندات القبض، وتخصيص الرصيد المقدم لسداد الفواتير صراحة.
  - `FinanceRepository`: إدارة مستودع البيانات المالية والمصروفات والشركاء وتوزيع الأرباح وسندات القبض للمزارعين مع Fallback محلي متين.
  - ربط شاشات المالية في `HomeScreen` و `FarmerDetailScreen` و `WellIrrigationApp`.
- **حالة الاختبارات والتحقق:**
  - `flutter test`: **195 PASS / 0 FAIL**.
  - `flutter analyze`: **No issues found! (0 issues)**.
  - `db:test`: **354 PASS / 0 FAIL**.

---

## 2026-08-25 — تنفيذ واجهات سجل الجلسات والخط الزمني ودليل المزارعين وكشف الحساب (UX-13 / 373–380 / ق-98)

- **الشاشات والمكونات المنفذة:**
  - `SessionHistoryScreen` (UX-13 / 373–376): سجل تاريخ جلسات السقي مع البحث الفوري بالاسم والكود ورقم الهاتف، الفلاتر الزمنية وحالة السداد، شريط الإحصائيات المختصر (العدد، الساعات، الإجمالي بالريال)، وبطاقات الجلسات بحالات السداد (خالص / دفعة جزئية / آجل).
  - `SessionDetailScreen` (UX-13 / 377): تفاصيل الجلسة الشاملة، الخط الزمني التفاعلي (Timeline) لمقاطع الطاقة وتغيراتها ومعدل السعر، فترات التوقف وأسبابها، كرت السداد وسند القبض، التفقيط بالريال اليمني، معاينة الفاتورة الحرارية (58mm) وإعادة طباعتها ومشاركتها عبر واتساب.
  - `FarmersDirectoryScreen` (UX-13 / 380): دليل المزارعين والأراضي مع البحث اللحظي، عداد الأراضي لكل مزارع، والزر العائم لإضافة مزارع جديد ذرياً (`api.create_farmer`).
  - `FarmerDetailScreen` (UX-13 / 380): الملف الشخصي الكامل للمزارع عبر 3 تبويبات (قائمة الأراضي مع زر إضافة أرض جديدة `api.create_farm`، سجل جلسات المزارع مع الانتقال للتفاصيل، وكشف الحساب والمالية مع التفقيط وصافي الرصيد وحالة المديونية).
  - `OperationsRepository`: دوال `fetchSessionHistory`، `fetchSessionDetail`، `fetchFarmerDetail`، `createFarmer`، `createFarm` مع طبقة Fallback متينة للاستخدام دون اتصال.
  - `HomeScreen` & `WellIrrigationApp`: ربط التنقل السلس بين لوحة التشغيل وسجل الجلسات ودليل المزارعين.
- **حالة الاختبارات والتحقق:**
  - `flutter test`: **185 PASS / 0 FAIL**.
  - `flutter analyze`: **No issues found! (0 issues)**.
  - `db:test`: **354 PASS / 0 FAIL**.

---

## 2026-08-25 — تنفيذ منسق الجلسات واستعادة الجلسة والطباعة الحرارية وسندات القبض (ق-89 / ق-90 / ق-91 / ق-114)

- **المكونات والأنظمة المنفذة:**
  - `OfflineSessionCoordinator`: منسق الجلسات المتين مع استعادة الجلسة الحية والعداد بدقة الثواني والمقاطع بعد إغلاق التطبيق أو موت العملية، مع ربط الطابور المتين `OutboxRepository` ومسقط الجلسة `ActiveSessionProjector` ومحرك المزامنة `SyncEngine`.
  - `ReceiptFormatter`: محرك تشكيل قوالب الفواتير وسندات القبض الحرارية بقياس 58mm و 80mm مع التفقيط المالي بالريال اليمني وفق مهارة `esc-pos-thermal-printer`.
  - `PaymentReceiptDialog` (UX-10 / ق-91 / ق-92): نافذة اعتماد الجلسة والسداد الفوري (نقداً / آجل / حوالة) مع احتساب المتبقي، والتفقيط اللحظي، ومعاينة وطباعة الفاتورة الحرارية 58mm.
  - `OperationsScreen`: دمج استعادة الجلسة تلقائياً فور فتح الشاشة، وتحديث شارة حالة المزامنة والاتصال، وربط زر "إنهاء واحتساب" بنافذة سداد واعتماد الفاتورة والطباعة الحرارية.
- **حالة الاختبارات والتحقق:**
  - `flutter test`: **175 PASS / 0 FAIL**.
  - `flutter analyze`: **No issues found! (0 issues)**.
  - `db:test`: **354 PASS / 0 FAIL**.

---

## 2026-08-25 — تنفيذ البحث الذكي الموحد ومبدل الآبار وربط شاشة التشغيل بالسقي والمزارعين


- **المكونات والواجهات المنفذة:**
  - `SmartLookupField<T>`: مكوّن البحث الذكي الموحد المكتمل وفق ق-88 وق-119 مع دعم الاقتراحات السريعة، البحث بالبادئة، التطبيع العربي، والإضافة اللحظية السريعة.
  - `TopWellSelector`: مكوّن مبدل الآبار في الشريط العلوي وفق UX-05 وق-87 مع قائمة سفلية لاختيار البئر النشط وتحديث السياق لحظياً.
  - `OperationsRepository`: مستودع عمليات السقي وربط المزارعين والأراضي والمضخات والجلسات المباشرة بعقد `api.*`.
  - `OperationsScreen` & `HomeScreen`: تحديث الشاشات لدعم مبدل الآبار واختيار المزارع والأرض وبدء الجلسات الميدانية واحتساب المستحق والتفقيط المالي.
- **حالة الاختبارات والتحقق:**
  - `flutter test`: 165 PASS / 0 FAIL.
  - `flutter analyze`: No issues found!
  - `db:test`: 354 PASS / 0 FAIL.

---

## 2026-08-25 — توثيق ق-119 ومعايير تجربة الاستخدام العالمية والبحث والدمج ومنع التكرار والتفقيط


- **القرار المعتمد:** `ق-119`.
- **الملفات المحدثة:**
  - `DECISIONS.md`: توثيق القرار ق-119 حول معايير UX العالمية، البحث الذكي، منع التكرار، التفقيط المالي، والحساب الموحد.
  - `INVARIANTS.md`: إضافة الثوابت 682 إلى 688 حول التنسيق المالي اللحظي وتفقيط الريال ومنع تكرار الهوية وسحب الصلاحية العامة عن الدوال الداخلية.
  - `PROGRESS.md`: تحديث حالة المرحلة السابعة ودمج محركات الهوية مع الواجهات.
  - `RESUME_POINT.md`: تحديث نقطة الاستئناف وخطة التنفيذ الميدانية المباشرة.
- **التنفيذ التقني والمعماري:**
  - اعتماد وتوثيق المكونات المعيارية: `CurrencyTextFormField`, `CurrencyDisplay`, `Tafqeet`, `ArabicToEnglishDigitsFormatter`, `ThousandsSeparatorInputFormatter`.
  - توثيق خريطة ومحركات منع التكرار والدمج والتطبيع اللغوي العربي الصارم.
  - اعتماد خطة التنفيذ الفوري (البحث الذكي الموحد Smart Lookup، استيراد بيانات الشخص بنقرة واحدة، ومبدل الآبار العلوي).

---

## 2026-08-24 — توثيق ق-118 وتكامل واجهات الهاتف والمصادقة والـ Bootstrap

- **القرار المعتمد:** `ق-118`.
- **الملفات المحدثة:**
  - `DECISIONS.md`: توثيق القرار ق-118 حول واجهات الدخول والتهيئة الميدانية وتكامل الـ Bootstrap والهوية البصرية.
  - `PROGRESS.md`: تحديث خط الأساس المثبت (84 migrations / 24 db test files / 354 db tests pass / 155 flutter tests pass / 0 issues).
  - `RESUME_POINT.md`: تحديث نقطة الاستئناف، وحالة الشاشات المنفذة، وطبقة النقل والـ Bootstrap.
- **التنفيذ التقني:**
  - إنشاء `AppBootstrapRepository` لربط `api.app_bootstrap` و `api.create_tenant_with_well`.
  - إنشاء `AuthRepository` لإدارة جلسات الدخول والتسجيل عبر Supabase Auth.
  - تحديث شاشات التطبيق (`login_screen.dart`, `create_well_wizard_screen.dart`, `app.dart`, `operations_screen.dart`, `home_screen.dart`) وضبط المؤقت المباشر وقواعد منع تجاوز نسب الشركاء 100%.

---

## الإصدار 1.0 — الأصل الموروث

**المصدر:** ملفان نصيان قدّمهما المالك.

* وثيقة تأسيس تطبيق البئر — 11,710 سطرًا، 249,325 حرفًا. تحتوي أربع وثائق مدمجة.
* تقرير نقل السياق — 2,785 سطرًا، 66,780 حرفًا، 37 قسمًا.

**الحالة:** محفوظ كمرجع تاريخي. **لا يُستشهد به في البناء.**

---

## الإصدار 2.0 — 2026-08-12

### أولًا: التفكيك — ق-31

فُصل الملف الأول إلى أربع وثائق مستقلة بحدود متحقق منها سطرًا بسطر:

* الأسطر 1–2474 إلى `reference/01_functional_reference.md`.
* الأسطر 2475–5612 إلى `reference/02_database_and_finance_design.md`.
* الأسطر 5613–9389 إلى `reference/03_implementation_schema.md`.
* الأسطر 9390–11710 إلى `reference/04_erd_and_data_dictionary.md`.

**التحقق:** مجموع الأسطر بعد التفكيك يساوي الأصل زائدًا أسطر الترويسات المضافة.

### ثانيًا: التعديلات الجراحية الثمانية

كلها تطبيق مباشر لقرارات موثّقة، وكلها أعادت حالة نجاح:

1. القسمان 17.3 و17.4 في الوثيقة 2 — إلغاء التقريب، وإحلال الاحتساب بالثواني مع مثال 93 دقيقة. ق-01، ق-12، ق-13.
2. القسم 18.6 في الوثيقة 2 — إعادة كتابة كاملة على أساس جزء من ألف والرصيد المُرحَّل. ق-14، ق-15.
3. `core.well_settings` في الوثيقة 3 — حذف ثلاثة حقول تقريب، وإضافة `long_session_alert_minutes` و`session_ending_alert_minutes`. ق-12، ق-34، ق-40.
4. القسم 23 في الوثيقة 3 — حذف `ops.ceil_to_quarter_hour` وإحلال `ops.time_charge_milli`. ق-12، ق-14.
5. الوثيقة 3 — حذف `minimum_billable_minutes`. ق-16.
6. القسم 12.4 في الوثيقة 1 — قاعدة 100% الحتمية وتوثيق ت-01. ق-03، ق-21، ق-22.
7. القسم 15.6 في الوثيقة 1 — أربعة أقسام فرعية جديدة لحسم يوم الجلسة. ق-27، ق-37 إلى ق-40.
8. القسم 16 في الوثيقة 1 — الإشعارات الخمسة المعتمدة. ق-34، ق-35، ق-36.

### ثالثًا: ترويسات التحذير

أُضيفت ترويسة في رأس كل وثيقة من الأربع، تذكر أن الوثيقة مُعدّلة وتلخّص الملغى والمضاف، وتحيل إلى `DECISIONS.md` عند أي تعارض.

### رابعًا: تصحيح أرقام القرارات

طُبّقت 13 تصحيحًا لمراجع داخلية كانت تشير إلى أرقام قرارات خاطئة، كلها بمطابقة واحدة دقيقة.

**السبب:** أثناء التعديل كانت أرقام الجولة الثالثة لمّا تُثبَّت بعد، فاختلف الترقيم المؤقت عن النهائي.

---

## قاعدة التحديث من الآن فصاعدًا

أي تعديل لاحق على وثيقة مرجعية يُكتب هنا بـ: التاريخ · الملف · القسم · ما تغيّر · رقم القرار المبرر.
تعديل بلا رقم قرار = مخالفة.

---

## 2026-08-12 — إضافة المجلد القانوني

**الملفات الجديدة:**

* `legal/terms_and_privacy_v1_original.pdf` — النص الرسمي كما أرسله المالك. المبرر: ق-49.
* `legal/LEGAL_REVIEW.md` — مراجعة النص مقابل القرارات ومتطلبات المتجر. المبرر: ق-49.
* `INSTALL_INTO_PROJECT.md` — طريقة نقل مجلد الوثائق إلى جهاز المالك. المبرر: ق-42.

**الملفات المُعدّلة:**

* `memory/DECISIONS.md` — أُضيف ق-48 وق-49، وحُدّث قسم الترقيم التالي.
* `memory/REMINDERS.md` — حُدّثت حالة ت-03، وأُضيف ت-05.
* `memory/OPEN_ISSUES.md` — أُغلقت م-01 كمسألة إفصاح، وأُضيفت م-08 إلى م-12.
* `README.md` و`PROJECT_MAP.md` و`memory/RESUME_POINT.md` — أُضيف المجلد القانوني وحُدّث عدد القرارات إلى 49.

**ما لم يتغير:** الوثائق المرجعية الأربع في `reference/` لم تُمس في هذه الدفعة.

## تحديث 2026-08-13
**التاريخ:** 2026-08-13
**الملف:** DECISIONS.md, REMINDERS.md, OPEN_ISSUES.md, PROGRESS.md, RESUME_POINT.md, ENVIRONMENT.md
**القسم:** اغلاق المرحلة صفر، بيئة التطوير و GitHub
**ما تغير:** اضافة القرارات ق-50 الى ق-56، تحديث حالة ت-08، اغلاق م-02 و م-03، اضافة م-14، تحديث نقطة الاستئناف وسجل التقدم، تصحيح مسار قديم في ENVIRONMENT.md.
**رقم القرار المبرر:** ق-42.

## تحديث 2026-08-13 (الدفعة الثانية) — مرحلة قاعدة البيانات الكاملة
**التاريخ:** 2026-08-13
**الملف:** DECISIONS.md، REMINDERS.md، OPEN_ISSUES.md، PROGRESS.md، RESUME_POINT.md، technical/MIGRATIONS.md
**القسم:** إغلاق مرحلة قاعدة البيانات الكاملة (25 هجرة) وتوثيق دورة حياة التوزيع المالي
**ما تغيّر:** إضافة القرارات ق-57 إلى ق-66، تحديث حالة ت-01 وإضافة ت-09، إغلاق م-04 وإضافة م-15 وم-16، إعادة كتابة `technical/MIGRATIONS.md` بالكامل، تحديث `PROGRESS.md` و`RESUME_POINT.md` بالأدلة الفعلية.
**رقم القرار المبرر:** ق-42.

## تحديث 2026-08-13 (الدفعة الثالثة) — إغلاق ت-01 نهائيًا
**التاريخ:** 2026-08-13
**الملف:** DECISIONS.md، REMINDERS.md
**القسم:** التأكيد النهائي على آلية أكبر الباقي بمثال حي من بئر حقيقي
**ما تغيّر:** إضافة ق-67 بالرد الحرفي للمالك، تحديث حالتي ت-01 وت-09 إلى منفّذ ومغلق.
**رقم القرار المبرر:** ق-67.

## تحديث 2026-08-13 (الدفعة الرابعة) — تسجيل فجوة النطاق المعتمد م-17
**التاريخ:** 2026-08-13
**الملف:** OPEN_ISSUES.md
**ما تغيّر:** إضافة م-17 موثّقة بمقارنة دقيقة بين قسم 55 (النطاق المعتمد) وقسم 56/60 (المؤجل رسميًا) في الوثائق المرجعية، مقابل ما بُني فعليًا في 25 هجرة. حُصرت 17 بندًا معتمدًا غير مبني، وميّزت عمّا هو مؤجل رسميًا فلا يُعامل كفجوة.
**رقم المسألة المرجعي:** م-17.

## تحديث 2026-08-13 (الدفعة الثالثة) — إغلاق المرحلة 1 (النواة)
**التاريخ:** 2026-08-13
**الملف:** DECISIONS.md، OPEN_ISSUES.md، PROGRESS.md، RESUME_POINT.md، technical/MIGRATIONS.md
**القسم:** إغلاق المرحلة 1 من ترتيب البناء الموثّق (persons/roles/permissions/locations)
**ما تغيّر:** إضافة ق-68، تحديث م-17، فتح م-18 (فجوة ربط كتالوج الأدوار)، تحديث PROGRESS/RESUME_POINT، إضافة سجل الهجرات 026-028 في MIGRATIONS.md.
**رقم القرار المبرر:** ق-42.

## تحديث 2026-08-13 (الدفعة الخامسة) — إغلاق المرحلة 2 (التشغيل)
**التاريخ:** 2026-08-13
**الملف:** DECISIONS.md، OPEN_ISSUES.md، PROGRESS.md، RESUME_POINT.md، technical/MIGRATIONS.md
**القسم:** إغلاق المرحلة 2 من ترتيب البناء الموثّق (water_lines/farmer_profiles/price_schedules/irrigation_bookings/resource_reservations/session_segments)
**ما تغيّر:** إضافة ق-69، تحديث م-17، فتح م-19 (فجوة مخطط core.pumps)، تحديث PROGRESS/RESUME_POINT، إضافة سجل الهجرات 029-034 في MIGRATIONS.md.
**رقم القرار المبرر:** ق-42.

## تحديث 2026-08-13 (الدفعة السادسة) — إغلاق المرحلة 3 (المال)
**التاريخ:** 2026-08-13
**الملف:** DECISIONS.md، OPEN_ISSUES.md، PROGRESS.md، RESUME_POINT.md، technical/MIGRATIONS.md
**القسم:** إغلاق المرحلة 3 من ترتيب البناء الموثّق (ledger_accounts/journal_entries/journal_lines/invoices/invoice_lines/payment_allocations)
**ما تغيّر:** إضافة ق-70، تحديث م-17، فتح م-20 (تباعد مخطط billing.payments عن §28)، تحديث PROGRESS/RESUME_POINT، إضافة سجل الهجرات 035-038 في MIGRATIONS.md.
**رقم القرار المبرر:** ق-42.

## 2026-08-13 — إقفال المرحلة 4
- DECISIONS.md: اضافة ق-71 و ق-72.
- OPEN_ISSUES.md: اقفال م-20.
- PROGRESS.md: تسجيل المرحلة 4 بدفعاتها الثلاث.
- RESUME_POINT.md: اعادة كتابة كاملة (46 هجرة، 49 جدولا، خطة المرحلة 5).
- MIGRATIONS.md: الصفوف 039 الى 046.

## تحديث 2026-08-14 — اعتماد قرارات المرحلة 5 (ق-73)
**التاريخ:** 2026-08-14
**الملف:** DECISIONS.md، DOC_CHANGELOG.md
**القسم:** قرارات المرحلة 5 (الشركاء والفترات المحاسبية والمدير العام)
**ما تغيّر:** إضافة ق-73 معتمدًا من المالك قبل كتابة أي هجرة: توحيد مصدر النِّسب في جدول الشركاء، إلزامية اكتمال 100%، سياسة سقي الشريك بقيمتين فقط (دفع عادي أو خصم من الأرباح)، دورا «مدير» و«شريك»، علم المدير العام للتطبيق، الفترات الشهرية والسنوية، تنفيذ الخطوة 5 المؤجلة، الإقفال بلا تصويت، إعادة الفتح بموافقة 70% من عدد الشركاء ثم المدير العام، والربط المؤجل للعمودين.
**رقم القرار المبرر:** ق-73.

## تحديث 2026-08-14 — إقفال المرحلة 5 (الشركاء)
**التاريخ:** 2026-08-14
**الملف:** DECISIONS.md، PROGRESS.md، RESUME_POINT.md، technical/MIGRATIONS.md، DOC_CHANGELOG.md
**القسم:** إقفال المرحلة 5 من ترتيب البناء الموثّق (well_partners / partner_irrigation_policies / accounting_periods / دورة إعادة الفتح / صلاحيات المدير والشريك)
**ما تغيّر:** تحويل ق-73 إلى «نافذ ومنفذ» مع نتائج التحقق البنيوي والوظيفي (23/23)، تسجيل الهجرات 039–046 و047–050 في السجل التاريخي docs/technical/MIGRATIONS.md وحذف الملف المكرر في docs/memory/، تحديث PROGRESS وRESUME_POINT بخطة المرحلة 6.
**رقم القرار المبرر:** ق-73.

## تحديث 2026-08-14 (الدفعة الثانية) — اعتماد قرارات المرحلة 6 (ق-74)
**التاريخ:** 2026-08-14
**الملف:** DECISIONS.md، DOC_CHANGELOG.md
**القسم:** قرارات المرحلة 6 (الإدارة)
**ما تغيّر:** إضافة ق-74 بعشرة بنود معتمدة من إجابات المالك: تبنّي إصدارات نسب الشريك من المرجع، استبدال جداول التوزيع بجداول الدورات الكاملة، الالتزامات المحتجزة بمفتاح تشغيل/تعطيل، احتياطي الصيانة بخمسة أنواع، اعتماد الأرصدة الافتتاحية للمالك، بناء الرواتب، قيود الديزل التلقائية، آلية خصم الشريك، إجراء §49، وترتيب التنفيذ بثلاث دفعات.
**رقم القرار المبرر:** ق-74.

## تحديث 2026-08-14 (الدفعة الثالثة) — إقفال المرحلة 6 ومحطة قاعدة البيانات
**التاريخ:** 2026-08-14
**الملف:** DECISIONS.md، PROGRESS.md، RESUME_POINT.md، technical/MIGRATIONS.md، DOC_CHANGELOG.md
**القسم:** إقفال المرحلة 6 (الإدارة) واكتمال مراحل قاعدة البيانات الست
**ما تغيّر:** تحويل ق-74 إلى «نافذ ومنفذ» مع نتائج التحقق (19/19 و17/17) وتوثيق الأعطال الثلاثة المكتشفة والمصححة أثناء التنفيذ، تسجيل الهجرات 051–056 في السجل التاريخي، تحديث PROGRESS وإعادة كتابة RESUME_POINT لتعكس اكتمال محطة قاعدة البيانات وبدء محطة تطبيق Flutter.
**رقم القرار المبرر:** ق-74.

## تحديث 2026-08-14 (الدفعة الرابعة) — اعتماد ق-75 (الدفعة الختامية لقاعدة البيانات)
**التاريخ:** 2026-08-14
**الملف:** DECISIONS.md، DOC_CHANGELOG.md
**القسم:** الدفعة الختامية بعد المطابقة الحرفية مع المرجعين
**ما تغيّر:** إضافة ق-75: بناء سجل التدقيق وطبقة المزامنة والمرفقات وعروض التقارير واستكمال الإجراءات الآن، قبول الدفع الزائد وتحويله لرصيد مقدم، الإبقاء على منع المخزون السالب كانحراف موثق، فرض فاتورة واحدة لكل جلسة، وتأجيلات موثقة داخل النطاق (الأجهزة، outbox، PowerSync).
**رقم القرار المبرر:** ق-75.

## 2026-08-14 — إقفال محطة قواعد البيانات
- DECISIONS: ق-75 → نافذ ومنفذ مع نص التحقق (17/17).
- MIGRATIONS (technical): صفوف 057–061 بحالة التطبيق.
- PROGRESS: الدفعة الختامية واكتمال المحطة.
- RESUME_POINT: إعادة كتابة كاملة — الوضع الحالي وخارطة المرحلة 7 والمؤجلات.
- OPEN_ISSUES: إغلاق م-17 بتقييم ختامي، وفتح م-21 للاختبارات الميدانية.

## 2026-08-14 — ق-76: سد فجوات التدقيق الشامل
- DECISIONS: إضافة ق-76 (دمج الأشخاص + قفل المضخة + تأجيلات موثقة + ملاحظة ترقيم ق-71)؛ تصحيح حالة ق-51 إلى نافذ ومنفذ؛ إحالة ق-63 إلى محرك التوزيع الجديد؛ توحيد تنسيق ق-72.
- OPEN_ISSUES: عنوان م-17 إلى مغلقة.

## 2026-08-14 — إقفال ق-76 (نافذ ومنفذ)
- DECISIONS: ق-76 → نافذ ومنفذ مع نص التحقق (13/13).
- MIGRATIONS (technical): صفوف 062–063 بحالة التطبيق.
- PROGRESS: سد فجوات التدقيق الشامل.
- RESUME_POINT: إعادة كتابة — 63 هجرة و66 جدولًا، توجيهات المرحلة 7 (Flutter) بما فيها استدعاء كاشف التكرار.

## 2026-08-14 — ق-77: حسم تعارضات تدقيق Codex
- DECISIONS: إعادة بناء ق-71 المفقود (وحدة المال = الريال الكامل)؛ ملاحظات نسخ على ق-14 وق-15 وق-21 وق-48 وق-67؛ حسم ق-18 وق-69 (مصدر التسعير)؛ إضافة ق-77.
- OPEN_ISSUES: ملاحظة م-13؛ تصحيح نص إغلاق م-17 (بعد تدقيق Codex) ونص م-20 (قيم الغرض الفعلية).
- technical: حفظ تقرير التدقيق المستقل CONFORMANCE_AUDIT_CODEX.md كوثيقة مشروع.

## 2026-08-14 — تحقق 064-065
- DECISIONS: ق-77 محدثة بالتحقق الجزئي (قناتان مستقلتان).
- MIGRATIONS/PROGRESS: صفوف 064-065.

## 2026-08-15 — تحقق 066
- DECISIONS: ق-77 محدثة (البند 3 منفذ) مع نص التحقق والتسويات الواعية.
- MIGRATIONS/PROGRESS: صف 066 ونتائجه.

## 2026-08-15 — ختام ق-77
- DECISIONS: ق-77 «نافذ ومنفذ بالكامل» مع نص تحقق 067 وتصحيح توقع فحص الشريك.
- INVARIANTS/GLOSSARY: ملحق توحيد وحدة المال (نسخ milli نهائيا).
- MIGRATIONS/PROGRESS/RESUME_POINT: صف 067 وختام المحطة وقاعدة عمل الوكيل الجديدة.

## 2026-08-15 — ت-11 مغلق
- RESUME_POINT: قرار بناء الاجراءات المتبقية الان على دفعتين 068 و069.

## 2026-08-15 — تحقق 068
- MIGRATIONS/PROGRESS: صف 068 ونتائجه (107 فحصا PASS بقناتين).

## 2026-08-15 — تحقق 069 واغلاق ت-11
- MIGRATIONS/PROGRESS: صف 069 ونتائجه (129 فحصا PASS بقناتين).
- OPEN_ISSUES: فتح م-22 (مالك الأرض مرتبط بملف الدخول).
- RESUME_POINT: ت-11 منجز بالكامل والباب التالي المرحلة 7.

## 2026-08-15 — قرار م-23
- OPEN_ISSUES: قيد م-23 بقراره ومواعيد اقفاله الثلاثة (منطق الليلة، ظهور في المرحلة 7، تفعيل عند النشر).
- RESUME_POINT: سجل القرار.

## 2026-08-15 — تحقق 070 وختام م-23 برمجيا
- MIGRATIONS/PROGRESS: صف 070 ونتائجه (138 فحصا PASS بقناتين).
- OPEN_ISSUES: م-08 — المنطق منفذ؛ العلامتان الباقيتان موثقتان (المرحلة 7 + بند النشر).
- RESUME_POINT: اكتمال محطة القاعدة.

## 2026-08-15 — تصحيح وجهة ملحق وحدة المال
- اكتشف مسح التوثيق ان ملحق ق-77 كُتب في ملفي reference الفارغين؛ نُسخ الان الى technical/INVARIANTS.md و technical/GLOSSARY.md حيث يعيش نص milli الفعلي — زال التناقض من موضعه الصحيح.

## 2026-08-15 — تصحيح تعارض ترقيم
- مرسلا التنبيهات الدوريين اعيد ترقيمهما من م-08 الى م-23 (م-08 محجوزة اصلا لبند قانوني في ت-03). عولجت السجلات الحية؛ تعليقات هجرة 070 تبقى كما هي (هجرات مطبقة لا تعدل).

## 2026-08-16 — اعتماد ق-78 وحد Data API

- `DECISIONS.md`: إضافة ق-78 مع السياق والأسباب والحد المعماري وشروط الإغلاق.
- `technical/INVARIANTS.md`: إضافة قواعد ثابتة لحد Data API ومبدأ Opt-in.
- `PROJECT_MAP.md`: تحديد مصدر الحقيقة لعقد Flutter مع قاعدة البيانات.
- `supabase/migrations/071`: إعداد التنفيذ الفني للحد.
- `supabase/tests/071`: إضافة اختبار قبول دائم لمنح مخطط api وDefault Privileges.
- `supabase/config.toml`: إعداد api كمخطط التطبيق المكشوف بدل الاعتماد على public.
- لا يسجل التنفيذ «مكتملًا» في PROGRESS قبل تشغيل التحقق الفعلي بواسطة المالك.
- المبرر: ق-78.

## 2026-08-16 — تصحيح فرضية Default Privileges في ق-78

كشف اختبار القبول 071 أن PostgreSQL يمنح EXECUTE على الدوال الجديدة إلى PUBLIC افتراضيًا، وأن REVOKE المحدود بمخطط عبر ALTER DEFAULT PRIVILEGES لا يستطيع إلغاء المنح العالمي الافتراضي للدوال. لم تُعدّل الهجرة 071 بعد تطبيقها التزامًا بقاعدة عدم تعديل الهجرات المطبقة. صُحح ق-78 وINVARIANTS ليعتمدا النمط الصحيح: CREATE FUNCTION ثم REVOKE ثم GRANT الصريح داخل المعاملة نفسها، مع اختبار دائم يفشل إذا أصبحت أي دالة داخل api قابلة للتنفيذ من anon.

## 2026-08-16 — إغلاق ق-78

- DECISIONS: ق-78 إلى نافذ ومنفذ ومغلق مع دليل إعادة البناء والاختبارات وPostgREST.
- INVARIANTS: تصحيح قاعدة دوال api لتطابق Default Privileges الفعلية في PostgreSQL.
- PROJECT_MAP: تثبيت api كعقد Flutter مع قاعدة البيانات.
- README: تحديث مرجع القرارات حتى ق-78.
- ENVIRONMENT: تسجيل Exposed Schemas الفعلية بعد ق-78.
- MIGRATIONS: تسجيل 071 وحالة 70 هجرة / 11 اختبار / 145 PASS.
- PROGRESS وRESUME_POINT: إغلاق الشرط الأول API Architecture وتثبيت نقطة الاستئناف.
- config.toml: api وgraphql_public فقط ضمن Exposed Schemas.
- لم تُعدّل الهجرة 071 بعد تطبيقها؛ التصحيح الناتج عن اختبار Default Privileges تم في القواعد والاختبار الدائم وفق قاعدة عدم تعديل الهجرات المطبقة.

## 2026-08-16 — اعتماد ق-79 وإعداد الهجرة 072

- `DECISIONS.md`: إضافة ق-79 الخاص بـ RPC-only writes.
- `technical/INVARIANTS.md`: منع Direct DML على مخططات الأعمال الداخلية.
- `OPEN_ISSUES.md`: إضافة م-24 حتى اكتمال التحقق.
- `RESUME_POINT.md`: تثبيت أن 072 قيد التحقق.
- `supabase/migrations/072`: سحب الكتابة المباشرة من أدوار تطبيق العميل.
- `supabase/tests/072`: اختبار دائم للمنع واستمرار إجراءات الأعمال.
- لا يسجل ق-79 كمنجز في `PROGRESS.md` قبل إثبات المالك.

## 2026-08-16 — إثبات الهجرة 072

- ثبتت إعادة البناء النظيفة مع 072.
- ثبت Direct DML = صفر لأدوار تطبيق العميل.
- ثبت استمرار EXECUTE لإجراءات الأعمال الحرجة.
- ثبت عدم وجود SECURITY DEFINER قابلة للتنفيذ بلا search_path.
- أصبح baseline: 71 هجرة، 12 اختبارًا دائمًا، 154 PASS.
- عدل اختبارا 066 و069 لفصل Fixtures الإدارية عن مسارات المستخدم بعد ق-79.
- لم يغلق ق-79 كاملًا؛ أغلفة api الكتابية هي العمل التالي.

## 2026-08-16 — إعداد 073 لعقد الكتابة

- إضافة 15 غلاف كتابة SECURITY INVOKER داخل api.
- عدم تكرار منطق الأعمال داخل api.
- إزالة معاملات هوية المنفذ من عقد Flutter واستخدام auth.uid().
- إبقاء create_farm خارج العقد بسبب م-22.
- إضافة اختبار دائم لسطح api والمنح وDirect DML وتشغيل api.create_farmer فعليًا.
- التنفيذ ما زال قيد تحقق المالك.

## 2026-08-16 — إعداد 074

- إضافة عقد api لإنشاء البئر.
- إضافة المصروفات والاعتماد.
- إضافة المناوبات والتسليم ونقل الجلسة.
- إضافة الإقفال وتوزيع الأرباح والرواتب الأساسية.
- منع إرسال معرف المنفذ من Flutter في هذه التدفقات.
- إبقاء create_farm خارج العقد بسبب م-22.

## 2026-08-17 — إغلاق ق-79 والشرط الثاني

- تصحيح اختبار 073 ليبقى مختصًا بعقد 073 ولا يعتمد على الهجرات اللاحقة.
- ثبتت 074 مع كامل الحزمة.
- baseline: 73 هجرة / 14 اختبارًا / 178 PASS.
- Direct DML = صفر.
- Data API = 31 RPC.
- تصنيف الدوال المتبقية: 14 داخلية / 9 مؤجلة / 0 غير مصنفة.
- أُغلق م-24.

## 2026-08-17 — مصالحة توثيقية شاملة للشرط 3

- إعادة بناء PROJECT_MAP ليعكس ترتيب السلطة الحالي.
- إعادة بناء RESUME_POINT كحالة حالية فقط بدل تراكم snapshots قديمة.
- إنشاء API_ARCHITECTURE.md.
- إنشاء SYNC_ARCHITECTURE.md.
- إنشاء DECISION_IMPLEMENTATION_MATRIX.md.
- تصحيح INVARIANTS وGLOSSARY إلى الريال الكامل وقاعدة الباقي الحالية.
- تصحيح النصوص المرجعية النشطة المتعلقة بالتقريب ووحدة المال.
- تمييز CONFORMANCE_AUDIT_CODEX بوضوح كـsnapshot تاريخي.
- توحيد حالة البيئة وFlutter وSupabase والـbaseline.
- إضافة فهرس حالة حالية للمسائل المفتوحة وإغلاق م-15 توثيقيًا.
- الفصل بين Server Sync المنفذ وMobile Offline Sync المؤجل.
- الحالة: التعديلات مكتوبة؛ بانتظار التحقق الساكن للمالك قبل إغلاق الشرط 3.

## 2026-08-17 — إغلاق الشرط 3 رسميًا

- Documentation Acceptance = PASS.
- Cross-sync = PASS.
- لا أخطاء static diff.
- أُغلق Documentation Conformance رسميًا.
- PROJECT_MAP وRESUME_POINT انتقلا إلى الشرط 4.
- الخطوة التالية: م-22 قبل شاشة الأراضي.

## 2026-08-17 — إعداد ق-80 / 075

- إضافة ق-80.
- م-22 انتقلت من انتظار القرار إلى التنفيذ قيد التحقق.
- إعداد Migration/Test 075.
- baseline المثبت لم يتغير بعد:
  73 migration مطبقة / 14 tests / 178 PASS / 31 RPC.

## 2026-08-17 — إغلاق ق-80 وم-22

- ق-80 أصبحت نافذة ومنفذة ومغلقة.
- 075 أصبحت migration مطبقة ومثبتة.
- baseline الرسمي أصبح:
  74 migrations / 15 tests / 193 PASS / 32 RPC.
- وثائق Farm identity انتقلت إلى Farmer Well Account.
- نقطة الاستئناف انتقلت إلى م-19.

## 2026-08-17 — إغلاق ق-81 وم-19 والشرط 4

- ق-81 أصبحت نافذة ومنفذة ومغلقة.
- Migration 076 أصبحت مطبقة ومثبتة.
- م-19 مغلقة.
- Condition 4 مغلق.
- baseline الرسمي:
  75 migrations / 16 tests / 205 PASS / 32 RPC.
- نقطة الاستئناف انتقلت إلى Condition 5.

## 2026-08-17 — Final Clean Acceptance / Stage 7 gate

- إغلاق Condition 5.
- إغلاق Stage 7 Readiness Gate.
- baseline النهائي:
  75 migrations / 16 tests / 205 PASS / 32 RPC.
- لا توجد FAIL أو ERROR.
- Direct DML بقي صفرًا.
- نقطة الاستئناف انتقلت إلى بدء Stage 7 implementation.


## تحديث 2026-08-17 — ق-82

**الملفات:** DECISIONS.md، INVARIANTS.md، PROGRESS.md،
RESUME_POINT.md، ENVIRONMENT.md، MIGRATIONS.md،
API_ARCHITECTURE.md، DECISION_IMPLEMENTATION_MATRIX.md.

**القسم:** Stage 7 App Bootstrap Read Contract.

**ما تغيّر:** توثيق Migration 077 وعقد `api.app_bootstrap()`،
وتحديث baseline المثبت إلى 76 migrations و17 اختبارًا دائمًا
و217 PASS و33 Data API RPC، وتثبيت بوابة الهوية البصرية
كخطوة تسبق بناء أي واجهة تشغيلية جديدة.

**القرار المبرر:** ق-82.


## تحديث 2026-08-17 — ق-83

**الملفات:** `design/VISUAL_IDENTITY.md`، DECISIONS.md،
PROGRESS.md، RESUME_POINT.md، README.md، PROJECT_MAP.md،
DECISION_IMPLEMENTATION_MATRIX.md.

**القسم:** الهوية البصرية العامة لـStage 7.

**ما تغيّر:** اعتماد وتوثيق الشعار المبدئي ونظام الألوان
والخط والأيقونات والأسطح والمسافات والصور والرسوم والحركة
ولغة الحالات وأيقونة التطبيق والوضع الداكن وAccessibility
وRTL والأرقام وقواعد استخدام الشعار وأصول الهوية.

**الحالة:** بوابة الهوية البصرية العامة مغلقة مبدئيًا.
لم يبدأ تنفيذ واجهات إنتاجية نتيجة هذا القرار.

**القرار المبرر:** ق-83.

## تحديث 2026-08-18 — Stage 7 UX وOffline والتسوية

**المجال:** توثيق UX-00 إلى UX-12 والقرارات حتى ق-92.

**ما تغير:**

- اعتماد وتوثيق هوية Stage 7.
- توثيق Splash/Login/Onboarding/Role routing/Home.
- توثيق Operations وSmart Lookup ومنع التكرار.
- توثيق Offline Field Operations وAndroid Background Sync.
- توثيق Device Readiness وSync Status.
- توثيق Active Irrigation Session.
- توثيق Session Completion & Settlement.
- إضافة معماريات تقنية منفصلة للبحث وOffline والجلسة
  الجارية والتسوية.
- فتح م-25 وم-26 وم-27 للفجوات التي لا يجوز نسيانها.
- إبقاء Migration 078+ كنقطة أول تغيير DB جديد.

**السبب:** القرارات ق-83 إلى ق-92 ومنهج
مناقشة → اعتماد → توثيق → انتقال.

**ملاحظة:** دفعات UX كانت توثيقية؛ لم تغير Baseline
اختبارات قاعدة البيانات المثبت سابقًا.

## تحديث 2026-08-18 — ق-93 وق-94

**الملفات الرئيسية:**

- `README.md`
- `PROJECT_MAP.md`
- `memory/AI_HANDOFF_PROTOCOL.md`
- `memory/DECISIONS.md`
- `memory/PROGRESS.md`
- `memory/DOC_CHANGELOG.md`
- `memory/RESUME_POINT.md`
- `design/UX_UI_SPEC.md`
- `technical/INVARIANTS.md`
- `technical/DECISION_IMPLEMENTATION_MATRIX.md`

**ما تغير:**

- إنشاء بروتوكول رسمي لتسليم المشروع لأي نموذج AI.
- إزالة Snapshot المتقادم من README وتحويله إلى مدخل ثابت.
- تحديث سجل التقدم إلى 2026-08-18.
- تسجيل الإنجازات التصميمية دون وصفها كتنفيذ.
- تثبيت أن RESUME_POINT هو نقطة التوقف الوحيدة.
- تثبيت Contract لتحديث ملفات الذاكرة في كل دفعة.
- دمج المناقشات المتبقية إلى UX-13..UX-17.

**السبب:** ق-93 وق-94.

## تحديث 2026-08-18 — ق-95 وق-96 / توثيق طريقة العمل

**السبب:** المالك طلب ألا يقتصر تسليم المشروع على
القرارات والملفات، بل يشمل طريقة التفكير القابلة
للمراجعة، العرض، أسلوب الحوار، وطريقة كتابة الأوامر.

**الملفات الجديدة:**

- `memory/AI_COLLABORATION_PROTOCOL.md`
- `memory/TERMINAL_COMMAND_PROTOCOL.md`

**الملفات المحدثة:**

- `README.md`
- `PROJECT_MAP.md`
- `memory/AI_HANDOFF_PROTOCOL.md`
- `memory/DECISIONS.md`
- `memory/PROGRESS.md`
- `memory/RESUME_POINT.md`
- `technical/INVARIANTS.md`
- `technical/DECISION_IMPLEMENTATION_MATRIX.md`
- `memory/DOC_CHANGELOG.md`

**ما تم تثبيته:**

- دور النموذج في المشروع.
- مستوى الشرح واللغة.
- منهج اتخاذ القرار.
- تسجيل الأسباب والأدلة.
- UX workflow.
- دلالة «اعتمد».
- الانتقال المباشر بعد الإغلاق.
- الفصل بين UX numbers وQ decisions.
- التعامل مع التعارضات والفجوات.
- التعامل مع مخرجات الطرفية.
- أسلوب كتابة الأوامر.
- Subshell و`set -e`.
- منع Base64 الافتراضي.
- منع Nested Code Fences.
- Recovery بعد الفشل.
- Lessons Learned من الحوادث السابقة.

**القرارات المبررة:** ق-95، ق-96.

## تحديث 2026-08-18 — ق-97 / بوابة اكتمال التوثيق

**السبب:** اشتراط ألا يضيع أي قرار أو سبب أو تحديث أو
سجل أو إنجاز أو Gap أو طريقة عمل عند تسليم المشروع
لنموذج ذكاء اصطناعي جديد.

**الملف الجديد:**

- `memory/DOCUMENTATION_GATE.md`

**الملفات المحدثة:**

- `README.md`
- `PROJECT_MAP.md`
- `memory/AI_HANDOFF_PROTOCOL.md`
- `memory/AI_COLLABORATION_PROTOCOL.md`
- `memory/TERMINAL_COMMAND_PROTOCOL.md`
- `memory/DECISIONS.md`
- `memory/PROGRESS.md`
- `memory/RESUME_POINT.md`
- `memory/DOC_CHANGELOG.md`
- `technical/INVARIANTS.md`
- `technical/DECISION_IMPLEMENTATION_MATRIX.md`

**ما تغير:**

- فرض Documentation Gate قبل الانتقال.
- فرض توثيق أسباب القرار المهمة.
- تحديد Checklist لكل نوع توثيق.
- فرض Traceability.
- إخضاع قواعد التوثيق نفسها لنفس البوابة.
- ربط Gate بإغلاق Git.

**القرار المبرر:** ق-97.

**نوع الدفعة:** توثيقية فقط؛ لا تغيير في Baseline
اختبارات قاعدة البيانات.

## تحديث 2026-08-18 — UX-13 / ق-98

**المجال:** التشغيل والسجلات والمزارعون والحجوزات والمناوبات.

**السبب:**

اعتماد UX-13 من القرار 373 إلى 407، مع الحاجة إلى حفظ
الأسباب والتعارضات الحالية وفق Documentation Gate ق-97.

**الملف الجديد:**

- `technical/OPERATIONS_RECORDS_ARCHITECTURE.md`

**الملفات المحدثة:**

- `design/UX_UI_SPEC.md`
- `memory/DECISIONS.md`
- `memory/OPEN_ISSUES.md`
- `memory/PROGRESS.md`
- `memory/RESUME_POINT.md`
- `PROJECT_MAP.md`
- `technical/API_ARCHITECTURE.md`
- `technical/SYNC_ARCHITECTURE.md`
- `technical/SEARCH_DEDUP_ARCHITECTURE.md`
- `technical/INVARIANTS.md`
- `technical/DECISION_IMPLEMENTATION_MATRIX.md`

**قرارات/قواعد مهمة موثقة:**

- حفظ التاريخ وعدم Hard Delete للسجلات المستخدمة.
- Booking Offline غير Confirmed حتى Backend.
- Backend يحسم Resource Conflict.
- Smart Lookup يعاد استخدامه.
- Transfer يحتاج قبول المستلم.
- Shift لا يغلق عاديًا مع Active Session غير محسومة.
- Cash Handover منفصل عن Operational Responsibility.
- تسجيل تعارض `p_allow_open_sessions` الحالي في م-28.

**القرار المبرر:** ق-98.

**نوع الدفعة:** توثيقية فقط؛ Baseline الاختبارات لم يتغير.

## تحديث 2026-08-18 — UX-14 / ق-99

**المجال:** المال والشركاء والتوزيعات والتصحيحات.

**السبب:**

اعتماد القرارات 408–459 مع ضرورة حفظ السياسات المالية
والفجوات الحالية بصورة يمكن تتبعها حسب ق-97.

**الملف الجديد:**

- `technical/MONEY_PARTNERS_ARCHITECTURE.md`

**الملفات المحدثة:**

- `design/UX_UI_SPEC.md`
- `memory/DECISIONS.md`
- `memory/OPEN_ISSUES.md`
- `memory/PROGRESS.md`
- `memory/RESUME_POINT.md`
- `PROJECT_MAP.md`
- `technical/API_ARCHITECTURE.md`
- `technical/SYNC_ARCHITECTURE.md`
- `technical/SESSION_SETTLEMENT_ARCHITECTURE.md`
- `technical/INVARIANTS.md`
- `technical/DECISION_IMPLEMENTATION_MATRIX.md`

**أهم ما ثبت:**

- Debt وAdvance منفصلان.
- No Silent Netting.
- Old Advance Allocation فعل صريح.
- Oldest invoices مجرد Default Suggestion.
- Receipt النهائي خادمي.
- Offline Payment ليست Posted قبل ACK.
- Attachment Skip Reason إلزامي عند التخطي.
- Ownership وProfit Percentage مستقلتان.
- Share History لا يعاد كتابتها.
- Calculation منفصل عن Approval.
- Approved Distribution مقفلة.
- Partner private financial projection مطلوبة.
- Final Financial Actions Online only.
- Corrections لا تستخدم Direct Edit.
- Rounding الحالي للاحتياطي يحتاج Audit.
- م-29 فتحت للفجوات التنفيذية.

**القرار المبرر:** ق-99.

**نوع الدفعة:** توثيقية فقط؛ Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — UX-15 / ق-100

**المجال:** إدارة البئر والمضخات والطاقة والوقود والتسعير
والتقارير والرسوم البيانية.

**السبب:**

اعتماد القرارات 460–510، ثم طلب المالك دراسة وتوظيف
رسوم بيانية بسيطة تناسب النسخة الأولى وتحديد أماكنها،
فأضيفت القرارات 511–526.

**الملف الجديد:**

- `technical/WELL_MANAGEMENT_REPORTING_ARCHITECTURE.md`

**الملفات المحدثة:**

- `design/UX_UI_SPEC.md`
- `design/VISUAL_IDENTITY.md`
- `memory/DECISIONS.md`
- `memory/OPEN_ISSUES.md`
- `memory/PROGRESS.md`
- `memory/RESUME_POINT.md`
- `PROJECT_MAP.md`
- `technical/API_ARCHITECTURE.md`
- `technical/SYNC_ARCHITECTURE.md`
- `technical/INVARIANTS.md`
- `technical/DECISION_IMPLEMENTATION_MATRIX.md`

**أهم ما ثبت:**

- Pump = Equipment.
- Modern Energy = Session Segments.
- Well Fuel وFarmer Fuel منفصلان.
- Fuel ليس Farmer Surcharge.
- Pricing تاريخية.
- Diesel V1 = Inclusive Hourly.
- `operation_plus_fuel` لا يظهر في Flutter V1.
- Reports تعتمد Backend Read Models.
- V1 Charts = Bar + Line.
- لا Chart في Owner Home V1.
- Chart رئيسية واحدة في Reports.
- Fuel Mini Chart فقط في سياق الوقود.
- Partner Chart تعرض بيانات الشريك نفسه.
- Chart تسمح Exact Value وDrill-down.
- Offline Chart تعرض Last Sync.
- لا Fake Zero Chart.
- م-30 فتحت للفجوات التنفيذية.

**القرار المبرر:** ق-100.

**نوع الدفعة:** توثيقية فقط؛ Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — UX-16A / ق-101

**المجال:** الحساب والإعدادات.

**التغيير الأساسي:**

فصل Platform Administration عن UX-16A بصورة رسمية.

**الملف الجديد:**

- `technical/ACCOUNT_SETTINGS_ARCHITECTURE.md`

**أهم القرارات:**

- Account موحد.
- Phone Change حساس.
- Forgot Password عبر OTP في V1.
- Role lifecycle لا يكرر الهوية.
- Platform Admin خارج UX-16A.
- Local State مرتبطة بالحساب.
- Logout لا يحذف Pending.
- Date/Time تظهر بالإنجليزية دائمًا.
- فتح م-31.

**نوع الدفعة:** توثيقية فقط.

## تحديث 2026-08-19 — PA-01 / ق-102

**المجال:** Platform Administration.

**السبب:**

تم تعريف مسؤول المنصة بأنه مدير المنتج العام وليس
مستخدم بئر، ثم اعتماد Dashboard مستقلة بإحصاءات رقمية
واسعة وتحديث تلقائي وقائمة جانبية ورسوم بسيطة.

**الملف الجديد:**

- `technical/PLATFORM_ADMINISTRATION_ARCHITECTURE.md`

**أهم القرارات:**

- Platform Admin مستقل عن Well Roles.
- Global Administration Authority.
- Admin Console مستقلة.
- Web/Desktop-first.
- RTL Sidebar يمين.
- Numeric KPIs أولًا.
- Near-real-time refresh.
- Bar/Line charts فقط.
- Drill-down.
- Operations/Finance/Sync/System monitoring.
- Audit mandatory.
- Trusted Backend mandatory.
- Password visibility requirement adopted but blocked.
- فتح م-32.

**نوع الدفعة:** توثيقية فقط.

## تحديث 2026-08-19 — PA-02 / ق-103

**المجال:**

Platform Admin Accounts, Wells, Support and Password Control.

**السبب:**

اعتماد PA-02 بالكامل، واختيار المالك Option B لكلمات المرور:
Platform Super Admin يستطيع عرض Current Password عبر
Recoverable Encrypted Password Vault.

**الملف الجديد:**

- `technical/PLATFORM_ADMIN_ACCOUNTS_WELLS_SUPPORT_ARCHITECTURE.md`

**أهم التحديثات:**

- Global Search.
- Account/Person Administration.
- Identity Resolution.
- Account Suspend/Restore.
- Well Administration.
- Support Cases.
- Error References.
- Admin Correction.
- Append-only Audit.
- Password Option B.
- Encrypted Recoverable Vault.
- Vault lifecycle.
- legacy unavailable state.
- Password Reveal.
- Password Orchestrator.
- no plaintext at rest.
- no password in local sync/cache/logs.
- key management requirements.
- ق-103 supersedes Password Non-Readability portion of ق-85.
- فتح م-33.

**نوع الدفعة:** توثيقية فقط.

## تحديث 2026-08-19 — ق-104 / ق-105

**المجال:**

Research Governance + Platform Admin Security + Password Recovery.

**السبب:**

طلب المالك تثبيت قاعدة تمنع اتخاذ القرارات المهمة دون
مراجعة المعايير العالمية وتجارب المنتجات والمستخدمين،
وفوض النموذج باختيار أفضل Password Architecture للمشروع.

**الملف الجديد:**

- `memory/RESEARCH_STANDARDS_GATE.md`

**أهم التغييرات:**

- Research & Standards Gate أصبحت إلزامية.
- README/Handoff/Collaboration/Documentation Gate حدثت.
- External Evidence hierarchy وثقت.
- Standards-aligned/Adapted/Exception ثبتت.
- Admin Dashboard usability hardening وثق.
- Pagination بدل Infinite Scroll للجداول الكبيرة.
- Sticky Search/Filters عند الحاجة.
- Hybrid Near-real-time.
- Symptom-first Monitoring.
- WCAG 2.2 AA.
- Platform Admin MFA.
- Step-up high-risk actions.
- Secret-safe logging.
- Password Vault Option B نُسخت.
- Current Password Reveal نُسخت.
- Password reset أصبح Admin-triggered + OTP +
  User-chosen new password.
- م-33 أعيدت صياغتها وفق ق-105.

**Supersession:**

ق-105 تنسخ فقط Password/Vault portion من ق-103.

PA-02 core تبقى معتمدة.

**نوع الدفعة:** توثيقية فقط.

Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — PA-03 / ق-106

**المجال:**

Platform Sales, Entitlements, Operations and Financial Control.

**السبب:**

اعتماد PA-03 بعد تطبيق Research & Standards Gate.

**الملف الجديد:**

- `technical/PLATFORM_ADMIN_SALES_OPERATIONS_FINANCE_ARCHITECTURE.md`

**أهم القرارات:**

- Platform Commerce ≠ Well Finance.
- permanent manual V1 sale.
- one entitlement per purchased well.
- atomic/idempotent sale and grant.
- atomic entitlement consumption.
- no double consumption.
- sale/activation correction preserves history.
- operations monitoring respects Offline uncertainty.
- no generic session editor.
- admin session closure is audited correction.
- financial monitoring Read-first.
- posted finance correction follows ق-99.
- no Force Reopen bypass.
- Step-up and transaction confirmation integrity.
- filtered audited export.
- server-side pagination/filter/sort.
- privileged writes Online-only.
- م-34 opened.
- PA-04 next.

**Supersession:**

ق-106 تنسخ من ق-10 فقط مجانية V1.

Subscription deferral remains.

**Documentation repairs:**

- stale Password Vault references corrected in active UX/Map.
- legal review updated for permanent manual sale model.

**نوع الدفعة:** توثيقية فقط.

Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — PA-04 / ق-107

**المجال:**

Platform Monitoring, Audit, Configuration and Incidents.

**الملف الجديد:**

- `technical/PLATFORM_ADMIN_MONITORING_SETTINGS_ARCHITECTURE.md`

**أهم القرارات:**

- symptom-first monitoring.
- actionable/deduplicated alerts.
- incident lifecycle.
- postmortems.
- correlation/error references.
- reuse existing append-only audit foundation.
- global admin audit projection.
- secret redaction.
- typed/versioned config.
- validation/rollback.
- scoped maintenance.
- Offline field continuity.
- version policies.
- release tracking.
- telemetry privacy.
- final admin navigation.
- م-35 opened.

**الحالة:**

Platform Administration PA-01..PA-04 مكتملة تصميميًا.

**التالي:**

UX-17.

**نوع الدفعة:** توثيقية فقط.

Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — UX-17 / ق-108

**المجال:**

Final Cross-Cutting UX Consistency.

**الملف الجديد:**

- `technical/FINAL_CROSS_CUTTING_UX_ARCHITECTURE.md`

**أهم القرارات:**

- canonical terminology.
- explicit Offline/Sync states.
- local-save vs server-confirmation honesty.
- stable status UI.
- form/error/success consistency.
- duplicate-submit protection.
- financial/sensitive review.
- loading/empty/error/stale distinction.
- 48dp Android targets.
- RTL/accessibility/adaptive layout.
- notification/support/privacy consistency.
- navigation/back/context safety.
- م-36 opened.

**الحالة:**

UX-00..UX-17 Design Complete.

PA-01..PA-04 Design Complete.

**التالي:**

IMPLEMENTATION-01.

**نوع الدفعة:** توثيقية فقط.

Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — IMPLEMENTATION-01 / ق-109

**المجال:**

V1 implementation sequencing and dependency planning.

**الملف الجديد:**

- `technical/V1_IMPLEMENTATION_SEQUENCE.md`

**اعتمد:**

- W1–W10.
- backend foundations first.
- Offline foundation second.
- vertical slices.
- domain-sized migrations.
- permanent tests.
- Trusted Admin Backend before Admin Web.
- owner-run DB/Docker verification.
- م-37.

**التالي:**

W1 — Backend Foundations.

**نوع الدفعة:** توثيقية فقط.

Baseline الاختبارات لم يتغير.

## تحديث 2026-08-19 — W1-01 / ق-110

**المجال:**

Canonical Login Profile ↔ Business Person identity.

**التنفيذ المحضر:**

- Migration 078.
- Permanent Test 078.
- Internal identity resolver.

**لا يشمل:**

- Farmer RLS rewrite.
- Role Catalog wiring.
- Entitlements.
- OTP.

**الحالة:**

Prepared / Pending Owner Verification.

لا Commit/Push قبل دليل الاختبار.

---

## 2026-08-19 — W1-01 local verification + Cloud baseline sync

- سجلت مزامنة Supabase Cloud baseline حتى Migration 077.
- سجلت Data API boundary السحابية: `api` + `graphql_public`.
- سجلت استعادة Auth config بعد التغيير غير المقصود.
- سجلت Owner Verification الحقيقي لـMigration 078.
- Local baseline: 77 migration files / 18 tests / 235 PASS.
- Cloud baseline: through 077؛ Migration 078 pending cloud deployment.

---

## 2026-08-19 — W1-01 Cloud verification closure

- صححت Cloud baseline إلى applied-through-078.
- سجلت Cloud structural/security verification لق-110.
- ثبتت Migration 071–078 immutable.
- نقلت RESUME_POINT إلى W1-02 / م-16.
- أي DB change جديد يبدأ Migration 079+.

---

## 2026-08-19 — W1-02 local verification

- وثقت ق-111 / Farmer self-scope.
- وثقت Migration 079 وPermanent Test.
- Local baseline صار 78 migration files through 079.
- Permanent tests = 19 files.
- Full DB Suite = 255 PASS / 0 FAIL / 0 ERROR.
- Cloud baseline بقي through 078؛ 079 pending.
- RESUME_POINT انتقل إلى Cloud deploy/verify لـ079.

---

## 2026-08-19 — W1-02 Cloud verification closure

- ثبتت Cloud baseline = 78 migrations through 079.
- سجلت 19 Farmer self-scope policies وغياب legacy broad Farmer policies.
- سجلت API/Direct DML invariants بعد 079.
- أغلقت م-16.
- ثبتت Migration 071–079 immutable.
- نقلت RESUME_POINT إلى W1-03 / م-18.
- أي DB change جديد يبدأ 080+.

---

## 2026-08-21 — W1-03a local verification / ق-112

- وثقت ق-112 / Permission Authority Foundation في DECISIONS.
- وثقت Migration 080 والاختبار الدائم في MIGRATIONS.
- سجلت توسيع Permission catalog من 21 إلى 38 code.
- سجلت `iam.well_assignment_role_map` = 6 صفوف مع استثناء `farmer` عمدًا.
- سجلت `iam.role_permissions` = 70 منح:
  tenant_owner 38 / well_manager 12 / operator 20.
- سجلت partner / accountant / viewer = 0 منح كقرار مؤجل صريح.
- سجلت `iam.has_well_permission` كدالة الصلاحية القانونية الجديدة.
- سجلت أن 273 policy على `iam.has_well_role` لم تتغير.
- Local baseline = 79 migration files through 080؛ 067 رقم غير مستخدم تاريخيًا.
- Permanent tests = 20 files.
- Permanent Test 080 = 20 PASS / 0 FAIL / 0 ERROR.
- Full DB Suite = 275 PASS / 0 FAIL / 0 ERROR.
- API = 33 authenticated / 0 anon / 0 SECURITY DEFINER؛ Direct DML = 0.
- حدّثت م-18 كمفتوحة ومحصورة في Enforcement wiring بدل الكتالوج.
- سجلت تحذير تشغيلي: `accountant` و`viewer` قابلان للتعيين بلا صلاحية فعلية،
  فلا يعرضان في أي واجهة قبل Migration 081.
- ثبتت Migration 071–080 immutable.
- Cloud baseline بقي through 079؛ 080 pending.
- نقلت RESUME_POINT إلى Cloud deploy/verify لـ080.
- أي DB change جديد يبدأ 081+.
- مؤجل بقرار المالك إلى جولة تنظيف مستقلة:
  `PROJECT_MAP.md` و`INVARIANTS.md`.

---

## 2026-08-21 — W1-03a Cloud verification closure

- سجلت Cloud verification لـ080: `CLOUD_080_ALL_PASS` = 20/20.
- سجلت remote migration history = 79 through `20260819235001`.
- سجلت `DATA_API_BOUNDARY=OK` من خارج قاعدة البيانات:
  default exposed schema = `api` وanon مرفوض،
  و`core`/`iam`/`public`/`audit`/`reporting` محجوبة،
  وجداول عبر `api` = 0.
- وثقت حدث إعادة بناء المشروع السحابي في `MIGRATIONS.md`:
  حساب جديد، منطقة South Asia (Mumbai)، 79 migration بالترتيب،
  ولا شيء فُقد لأن المشروع السابق كان Schema بلا بيانات.
- وثقت قناة النشر السحابي المثبتة: Supavisor transaction mode
  على المنفذ 6543 هي القناة العاملة الوحيدة؛ المنفذ 5432
  والاتصال المباشر IPv6 لا يصلان، فـ`db push` لا يعمل.
- وثقت أن النشر يجري بسكربت `psql` قابل للاستكمال ينقل نفس
  الملفات بلا تعديل ويسجّلها في
  `supabase_migrations.schema_migrations`.
- وضّحت في `V1_IMPLEMENTATION_SEQUENCE.md` القاعدة 9 أن هذا
  نقل لا تعديل يدوي، والملفات تبقى المصدر الوحيد.
- وثقت أن `api` تُنشأ في Migration 071، فترتيب العمل الصحيح =
  login → build → set Exposed schemas → verify، و`api` أولًا.
- حدّثت م-18 بدليل التحقق السحابي مع بقائها مفتوحة.
- حدّثت ق-112 إلى Cloud Verified في DECISIONS والمصفوفة.
- أغلقت W1-03a في PROGRESS و`V1_IMPLEMENTATION_SEQUENCE`.
- نقلت RESUME_POINT إلى Migration 081 / W1-03b Enforcement wiring.
- ثبتت Migration 071–080 immutable؛ أي DB change جديد يبدأ 081+.
- التحذير التشغيلي لـ`accountant` و`viewer` ما زال قائمًا.

## 2026-08-22 — W1-03b Permission Enforcement Wiring / ق-113

- وثقت ق-113 في `DECISIONS.md`: نقل إنفاذ أجساد الدوال من
  مصفوفات الأدوار النصية إلى Permission Codes، بثمانية قواعد
  حاكمة أولها Zero Behavior Change وثانيها Proof before Write.
- سجّلت في القرار الاستكشاف الذي غيّر التنفيذ: مواضع الحرس
  29 لا 23؛ `ops.change_session_energy_source` بلا permission
  code إطلاقًا؛ `shift.close_override` موجودة أصلًا ومطابقة؛
  موضع ميت في 069 تُسقطه 075؛ وأربع دوال تفوّض بالهوية لا
  بالدور.
- وثقت البرهان: 28 EQUIVALENT / 1 MISSING_CODE / 0 DIFFERS
  = `NO_SILENT_DRIFT`، ثم diff آلي على الناتج بصفر تغيير غير
  مقصود، ثم تدقيق 283 إشارة عمود بصفر خطأ.
- أضفت قسمي Migration 081 و082 في `technical/MIGRATIONS.md`
  مع جدولَي «الدالة ← الصلاحية» الكاملين، وسبب أخذ
  `ops.create_farm` من تعريف 075 لا 069، وسبب بقاء حرس
  الهوية في `api.close_shift` إلى جانب صلاحية التجاوز.
- صحّحت ملاحظة الترقيم في `MIGRATIONS.md`: ملفات الترحيل
  المحلية = 81 وأعلى رقم = 082.
- **أغلقت م-18 في `OPEN_ISSUES.md`** بدليل محلي وسحابي، مع
  تسمية ما بقي خارج نطاقها عمدًا: 273 RLS policy تبقى طبقة
  توافق للقراءة وهي الآن مستهلكها الوحيد.
- حدّثت الفجوة 4 في م-31 المفتوحة لتشير إلى إغلاق م-18 بدل
  «لم تغلق بعد».
- أضفت قسم W1-03b في `PROGRESS.md` بلغة الأثر على المستخدم
  أولًا ثم الأدلة الرقمية.
- أضفت صف ق-113 في `DECISION_IMPLEMENTATION_MATRIX.md`،
  وعدّلت صف ق-112 إلى superseded on enforcement.
- أغلقت W1-03 في `V1_IMPLEMENTATION_SEQUENCE.md` وحدّثت
  القاعدة 2: Migration 071–082 immutable، والجديد يبدأ 083+.
- نقلت `RESUME_POINT.md` إلى ما بعد W1: W1 كلها مغلقة،
  والخيارات المطروحة = W2 / م-25 كمسار افتراضي بحسب Rule C،
  أو دفعة RLS مستقلة، أو قرار صلاحيات
  `partner` / `accountant` / `viewer`.
- وثقت تعديل اختبار 080 من 38/70 إلى 39/73 مع التنصيص على أن
  ملف Migration 080 نفسه لم يُمسّ — تعديل اختبار مسموح لا
  تعديل هجرة مختومة.
- التحذير التشغيلي وُسّع ليشمل `partner` مع `accountant`
  و`viewer`: صفر منح بالتصميم، فلا تعرضهم أي واجهة قبل قرار
  صريح — ولم يتغير بإغلاق W1-03b.

### إغلاق التدقيق — 2026-08-22

- `technical/MIGRATIONS.md`: حُدّث رأس «الحالة الحالية
  الحاكمة» إلى 2026-08-22 — 81 migration حتى 082 محليًا
  وسحابيًا، 22 ملف اختبار دائم، 315 PASS / 0 FAIL / 0 ERROR،
  وأُضيف 080 و081+082 لقائمة المعالم.
- `technical/MIGRATIONS.md`: ذيل قسم 079 كان ما زال يقول
  «071–079 immutable / يبدأ 080+» فصُحّح إلى 071–082 و083+.
- `design/UX_UI_SPEC.md` قاعدة 549: «تبقى م-18 اعتمادًا
  قائمًا» صارت تحديث ق-113 — القاعدة منفذة والكتالوج هو
  مصدر الصلاحية فعليًا.
- تُركت عمدًا بلا تعديل السجلات المؤرخة التي تصف ما كان
  صحيحًا وقتها: كتل أدلة 079/080 داخل MIGRATIONS و
  DECISIONS و PROGRESS و OPEN_ISSUES و RESUME_POINT،
  وصفوف ق-111/ق-112 في المصفوفة، وقيود W1-02 في
  V1_IMPLEMENTATION_SEQUENCE، ومداخل DOC_CHANGELOG
  السابقة، و`RESEARCH_STANDARDS_GATE` (سجل بحث مؤرخ)،
  و`CONFORMANCE_AUDIT_CODEX` (لقطة تدقيق تاريخية تعلن
  عن نفسها أنها ليست الحالة الحالية).
- `memory/DECISIONS.md`: كتلة «الحالة» في ق-112 تحمل قاعدة
  ترقيم قديمة، فأُضيف تحتها مؤشر تجاوُز بق-113 بدل تعديل
  السجل المؤرخ نفسه.

---

## 2026-08-22 — W2-01 Server-side Idempotency / ق-114

- وثقت ق-114 في `DECISIONS.md`: توصيل طبقة `sync` القائمة
  بأغلفة `api.*` للدورة الميدانية الأولى، بثمانية قواعد
  حاكمة أولها «الجهة تُستخرَج على الخادم لا يُرسلها العميل»
  وثانيها «حدّ نطاق لا قرار صلاحية» وثالثها التوافُق الخلفي
  الحرفي عند `p_command_id = null`.
- سجّلت في القرار السياق بلغة الميدان: دفعة 500 تُسجَّل
  مرتين والحساب يقول 1000 عند ضياع الردّ — وهو السلوك
  الحتمي قبل هذه الجولة لا احتمال نظري.
- سجّلت الاكتشاف الذي بَرّر الجولة: طبقة 058 كانت **مبنية
  وغير موصولة** — لا دالة على الخادم تستدعيها، ولا واحدة من
  33 دالة `api.*` تقبل معرّف عملية.
- وثقت القيود التي شكّلت التصميم: سطح API مقفل على 33 دالة
  بخمسة اختبارات ⟹ **استبدال** التوقيع لا إضافة overload؛
  و072 سحبت DML من `authenticated` على `sync` ⟹ الغلاف
  invoker لا يكتب مباشرة؛ و`sync` غير مكشوف عبر Data API ⟹
  الغلاف في `api` إلزامًا.
- وثقت جدول البدائل المستبعدة الثمانية وسببَ رفض كل بديل،
  ومنها «تمرير `tenant_id` من العميل» لأنه يسمح بحجز معرّف
  عملية في جهة أخرى فتبدو عملية الضحية مكرَّرة فلا تُنفَّذ.
- أضفت قسمي Migration 083 و084 في `technical/MIGRATIONS.md`:
  جدول سحب المنح، جدول المُحلِّلات الأربعة، جدول الأغلفة
  الثمانية بأعداد الوسائط وأنواع الإرجاع، ومنطق الغلاف
  بخمس خطوات.
- أضفت في قسم 083 عنوان **Scope is not Authority** ببرهانه:
  الشرط لا يُحدث انحرافًا لأن `iam.has_well_permission` نفسها
  تشترط `wa.profile_id = auth.uid() and wa.status = 'active'`
  (`080:243`) ⟹ لا يرفض المُحلِّل شيئًا كانت الدالة الداخلية
  ستقبله.
- وثقت في 084 قرار **«لا تسجيل للرفض ولا حالة عالقة»**:
  استدعاء RPC واحد = transaction واحدة، فالعملية المرفوضة
  تتراجع بكاملها بما فيها صفّ الحجز ⟹ لا حالة `processing`
  عالقة ولا حاجة إلى مُنظِّف دوري.
- صحّحت ملاحظة الترقيم في `MIGRATIONS.md`: ملفات الترحيل
  المحلية = 83 وأعلى رقم = 084 (الرقم 067 غير مستخدم
  تاريخيًا).
- حدّثت رأس «الحالة الحالية الحاكمة» في `MIGRATIONS.md` إلى
  83 migration حتى 084 محليًا وسحابيًا، 24 ملف اختبار دائم،
  354 PASS / 0 FAIL / 0 ERROR، وأُضيف 083+084 لقائمة المعالم.
- **ضيّقت م-25 في `OPEN_ISSUES.md` ولم أغلقها:** حالتها
  صارت «مفتوحة — ضُيِّقت بق-114»، وأُضيف قسم «أُنجز وأُثبت»
  وقسم «لماذا لا تُغلق»، وشُطب `idempotent API replay` من
  المتبقي بوصفه **منفَّذًا خادميًا**. كتلة «لا يغلق قبل»
  بقيت كما هي حرفيًا — معايير القبول لم تتغير.
- حدّثت الفجوة 3 في م-29 لتسجّل الشق الخادمي منفَّذًا بق-114،
  وأضفت الفجوة 14: دفعة الرصيد المقدم تُكتب ولا تُقرأ.
- أضفت صف ق-114 في `DECISION_IMPLEMENTATION_MATRIX.md`، وصف
  م-25 في «فجوات ليست منجزات»، وأرّخت الـbaseline القديم
  (76/17/217) كلقطة 2026-08-17 تحيل إلى `MIGRATIONS.md`
  للأرقام الحالية.
- صحّحت `technical/SYNC_ARCHITECTURE.md`: القسم 5 كان يقول
  إن الدمج «لم يُعتبر منجزًا بعد»، فحلّ محله تحديث مؤرّخ
  يسمّي الأغلفة الثمانية وضمانة عدم تضاعف الإجمالي واستخراج
  الجهة خادميًا، ويُسمّي التزام المعرّف الثابت على الهاتف
  بوصفه بقية م-25. والقسم 2 يسجّل أن 058 كانت غير موصولة
  وأن منح `PUBLIC` سُحبت.
- صحّحت `technical/ANDROID_OFFLINE_BACKGROUND_SYNC.md`:
  القسم 7 كان يقول إن العقود «يجب أن تدمج idempotency قبل
  اعتبارها جاهزة»، فصار تحديثًا مؤرّخًا يعلن الخصائص الثلاث
  منفَّذة خادميًا للثماني، ويحدّد الباقي على Android —
  **توليد المعرّف مرة واحدة لكل عملية ميدانية**، لأن توليده
  لكل محاولة يُبطل الحماية كلها.
- في نفس الملف: «أول DB Migration جديدة: 078 أو أحدث» صارت
  085؛ وبند Definition of Done «api idempotency مربوط» شُطب
  وأُلحق به أنه مربوط خادميًا للثماني ويبقى إرسال المعرّف
  الثابت وتوسيع النمط.
- أضفت قسم W2-01 في `PROGRESS.md` بلغة الأثر على المستخدم
  أولًا ثم الأدلة الرقمية، مع تأريخ سطرَي «071–082 / 083+»
  في قسم W1-03b بوصفهما حالة ذلك اليوم.
- نقلت `RESUME_POINT.md` إلى ما بعد W2-01: الخطوة التالية
  صارت **W2-02 طابور الهاتف**، وأُضيف توسيع نمط ق-114 لبقية
  العقود كخيار ثانٍ، وحُدّث الـbaseline إلى 83 ملفًا وأعلى
  رقم 084، وأُضيفت م-25 إلى قائمة المسائل المفتوحة موصوفة
  بأنها ضُيِّقت.
- حدّثت `technical/V1_IMPLEMENTATION_SEQUENCE.md`: القاعدة 1
  صارت 071–084 immutable والجديد يبدأ 085+، وأُضيف قسمان
  في W2 — «W2-01 مكتملة ومغلقة» بأدلتها و«W2-02 لم تبدأ»
  بما تبقى من م-25.
- وثقت تعديل اختبارَي 073 و075 في موضعين محدَّدين فقط
  لتوقيعَي `api.create_farm` و`api.start_irrigation_session`،
  مع التنصيص على أن السطر الرافض للتوقيع الزائد في 073 بقي
  كما هو، وأن موضع `ops.create_farm` الداخلية في 075 لم
  يُمسّ، وأن ملفَي Migration 073 و075 أنفسهما لم يُمسّا —
  تعديل اختبار مسموح لا تعديل هجرة مختومة.
- أُضيف مؤشر تجاوُز تحت كتلة «الحالة» في ق-113 لأنها تحمل
  قاعدة ترقيم قديمة (071–082 / 083+)، بدل تعديل السجل
  المؤرّخ نفسه.
- التحذير التشغيلي بشأن `partner` / `accountant` / `viewer`
  لم يتغير: صفر منح بالتصميم، فلا تعرضهم أي واجهة قبل قرار
  صريح.

### ملاحظة مكتشفة أثناء التحقق — ليست من هذه الجولة

`payments_select_assigned` (`016:45`) تشترط ارتباط الدفعة
بفاتورة جلسة، فدفعة الرصيد المقدم (`session_charge_id = null`)
تُكتب بنجاح لكن **لا تُقرأ** عبر دور التطبيق. الأثر: المشغّل —
وهو من يستلم النقد — لن يرى الدفعة على شاشته حين تُبنى شاشة
الدفعات.

سُجّلت في ثلاثة مواضع بمستوى بروز مناسب: ملاحظة «ليست من هذه
الجولة» في ق-114، ونفسها في قسم 084 من `MIGRATIONS.md`،
وفجوة 14 قابلة للإجراء تحت م-29. ولم يُختَرع لها رقم مسألة
جديد. وأُضيفت إلى «مؤجل» في `RESUME_POINT.md` بوصفها قرارًا
صريحًا مطلوبًا: هل يرى المشغّل دفعات مزارعي بئره كلها أم
المرتبطة بفاتورة فقط؟

### تُركت عمدًا بلا تعديل

السجلات المؤرَّخة التي تصف ما كان صحيحًا وقتها: كتل أدلة
079/080/081/082 داخل `MIGRATIONS` و`DECISIONS` و`PROGRESS`
و`OPEN_ISSUES` و`RESUME_POINT`، وصفوف ق-111/ق-112/ق-113 في
المصفوفة، ومداخل `DOC_CHANGELOG` السابقة. القاعدة نفسها
المتّبعة في تدقيق W1-03b: قاعدة ترقيم قديمة داخل سجل مؤرَّخ
تُعالَج بمؤشر تجاوُز لا بتعديل السجل.

وكذلك تُركت — بتسجيل صريح لا بإغفال — قاعدة الترقيم القديمة
«أول DB Migration جديدة: 078 أو أحدث». **قِيس حجمها بعد
الحفظ فبان أوسع بكثير من التقدير الأول: 123 موضعًا في 24
ملفًا، لا ست وثائق.** القاعدة خاطئة منذ تطبيق 078 فعلًا
وليست من إنتاج هذه الجولة، والخطر حقيقي لا تجميلي: من يتبعها
يبدأ هجرة تصطدم بملف مختوم. الصواب = 085 أو أحدث.

المواضع قسمان: قواعد حيّة تُصحَّح (9 ترويسات معمارية، و15
ثابتًا مرقّمًا في `INVARIANTS.md` منها الثابت 681 «071–077
immutable»، و14 قاعدة في `API_ARCHITECTURE.md`، و20 موضعًا في
`UX_UI_SPEC.md`، وخلايا المصفوفة، و`README` و`PROJECT_MAP`)؛
وسجلات مؤرَّخة تُترك كما هي (`PROGRESS` 19 و`DOC_CHANGELOG`
11 و`DECISIONS` 29 وكتل أدلة `OPEN_ISSUES`).

لم تُعدَّل هنا لأن سحب 123 موضعًا داخل حفظة خاصة بحماية
التكرار كان سيخفي تغييرها الحقيقي، ولأنها جولة قائمة بذاتها.
التفصيل الكامل والتصنيف في `RESUME_POINT.md`.

> **تجاوُز:** نُفِّذت هذه الجولة في نفس اليوم بعد حفظ W2-01 —
> انظر المدخل التالي «تصحيح قاعدة الترقيم».

---

## 2026-08-22 — تصحيح قاعدة الترقيم (جولة تنظيف مستقلة)

**السبب:** قاعدة «أول DB Migration جديدة: 078 أو أحدث» كانت
مكتوبة في 123 موضعًا موزَّعة على 24 ملفًا. صارت خاطئة منذ
تطبيق 078 فعلًا — أي قبل جولة W2-01 ولا علاقة لها بها. الخطر
تشغيلي لا تجميلي: من يقرأ أي موضع منها ويبدأ Migration 078
يصطدم بملف مختوم موجود. الصواب النافذ = **085 أو أحدث**،
و**071–084 immutable**.

**لماذا جولة مستقلة:** المواضع تنقسم قسمين مختلفَي المعالجة،
و`sed` أعمى على 24 ملفًا كان سيُفسد السجل التاريخي للمشروع.

### أ — قواعد حيّة صُحِّحت

- 9 ترويسات «أول DB Migration جديدة» في الوثائق المعمارية:
  `SEARCH_DEDUP_ARCHITECTURE:6` و`ACTIVE_SESSION:7` و
  `SESSION_SETTLEMENT:7` و`OPERATIONS_RECORDS:8` و
  `MONEY_PARTNERS:8` و`WELL_MANAGEMENT_REPORTING:8` و
  `PLATFORM_ADMIN_ACCOUNTS_WELLS_SUPPORT:8` و
  `PLATFORM_ADMIN_SALES_OPERATIONS_FINANCE:9` و
  `PLATFORM_ADMIN_MONITORING_SETTINGS:9`.
  (`ANDROID_OFFLINE_BACKGROUND_SYNC:8` كانت صُحِّحت في W2-01.)
- `technical/INVARIANTS.md` — 15 ثابتًا مرقّمًا، ومنها الثابت
  681 صار «Migration 071–084 تبقى immutable».
- `technical/API_ARCHITECTURE.md` — 14 قاعدة.
- `design/UX_UI_SPEC.md` — 20 موضعًا، منها `:1060` و`:1094`
  بصيغة «Migration 078 أو أحدث» صُحِّحت يدويًا.
- `technical/DECISION_IMPLEMENTATION_MATRIX.md` — 14 خلية
  «Migration 078+ Pending» + خلية `:205` «correct in 078+».
- `technical/V1_IMPLEMENTATION_SEQUENCE.md` — قاعدة تصحيح 066
  (`:281`) وسطر «Next» الختامي (`:515`) الذي كان ما زال يشير
  إلى W1 وتخطيط 078.
- **`memory/AI_HANDOFF_PROTOCOL.md` — أخطر موضع في الحزمة**،
  لأنه أول ملف يقرأه أي مساعد جديد ويأخذ منه نقطة البدء:
  «Do not edit migrations 071–084» و«New DB changes begin
  085+»، وCurrent point صار W2 بدل W1.
- `docs/README.md` و`docs/PROJECT_MAP.md`.
- `SEARCH_DEDUP_ARCHITECTURE:293` — موضع مقسوم على سطرين
  («Migration» في آخر السطر و`078+` في أوله) نجا من الاستبدال
  الجماعي وصُحِّح منفردًا.

### ب — تُركت عمدًا: عبارات صحيحة *عن* الملف 078

هذه ليست قواعد بدء ترقيم بل توصيف لما فعلته 078 وما لم تفعله:
الثوابت 678/679/680 في `INVARIANTS.md`، و`API_ARCHITECTURE:829`،
و`SEARCH_DEDUP_ARCHITECTURE:702`، و`PROJECT_MAP:863`،
و`DECISION_IMPLEMENTATION_MATRIX:109`،
و`V1_IMPLEMENTATION_SEQUENCE:30` و`:521`.

### ج — سجلات مؤرَّخة تُركت كما هي

`PROGRESS.md` (19 موضعًا) و`DOC_CHANGELOG.md` (11) و
`DECISIONS.md` (29) وكتل أدلة `OPEN_ISSUES.md` (9). تصف ما كان
صحيحًا في تاريخه. و`RESEARCH_STANDARDS_GATE.md:909` (قسم
مؤرَّخ 2026-08-19) عُولج **بمؤشر تجاوُز** لا بتعديل — نفس
القاعدة المتّبعة في تدقيق W1-03b وW2-01.

### صُحِّح أيضًا في نفس الجولة

`PROJECT_MAP.md` كان يقول عن W1-01 «الحالة: Prepared /
Pending Owner Verification» وهي مكتملة ومغلقة ومتحقق منها
محليًا وسحابيًا منذ عدة جولات. صارت «مكتملة ومغلقة — Local +
Cloud verified».

### التحقق

- صفر موضع حيّ باقٍ من `078+` أو `071–077` في كل `docs/`
  خارج السجلات المؤرَّخة.
- أعداد السجلات المؤرَّخة لم تتغير: 19 / 11 / 29 / 9.
- 39 موضع «Migration 085» في الوثائق المعمارية.

### لا تغيير في السلوك

جولة توثيق خالصة: لا Migration، ولا اختبار، ولا سطر كود.
سلوك المستخدم وسطح API وقاعدة البيانات = بلا أي تغيير.

---

## 2026-08-23 — W2-02a طابور الجهاز الدائم / ق-115

**جولة كود + توثيق. صفر تغيير على قاعدة البيانات.**

### ما بُني

`apps/mobile/lib/core/sync/` — 13 ملف Dart، و
`apps/mobile/test/core/sync/` — 7 ملفات اختبار. أضيفت
`sqflite` و`path` إلى التبعيات، و`sqflite_common_ffi`
للتطوير.

قبل هذه الجولة كان التطبيق 6 ملفات Dart لا تحفظ شيئًا على
الجهاز؛ وكانت حماية التكرار الخادمية (ق-114) مبنية وغير
مستعملة لأن لا شيء في الهاتف يُرسل معرّفًا ثابتًا.

### القرار الجديد — ق-115

`DECISIONS.md`: قرار مرقَّم جديد يحسم بندًا تركته
`ANDROID_OFFLINE_BACKGROUND_SYNC.md` بند 5 مفتوحًا صراحةً
(«الاختيار النهائي يحتاج Migration/API tests»): **هوية الجلسة
= الربط الدائم المحلي↔الخادمي**، ببرهانه من Migration 084،
وسبب استحالة النموذج الآخر بلا Migration 085+ على دالة
مختومة. ومعه اختيار `sqflite` وسببه، وتسع قواعد تنفيذية،
وجدول ما استُبعد ولماذا.

`DECISIONS.md:3` — آخر قرار مرقّم صار ق-115.

### الوثائق التقنية

- **`ANDROID_OFFLINE_BACKGROUND_SYNC.md`:** الترويسة صارت
  «التنفيذ جزئي»؛ بند 3 كُتب فيه ما نُفِّذ من قائمة القاعدة
  المحلية وما بقي؛ **بند 5 صار محسومًا لا مخيَّرًا** بقسم
  «محسوم — ق-115» يحمل البرهان؛ بند 29 (Definition of Done)
  شُطب منه «Local DB موجودة» و«Outbox موجود»، وبند api
  idempotency صار «مربوط خادميًا **ومن الطابور**»، و«Worker
  موجود» بقي صريحًا كغير منفَّذ.
- **`SYNC_ARCHITECTURE.md`:** بند 3 («ما لم يعتبر منفذًا على
  الهاتف بعد») شُطب منه Local outbox وLocal durable database
  وretry queue وبقاء البيانات بعد إغلاق التطبيق، مع تحديد ما
  بقي في كل بند بدل شطبه كاملًا؛ بند 8 صار جدول حالة من 10
  صفوف يفصل المنفَّذ عن غيره.
- **`V1_IMPLEMENTATION_SEQUENCE.md`:** W2-02 من «لم تبدأ» إلى
  **W2-02a مكتملة** مع تفصيل ما نُفِّذ، وتقسيم المتبقي إلى
  W2-02b (الإرسال الخلفي) وW2-02c (سجل الجلسة النشطة) و
  W2-02d (الجاهزية والشاشات)؛ وسطر «Next» صار W2-02b.
- **`DECISION_IMPLEMENTATION_MATRIX.md`:** صف ق-115 كامل، وصف
  م-25 صار «ضُيِّقت مرتين».

### وثائق الذاكرة

- **`OPEN_ISSUES.md` / م-25: ضُيِّقت ثانيةً ولم تُغلق.** كتلة
  «أُنجز وأُثبت — ق-115» جديدة، و«لماذا لا تُغلق» أُعيدت
  كتابتها (السبب صار «الإرسال يحتاج أن يكون التطبيق مفتوحًا»
  لا «الهاتف لم يُبنَ»)، وقائمة المتبقي حُدِّثت بندًا بندًا:
  6 بنود شُطبت، و6 بنود وُصف ما تحقق منها وما بقي، و12 بندًا
  بقيت كما هي.
- **`PROGRESS.md`:** قسم W2-02a — المشكلة، وما صار عليه الحال،
  والثوابت الأربعة المفروضة بنيويًا، والقرارات التي شكّلت
  التنفيذ (ومنها إسقاط عمود `depends_on_local_id` من مخطط
  الخطة كآلية ثالثة زائدة)، وما تُبرهنه الحزمة. **مكتوب فيه
  صراحةً أن التحقق بانتظار تشغيل المالك** ولا يُعتمد قبل
  الناتج.
- **`RESUME_POINT.md`:** الخطوة التالية صارت W2-02b، والقسم
  «بانتظار المالك — تحقق W2-02a» يحمل أمر التحقق الوحيد
  المطلوب، و«المسألة الحالية» تفصل ما حسمته ق-114 عمّا حسمته
  ق-115 عمّا بقي.

### حدود الجولة

لا Migration، ولا اختبار SQL، ولا نشر سحابي، ولا `db:reset`.
Migration 071–084 تبقى immutable وأي DB change جديد يبدأ 085+.
ولا واجهة ميدانية: الكود المبني عقدُ كتابة لا شاشة، ولا شيء
يستخدمه بعد.

### التحقق — نتيجة فعلية لا متوقعة (2026-08-23)

`flutter analyze` = `No issues found!`
`flutter test` = **69 PASS / 0 FAIL**.

التشغيل الأول أعاد 4 ملاحظات أسلوبية (لا أخطاء) في
`command_transport.dart` و`outbox_repository.dart` و
`sync_engine.dart`، أُصلحت بلا أي تغيير في السلوك ولا في أسماء
الوسائط. والتشغيل الثاني أسقط اختبارًا واحدًا من 69:
مساعد `sqlite_outbox_store_test.dart` كان يبني مولّد معرّفات
جديدًا لكل مستودع، فيعيد الترقيم من أوله بعد إعادة فتح ملف
القاعدة ويصطدم بشرط الفرادة على `command_id`.

**الكود الإنتاجي رفض المعرّف المعاد — وهو بالضبط الثابت الذي
بُنيت عليه الجولة.** العيب كان في المساعد لا في المخزن؛ المولّد
الحقيقي `SecureIdGenerator` لا يعيد معرّفًا أبدًا. صُحِّح المساعد
إلى مولّد واحد لكل اختبار، مع تعليق يشرح لماذا لا يجوز غير ذلك.

حُدِّثت بعدها جميع مواضع التحقق من «بانتظار المالك» إلى النتيجة
الفعلية: `PROGRESS.md` و`RESUME_POINT.md` و
`V1_IMPLEMENTATION_SEQUENCE.md` و`DECISION_IMPLEMENTATION_MATRIX.md`
و`OPEN_ISSUES.md`.

---

## 2026-08-23 — W2-02c سجل الجلسة النشطة والاستعادة / ق-116

**المجال:** Flutter — نموذج قراءة الجلسة الجارية واستعادتها
بعد موت التطبيق. **تنفيذي + توثيقي.**
**Baseline:** تغيّر — من 69 PASS إلى **115 PASS / 0 FAIL**.

### ماذا تغيّر في الكود

`apps/mobile/lib/core/session/` — 5 ملفات جديدة:
`session_business_state.dart` (حالة السقي ونصوصها المعتمدة)،
`time_integrity.dart` (المرساة والقراءة وحسم «الآن» والأعلام)،
`session_segment.dart` (المقاطع والزمن المحتسب والمستحق)،
`active_session_record.dart` (نموذج القراءة والدفعات المحلية)،
`active_session_projector.dart` (إعادة التشغيل من الطابور).

`apps/mobile/test/core/session/` — 4 ملفات، 46 اختبارًا.

**لا Migration، ولا عمود، ولا جدول، ولا تغيير على `api`.**

### لماذا تغيّر

بند 16 من `ACTIVE_SESSION_ARCHITECTURE.md` يُلزم استعادة
الجلسة بعد Process Death ويمنع الاعتماد على `Timer` في
الذاكرة، ولا يحدّد مصدر الاستعادة. القرار حسم المصدر:
الطابور نفسه، لا جدول حالة موازٍ.

### القرار المبرِّر

**ق-116** — سجل الجلسة النشطة يُشتق من الطابور لا يُخزَّن
مرتين. جدول موازٍ يعني كتابتين لكل حدث، وانقطاعٌ بينهما يترك
حالة تخالف الأحداث المحفوظة وتُعرض كأنها صحيحة.

### الملفات الوثائقية المحدَّثة

- **`DECISIONS.md`:** ق-116 كاملًا — المسألة، القرار بسبعة
  بنود، برهان قاعدة القسمة من Migration 066 (198 مقابل 199)،
  جدول البدائل المرفوضة بأسبابها، الأثر على UX/Backend/
  البيانات/المالي، وما يحتاج تنفيذًا لاحقًا.
- **`PROGRESS.md`:** قسم W2-02c — النتيجة الفعلية 115 PASS،
  والمشكلة، والثوابت الأربعة، و**الخلل الذي وُجد وأُصلح داخل
  الجولة** (اشتقاق القراءة من المرساة كان يُبطل كشف تعديل
  الساعة)، وما تُبرهنه الحزمة.
- **`RESUME_POINT.md`:** م-25 تضيق ثالثةً، وW2-02c مُغلقة،
  وW2-02b تبقى الخطوة التالية مع سبب حجبها في بيئة المساعد.
- **`PROJECT_MAP.md`** (بوابة التوثيق بند 12 — آخر قرار حاكم
  ومسار الاستئناف تغيّرا): آخر قرار مرقم 110 → **116**؛ نقطة
  التنفيذ الحالية W1 → **W2** بحالة كل بند فرعي؛ سطر «المزامنة
  على الهاتف لم تُعتبر منجزة» صار وصفًا دقيقًا لما نُفِّذ وما
  لم يُنفَّذ، **مع تصحيح PowerSync**: لم يُستخدم، والأساس
  المنفَّذ Outbox على `sqflite` عبر `api.*`؛ وبند 16 من
  `ACTIVE_SESSION_ARCHITECTURE` وُسم منفَّذًا. وقائمة المسائل
  المفتوحة كانت تصف م-16 وم-18 مفتوحتين بعد إغلاقهما بق-111
  وق-113 — صُحِّحت، وأُضيفت م-25 وم-26/م-27.
- **`memory/AI_HANDOFF_PROTOCOL.md`** — أول ملف يقرأه أي مساعد
  جديد؛ سطر «نقطة العمل الحالية» كان يقول «التالي W2-02 طابور
  الهاتف» وقد نُفِّذ الطابور فعلًا. صار يسمّي W2-02a وW2-02c
  مكتملتين وW2-02b التالية مع سبب حجبها.
- **`PROJECT_MAP.md` — بند 3 (baseline):** كان مجمَّدًا على
  2026-08-17 (76 migration / 17 test / 217 PASS) وهو أقدم من
  الواقع بأربع دفعات. صار قسمين منفصلين — قاعدة بيانات (83
  ملف / 24 اختبار / 354 PASS / آخرها 084) وكود هاتف (115 PASS)
  — مع حفظ رقم 2026-08-17 موسومًا **تاريخيًا للمقارنة فقط**.
- **`RESUME_POINT.md` — `Final verified baseline`:** كان يحمل
  نفس أرقام 2026-08-17 القديمة، وهو المصدر الوحيد لنقطة
  الاستئناف فلا يجوز أن يكون متخلّفًا. فُصل إلى قاعدة بيانات
  وكود هاتف بالأرقام الحالية.

### حدود الجولة — قيل صراحةً ولم يُداور

- **W2-02b لم تُنفَّذ.** `workmanager` و`connectivity_plus`
  و`permission_handler` غائبة عن `~/.pub-cache`، و
  `androidx.work` غائبة عن Gradle cache، والشبكة في بيئة
  المساعد لا تصل pub.dev. كتابة كود لا يُترجَم ولا يُختبَر
  كانت ستُنتج جولة كاملة من الأخطاء تظهر عند المالك لا هنا.
- **م-26 لم تُطابَق لإخفائها.** المستحق المحلي زمن فقط
  (ق-17)، وMigration 066 ما زالت تجمع `fuel_charge_minor`.
  الفرق مُوثَّق في ترويسة `session_segment.dart` ويُغلق في
  Migration 085+، ولم يُنسخ الخطأ إلى الهاتف ليتطابق الرقمان.
- **لا واجهة ميدانية.** المبني نموذج قراءة، ولا شاشة تستخدمه
  بعد؛ أسماء المزارع والأرض والمضخة تحتاج عقد قراءة `api.*`
  (بند 20 بند 2)، فالنموذج يحمل معرّفات ولا يخترع نصًّا.
- **بوابة التوثيق بند 17 خطوة 1 لم تُشغَّل.** `git` لا يقرأ
  إعداداته في بيئة المساعد: `.git/config.worktree` و
  `.git/config.lock` بمِلكية `nobody` وبلا صلاحية قراءة، فيخرج
  `git` بـ`fatal: unknown error occurred while reading the
  configuration files`. لذلك لم يُشغَّل `git diff --check` ولم
  يُدَّع نجاحه، ولا قائمة ملفات مُغيَّرة مُستخرجة من `git`.
  الأثر عند المالك = صفر؛ الملفان بحجم 0 بايت ومتخلّفان عن
  عملية سابقة، وحذفهما (أو `sudo chown kali`) يعيد `git` للعمل.

Migration 071–084 immutable. أي DB change جديد يبدأ 085+.

---

## 2026-08-23 — W2-02b الإرسال الخلفي بلا فتح التطبيق / ق-117

**المجال:** Flutter — جدولة الإرسال ونظام التشغيل.
**تنفيذي + توثيقي.**
**Baseline:** تغيّر — من 115 PASS إلى **155 PASS / 0 FAIL**.

### ماذا تغيّر في الكود

`apps/mobile/lib/core/sync/` — 6 ملفات جديدة للمنطق:
`background_sync_policy.dart` (قرار العامل: يُعاد أم انتهى،
والتراجع)، `background_sync_coordinator.dart` (تنفيذ نافذة
الإرسال الواحدة والتفريغ)، `background_sync_scheduler.dart`
(الواجهة المجرَّدة للجدولة)، `background_sync_worker.dart`
(نقطة الدخول التي ينادِيها النظام)،
`background_sync_trigger.dart` (مصادر الإيقاظ والكبح)،
`background_sync_binding.dart` (تركيب الطبقات في التطبيق).

ملف واحد لكل حزمة منصّة — لا تتسرّب أسماء الحزم إلى المنطق:
`workmanager_sync_scheduler.dart` و`connectivity_plus_watcher.dart`
مقابل الواجهتين `background_sync_scheduler.dart` و
`connectivity_watcher.dart`.

`outbox_database.dart` — نقطة واحدة لمسار ملف الطابور، لأن
التطبيق والعامل عمليتان منفصلتان على **نفس الملف**؛ حسابان
مختلفان للمسار = طابور لا يُفرَّغ أبدًا بلا رسالة خطأ.

`apps/mobile/test/core/sync/` — 3 ملفات جديدة + مساعد،
**40 اختبارًا جديدًا**.

معدَّلة: `lib/main.dart` (تسجيل نقطة الدخول الخلفية قبل أي
جدولة)، `android/app/src/main/AndroidManifest.xml`
(`ACCESS_NETWORK_STATE` وحدها)، و`pubspec.yaml`/`pubspec.lock`
(`workmanager 0.10.9` و`connectivity_plus 7.3.1` — ثبّتهما
المالك).

**لا Migration، ولا عمود، ولا جدول، ولا تغيير على `api`.**

### لماذا تغيّر

بند 8 من `ANDROID_OFFLINE_BACKGROUND_SYNC.md` يقول «re-enqueue
بينما توجد عناصر Pending» بلا أن يحدد **من** يُعيد الجدولة،
ولا ماذا يحدث لعنصرٍ معلَّق لن ينجح أبدًا بلا قرار إنسان —
وهذا حرفيًا حلقة إيقاظ لا نهائية على أمرٍ محجوب على مراجعة.
البند احتاج حسمًا لا شرحًا.

### القرار المبرِّر

**ق-117** — قيمة إرجاع العامل هي الحوار كلّه مع نظام التشغيل:
`false` ⟹ أعِد بالتراجع المسجَّل عند الجدولة، `true` ⟹ انتهى.
والعامل لا يجدول لنفسه أبدًا.

### الملفات الوثائقية المحدَّثة

- **`DECISIONS.md`:** ق-117 كاملًا — المسألة، 12 قرارًا مرقّمًا،
  Research & Standards Gate = PASS (الواجهات مقروءة من الحزم
  المثبَّتة فعلًا في `~/.pub-cache` لا من ذاكرة النموذج)، الخلل
  الذي وُجد وأُصلح داخل الجولة، جدول البدائل المرفوضة، الأثر،
  و«ما لم يُثبت بعد — بصراحة». وسطر «آخر قرار مرقّم» 115 → 117.
  وفي ق-116 أُضيف **تجاوُز مؤرَّخ** لبند الحجب بدل حذفه —
  القرارات لا تُمحى.
- **`OPEN_ISSUES.md`:** م-25 تضيق **رابعةً** ولا تُغلق؛ قسم
  «أُنجز وأُثبت — ق-117»، وإعادة كتابة «لماذا لا تُغلق»
  و«المتبقي» بحيث يُشطب المنفَّذ ويبقى المكشوف مكشوفًا.
- **`PROGRESS.md`:** قسم W2-02b — النتيجة الفعلية 155 PASS،
  الثوابت البنيوية الخمسة، والخلل الذي وُجد وأُصلح داخل الجولة
  (طابور موقوف كله على مراجعة كان يُوقظ الهاتف بلا فائدة، ومنه
  الحالة العابرة للأصول).
- **`RESUME_POINT.md`:** خط الأساس 115 → **155**؛ الخطوة التالية
  صارت **W2-02d** ثم قياسات بند 9؛ وقسم «ما لا يجوز إعادة عمله»
  حتى لا يُنقض القرار بحسن نية لاحقًا.
- **`AI_HANDOFF_PROTOCOL.md`:** سطر «نقطة العمل الحالية» كان
  يقول إن W2-02b محجوبة حتى يُثبِّت المالك الحزمتين — وقد
  ثبّتهما ونُفِّذت. صار يسمّي ق-117 مكتملًا وW2-02d التالية،
  ويقول صراحة إن الإثبات على الحاسوب لا على جهاز.
- **`DECISION_IMPLEMENTATION_MATRIX.md`:** صف ق-117 كامل
  (القرار / التنفيذ / البرهان / الحالة)، وصف م-25 صار أربع
  تضييقات، وصف ق-116 حمل `superseded` مؤرَّخًا لا مُستبدلًا.
- **`V1_IMPLEMENTATION_SEQUENCE.md`:** W2-02b من «لم يبدأ /
  محجوب» إلى **مكتملة بصفر تغيير DB**، وقاعدة 2 تذكر الجولات
  الثلاث، وسطر `Next:` صار W2-02d وقياسات بند 9.
- **`ANDROID_OFFLINE_BACKGROUND_SYNC.md`** (الوثيقة الحاكمة
  للموضوع): بند 8 حصل على «محسوم — ق-117» بثمانية بنود؛ بند 9
  يقول صراحة إن القياسات الأربعة **غير موصولة**؛ بند 10 يفصل
  المنفَّذ برمجيًا عن الشرح المؤجَّل إلى W2-02d؛ بند 11 يعدّد
  المُعلَن يدويًا ويُسجِّل ما تدمجه الحزمة للمراجعة قبل النشر.
- **`PROJECT_MAP.md`** (بوابة التوثيق بند 12 — آخر قرار حاكم
  ومسار الاستئناف تغيّرا): آخر قرار مرقم 116 → **117**؛ نقطة
  التنفيذ W2-02b مغلقة وW2-02d الباقية؛ baseline كود الهاتف
  115 → 155؛ وبند 8 من وثيقة الخلفية وُسم منفَّذًا في منطق
  القرار **لا على جهاز**؛ وم-25 صارت أربع تضييقات.

### حدود الجولة — قيل صراحةً ولم يُداور

- **لم يُجرَّب على جهاز.** المُثبت منطق القرار في Dart وحده:
  إعادة الجدولة بعد إقلاع الهاتف، وسلوك Force Stop، والمانيفست
  المدموج — لا شيء منها جُرِّب. وبناء Android لم يُجرَّب أصلًا
  لأن `androidx.work` غائبة عن Gradle cache في بيئة المساعد.
- **قياسات بند 9 غير موصولة.** أسباب الإيقاظ ورقم المحاولة
  مُعدَّدة ومُمرَّرة لتصلح للقياس، لكن لا شيء يسجّل الأزمنة ولا
  عمر أقدم عملية معلَّقة. مطلوبة قبل م-21.
- **لا شاشة.** لا واجهة تُظهر حالة المزامنة ولا الجاهزية ولا
  زر إعادة محاولة يدوية — كلها W2-02d.
- **`dart format` أعاد تنسيق 7 ملفات مُسلَّمة سابقًا** خارج نطاق
  الجولة: `command_reference.dart`، `command_transport.dart`،
  `in_memory_outbox_store.dart`، `outbox_store.dart`،
  `command_contract_test.dart`، `retry_classification_test.dart`،
  `sync_engine_test.dart`. تغيير تنسيق بحت لا سلوك، وأُعيد
  `analyze` + `test` بعده للتأكد (`No issues found!` و155 PASS).
  لم يُتراجَع عنه لأن `git` لا يعمل في بيئة المساعد؛ للمالك أن
  يُسقط هذه الأجزاء إن أراد فرقًا أضيق.
- **بوابة التوثيق بند 17 خطوة 1 لم تُشغَّل.** `git` ما زال لا
  يقرأ إعداداته في بيئة المساعد بعد حذف المالك للملفين
  (`Permission denied` على `.git/config.worktree`)، وجُرِّب
  تجاوُزان (`-c extensions.worktreeConfig=false` و
  `GIT_CONFIG_NOSYSTEM=1 --git-dir=.git`) وفشلا بالمثل. لذلك لم
  يُشغَّل `git diff --check` **ولا يُدَّعى نجاحه**؛ استُعيض عنه
  بحصر الملفات بـ`find . -newermt` وفحصها بـ`grep` من مسافات
  آخر السطر وعلامات التعارض.

Migration 071–084 immutable. أي DB change جديد يبدأ 085+.
## 2026-08-30 — P0 Create-Well Correctness

- م-38/م-39/م-40 أصبحت **Verified local** بعد الاختبارات الفعلية.
- Migration 087: اختبار مستهدف = 8 PASS / 0 FAIL / 0 ERROR.
- Full DB Suite بعد reset محلي = 25 files / 362 PASS / 0 FAIL / 0 ERROR.
- Flutter: targeted = 2/2 PASS؛ full = 222/222 PASS؛ analyze = No issues found.
- لم يحدث Cloud verification بعد؛ م-38 تبقى Cloud pending حتى ما بعد مراجعة ودمج PR.

## 2026-08-30 — P0 Cloud Verification Closure

- PR #3 دُمج بـSquash إلى `main` عند
  `0e3d46e873c4f6b1088b5d98b65c1b65316f17aa`.
- Remote Migration History يتضمن
  `20260830010001_087_setup_well_full_permissions`.
- فحص Cloud البنيوي أثبت:
  `api.setup_well_full` = SECURITY INVOKER،
  `core.setup_well_full` = SECURITY DEFINER،
  authenticated/service_role مسموحان، anon ممنوع،
  Direct DML = 0.
- تحقق سحابي مؤقت استدعى `api.setup_well_full` فعليًا
  كمستخدم `authenticated` ونجح في إنشاء البئر داخل Transaction.
- قيم التسعير المرصودة سحابيًا:
  Solar = 3500، Well Diesel = 7000، Farmer Diesel = 6000.
- `anon` رُفض كما هو مقصود.
- Transaction انتهت بـ`ROLLBACK`، وفحص ما بعدها وجد
  صفر بقايا في auth/profile/tenant/well.
- م-38 = Verified local + Cloud.
- م-39 = Verified local + Cloud.
- م-40 = Verified local؛ Cloud verification غير منطبق.
- P0 Create-Well Correctness مغلق.
- `RESUME_POINT` انتقل إلى **Pre-Production Audit Queue**.
- ق-120 تبقى نافذة: لا Feature/Screen جديدة قبل استمرار
  Audit → Inspection → Evaluation → Repair → Gap Closing.
- هذه الدفعة اللاحقة **توثيقية فقط**؛ لا تغيير كود ولا DB
  ولا إعادة تطبيق Migration.

## 2026-08-30 — Audit Finding م-41

- بدأ أول بند بعد إغلاق P0: Flutter ↔ Data API conformance.
- المسح المحلي = 76 Dart files.
- internal-schema access = 9.
- bare RPC candidates = 20.
- dotted `from()` candidates = 5.
- live `api` inventory = 34 RPC.
- من أسماء Bare RPC: 7 موجودة داخل `api` و13 غير موجودة
  بهذا الاسم.
- Server Data API boundary ما تزال صحيحة؛ الانحراف داخل Flutter.
- وُثقت المسألة الجديدة:
  **م-41 — Flutter Data API Boundary Drift**.
- الحالة = Confirmed Gap / Repair Now.
- `RESUME_POINT` أصبح:
  **NEXT = م-41 — Flutter Data API Boundary Repair**.
- لا Flutter code change في هذه الدفعة.
- لا Migration 088 في هذه الدفعة.
- Cloud inspection كان قراءة فقط.

## 2026-08-30 — م-41 Data API Regression Guard

- أضيف Regression Guard دائم لحد Flutter ↔ Data API.
- Known Debt المثبت:
  9 internal-schema accesses / 20 bare RPC / 5 dotted from.
- Targeted guard = 3/3 PASS.
- flutter analyze = No issues found.
- لا Production code change.
- لا Migration 088.
- NEXT = م-41A — إصلاح 7 RPC موجودة أصلًا داخل api في
  FinanceRepository.

## 2026-08-30 — م-41A Finance API RPC Boundary Repair

- أصلحت 7 Bare RPC في FinanceRepository بتوجيهها إلى
  `schema('api').rpc(...)`.
- Cloud read-only inspection أثبت وجود العقود السبعة وتوافق
  توقيعاتها مع الاستدعاءات الحالية.
- Bare RPC debt = 20 → 13.
- Internal-schema debt = 9.
- Dotted-from debt = 5.
- Data API Regression Guard = 3/3 PASS.
- flutter analyze = No issues found.
- Full Flutter regression = 225/225 PASS.
- لا Migration 088.
- لا Cloud write.
- NEXT = م-41B — Remaining Data API Contract Mapping.

## 2026-08-30 — م-41B1 Physical Fuel Count Repair

- رُبط الجرد الفعلي للوقود بـ
  `api.record_physical_fuel_count`.
- أصبح التطبيق يرسل well + tank الصحيحين.
- تحويل القياس: liters → milliliters عند API boundary.
- Backend failure لم يعد يتحول إلى success في مسار الجرد.
- Bare RPC debt = 13 → 12.
- Internal-schema debt = 9.
- Dotted-from debt = 5.
- Targeted tests = 9/9 PASS.
- flutter analyze = No issues found.
- Full Flutter = 228/228 PASS.
- Cloud contract inspection = read-only.
- لا Migration 088.
- لا Cloud write.
- NEXT = م-41B2 — Account Profile Read Boundary Repair.

## 2026-08-30 — م-41B2 Account Profile Read Boundary Repair

- Profile read أصبح عبر `api.app_bootstrap`.
- أزيل direct read من `iam.profiles`.
- أزيل mock profile fallback عند فشل Backend.
- أضيفت حالة خطأ واضحة وزر إعادة المحاولة.
- Internal-schema debt = 9 → 8.
- Bare RPC debt = 12.
- Dotted-from debt = 5.
- Targeted tests = 8/8 PASS.
- flutter analyze = No issues found.
- Full Flutter = 230/230 PASS.
- لا Migration 088.
- لا Cloud write.
- NEXT = م-41B3 — Remaining Account Write / Team Contract Mapping.

## 2026-08-30 — م-41B3A Profile Name Write Boundary Repair

- أضيفت Migration 088 محليًا.
- أضيف `api.update_profile_name(text)`.
- Flutter لم يعد يكتب الاسم مباشرة إلى `iam.profiles`.
- فشل Backend لا يعرض نجاحًا وهميًا عند حفظ الاسم.
- 088 = 7/7 PASS.
- Full DB = 26 files / 369 PASS.
- Flutter targeted = 11/11 PASS.
- flutter analyze = No issues found.
- Full Flutter = 233/233 PASS.
- Internal-schema debt = 8 → 7.
- Bare RPC debt = 12.
- Dotted-from debt = 5.
- Cloud 088 = موجودة في Remote Migration History؛ contract/security verified، residue = 0؛ لم يُنفذ نجاح mutation على حساب حقيقي.
- Team mapping أثبت غياب عقود read/add/status وعدم صلاحية
  إعادة التسمية العمياء.
- NEXT = م-41B3B — Team Boundary Gap Closure.


## 2026-08-30 — م-41B3B Team Boundary Gap Closure

- أزيلت `get_well_team`.
- أزيلت `add_team_member`.
- أزيلت `set_team_member_status`.
- أزيل Production Mock team.
- شاشة الفريق لا تعرض نجاحًا أو أعضاء وهميين.
- تظهر حالة صريحة أن إدارة الفريق لم تُفعّل بعد.
- Bare RPC debt = 12 → 9.
- Internal-schema debt = 7.
- Dotted-from debt = 5.
- Targeted Flutter = 12/12 PASS.
- flutter analyze = No issues found.
- Full Flutter = 234/234 PASS.
- لا Migration جديدة ولا DB/Cloud write.
- Team feature نفسها ما زالت Backend/Auth Gap وليست منفذة.
- NEXT = م-41C — Operations Read Boundary Repair.


## 2026-08-31 — م-41C1 Operations Read Contracts (farmers / farms / pumps)

- Migration جديدة **089** — أول عقود قراءة للعمليات في `api`:
  `list_well_farmers` و`list_well_farms` و`list_well_pumps`.
- الثلاثة INVOKER + STABLE + `search_path = pg_catalog, pg_temp`،
  fail-closed بـ42501، حد نتائج مثبت، ترتيب حتمي، anon محجوب.
- اختبار دائم جديد: `20260831_089_operations_read_contracts.test.sql`
  (20 تحققًا).
- `OperationsRepository`: `fetchFarmers`/`fetchFarms`/`fetchPumps`
  تحوّلت إلى `schema('api').rpc(...)`.
- أزيلت `_getMockFarmers` و`_getMockFarms` وmock المضخات
  وmock حساب المزارع في `fetchFarmerDetail`.
- أزيل النجاح الكاذب في `createFarmer`/`createFarm`.
- Internal-schema debt = **7 → 4** (الباقي: سجل الجلسات وتفصيلها).
- Bare RPC debt = **9** بلا تغيير.
- Dotted-from debt = **5** بلا تغيير.
- شاشات المزارعين وملف المزارع والعمليات وحقل البحث الذكي
  تعرض الآن حالة فشل صريحة مع إعادة المحاولة بدل بيانات مصطنعة.
- flutter analyze = **No issues found**.
- Full Flutter = **237/237 PASS** (كان 234؛ +3 اختبارات جديدة).
- DB (شغّلها المالك: `db:reset` ثم `db:test`): Target 089 =
  **20 PASS / 0 FAIL / 0 ERROR**؛ Full DB = **27 files / 389 PASS /
  0 FAIL / 0 ERROR** (كان 26 files / 369 PASS — صفر انحدار)؛
  Local migrations = **88** مطبقة حتى 089.
- Cloud = **غير منشورة**؛ الحالة Verified local لا Cloud Verified.
  ولا Direct DML جديد.
- NEXT = م-41C2 — قراءة سجل الجلسات وتفصيلها (Migration 090).

## 2026-09-03 — م-41D8 جولة الهوية الحقيقية (ق-122)

- **نوع الدفعة:** تنفيذية + توثيقية. **بلا هجرة** — العقد 077 كافٍ،
  والسقف يبقى 071–093 والتالي 094.
- **قرار جديد ق-122** في `DECISIONS.md`: الهوية تُقرأ مرة واحدة من
  `api.app_bootstrap()` وتُمرَّر وحدةً واحدة (`AppIdentity`) إلى كل شاشة.
  ومعه **المقايضة المعتمدة صريحًا** من المالك: فشل عقد الحساب يحجب
  الاستخدام بسبب معلن وإعادة محاولة، ولا تخزين محلي للهوية (مؤجَّل
  بق-120).
- **ثوابت جديدة 700–705** في `technical/INVARIANTS.md`: الهوية وحدة
  واحدة؛ مفتاح الطابور هو `auth.uid()` نفسه؛ لا حقل قابل للإفراغ حيث لا
  يُسمح بالغياب؛ ثلاث حالات معلنة لا رابعة؛ القيم الجاهزة في `test/`
  وحده؛ ولا لقب عام في موضع اسم إنسان على السند.
- **سجل الجولة** في `OPEN_ISSUES.md` قسم م-41D8: المحيط المقيس قبل، وما
  صار، والقياس، وما بقي مفتوحًا.
- **الكود:** ثلاثة ملفات جديدة في `lib/` (`core/identity/app_identity.dart`
  و`app/identity_gate.dart` و`app/authenticated_shell.dart`)، و23 ملف
  `lib/` معدَّلًا، و15 شاشة تستقبل `required this.identity`.
- **الاختبارات:** ملفان جديدان (`test/app/identity_gate_test.dart` بثمانية
  اختبارات، و`test/support/identity_fixture.dart` مصدرًا وحيدًا للقيم
  الجاهزة)، و18 ملف اختبار معدَّلًا.
- **دين الهوية:** `'well-1'` **45 → 0**، `'tenant-1'` **13 → 0**،
  `'active-user'` **8 → 0**، وكذلك `'بئر الخير الرئيسي'` و`'777123456'`
  و`placeholderAccountKey` → **0** في `lib/`.
- **حرس الحد:** 22 → **24 مقياسًا**، ومقياس الدين الخامس انقلب إلى
  `expected = []`.
- **إصلاح أداة تحقق:** `scripts/check_app.sh` — `--no-version-check`
  (انهيار الأداة كان يُقرأ فشل تحليل)، وبذر مخزن `sqlite3` المُعنون
  بالمصدر من المستودع الأصلي بسطر حالة صريح.
- **Baseline تغيّر:** `flutter analyze` (`lib` + `test`) =
  **No issues found!**، والحزمة **300 → 313/313 PASS**. أرقام القاعدة
  والسحابة **بلا تغيير** (92 هجرة / 500 PASS / `MISSING_IN_CLOUD=0`).
- **غير مثبت:** لا تشغيل على جهاز أندرويد حقيقي، ولا تحقق سحابي — لا شيء
  في الجولة يمسّ القاعدة ولا الشبكة.

## 2026-09-03 — ق-123 / ق-124 قرارات الفريق والرسائل النصية (م-41E)

- **نوع الدفعة:** **توثيقية بحتة** — قرار وخطة، **بلا سطر كود وبلا هجرة**.
  Baseline **لم يتغيّر**: الحزمة تبقى 313/313 و`analyze` نظيف، والقاعدة 92
  هجرة و500 PASS، والسقف 071–093 والتالي 094.
- **ق-123** — حساب الفريق يُهيَّأ بالدعوة ويُطالِب به صاحبه: لا كلمة مرور
  يكتبها المالك؛ ودعوة بصفر صلاحية لها انتهاء؛ ورمز باليد أساسًا والرسالة
  بديلًا؛ والربط يُحلّ عند كل دخول؛ وشاشة الرقم لا تفشي من هو مسجَّل؛
  والدخول يبقى بكلمة المرور. ومعه نطاق قراءة الشريك، و«المستلم» = المشغّل
  نفسه لا دورًا رابعًا، والأرقام النافذة (14 يومًا / 6 أرقام / 5 محاولات /
  60 ثانية / 5 رسائل يوميًّا).
- **ق-124** — التحقّق والإشعارات بالرسائل النصية مقرَّرة والربط مؤجَّل،
  والتطوير يمضي بالرمز اليدوي وبرمز ثابت محليًّا، والمزوّد قابل للاستبدال
  شرطًا معماريًّا.
- **الجذر المكتشَف بالقراءة** (لا بالتخمين): **دور المشغّل غير قابل
  للاستخدام أصلًا** — لا مسار في التطبيق لإنشاء حساب له، والربط يجري لحظة
  إنشاء البئر وحدها. ومعه أربع كِذبات في المصادقة: دخول ينجح بلا مصادقة عند
  انقطاع الشبكة، وتغيير رقم هاتف تمثيلٌ كامل لا يُقرأ فيه الرمز أصلًا،
  وخانة كلمة مرور حالية ديكور، وشارة «معتمد وموثق ✅» بلا تحقّق.
- **ثوابت جديدة 706–715** في `technical/INVARIANTS.md`.
- **`technical/ACCOUNT_SETTINGS_ARCHITECTURE.md`** كسب §25 دورة حياة
  الدعوة والتنشيط (خمس حالات لا سادسة)، و§26 جدول نطاق قراءة الشريك،
  و§27 حدّ الجولة وسبب تأجيل الاستعادة.
- **`OPEN_ISSUES.md`** قسم م-41E: المشكلة، والخطة بأربع مراحل ومعايير قبول
  كل مرحلة، والمقايضات المقبولة صريحًا بأسمائها.
- **`RESUME_POINT.md`**: §2 سابقة استثناء ثانية، و§4 صار البند الأول م-41E
  بمراحله، و§5 حدّ `partner` تغيّر و ثلاثة حدود جديدة، و§6 درس الفريق،
  و§7 المؤجَّلات محدَّثة.
- **المصفوفة** كسبت صفَّي ق-123 (Planned) وق-124 (Decided + Deferred).
- **`PROJECT_MAP.md`**: آخر قرار مرقم = ق-124.
- **`PROGRESS.md`** لم يُمسّ: لا شيء أُنجز بعد، وبوابة Progress تمنع تسجيل
  المخطَّط إنجازًا.
- **الأدلة الخارجية** التي بُني عليها ق-124 مسجَّلة داخل القرار نفسه
  (أسعار المزودين، وقيود شبكات اليمن، والتجربة المجانية، وهجمة تضخيم
  الإرسال) لا في محادثة.
