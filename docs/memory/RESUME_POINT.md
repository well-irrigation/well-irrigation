# نقطة الاستئناف

**آخر تحديث:** 2026-08-13

أول ملف يُقرأ عند استئناف العمل، وأول ملف يُحدَّث عند إيقافه.

---

## أين نحن الآن

**منتهٍ:** التحليل والقرارات الـ 49 وتنظيم الوثائق ومجلد الذاكرة واستلام النصوص القانونية ومراجعتها (ق-49).
**معلق عند المالك:** تنزيل مجلد `docs/` إلى جهازه حسب `INSTALL_INTO_PROJECT.md`، ثم أول Commit.
**التالي:** المرحلة صفر — تجهيز البيئة.
**لم يبدأ بعد:** لا يوجد سطر واحد من كود التطبيق. لا توجد هجرة مُطبّقة.

---

## تحذير إلزامي — ت-01

قبل إنشاء أي جدول فيه `ownership_percentage` أو `profit_percentage`، وقبل كتابة دالة توزيع الأرباح:
**توقف تمامًا، واعرض القرار ق-21 على المالك بمثال رقمي حي من أحد آباره، وانتظر ردّه.**
التفاصيل الكاملة في `REMINDERS.md`.

---

## أول خمس خطوات

1. إعادة تسمية مجلد المشروع من `well-management-platform` إلى `well-irrigation` — ق-41.
2. تثبيت Flutter وAndroid SDK حتى ينجح `flutter doctor`.
3. ضبط `ANDROID_HOME` و`ANDROID_SDK_ROOT`.
4. إثبات أن `npx supabase start` يعمل — لم يُثبت أبدًا.
5. أول Commit في Git ثم إنشاء مستودع GitHub خاص.

بعد كل خطوة: يُحدّث `PROGRESS.md` و`ENVIRONMENT.md` بالنتيجة الحقيقية لا بالنية.

---

## خطة الشهر — ق-08

إجمالي نحو 210 ساعة عمل.

1. المرحلة صفر — البيئة: يوم إلى يومين.
2. قاعدة البيانات الكاملة: 5 إلى 7 أيام.
3. المحرك المالي واختباراته: 5 إلى 7 أيام.
4. التطبيق الأساسي: 7 إلى 8 أيام.
5. بقية الأقسام: 5 إلى 6 أيام.
6. التجربة الميدانية: يومان إلى ثلاثة.

**تحذير موثّق:** لا يوجد هامش أمان. أي تعطّل يومين يُزيح الجدول كله.

---

## ما يجب ألا يُنسى

* الإشعارات **مطلوبة في النسخة الأولى** وليست مؤجلة — ق-34.
* يوم الجلسة هو **يوم النهاية** — ق-27.
* لا تقريب في أي مكان — ق-01 وق-12.
* جداول الاشتراكات `billing.*` وليس `core.*` — ق-26.
* `com.wellirrigation.app` لا يُغيّر بعد أول نشر — ق-41.

## تحديث حالة الاستئناف — 2026-08-13
هذا التحديث يلغي اي اشارة سابقة الى ان اعادة التسمية او رفع المشروع الى GitHub لا يزالان مهمة مستقبلية، فكلاهما اكتمل فعليا.
الحالة الحالية: المستودع الخاص well-irrigation/well-irrigation يحتوي تاريخا نظيفا من حفظتين بالبريد المقنع، والبيئة التقنية كاملة وجاهزة.
الخطوة التالية المعتمدة: تنفيذ ق-51، اعادة بناء ملفي الترحيل من الصفر، ثم بدء مرحلة قاعدة البيانات.

## تحديث نقطة الاستئناف — 2026-08-13 (بعد مرحلة قاعدة البيانات الكاملة)

**منتهٍ الآن:** المخطط الأساسي الكامل، RLS الكاملة مع اختبار عزل مؤكد، التوفير التلقائي، رصد الجلسات وجدول الإشعارات، دورة حياة توزيع الأرباح الكاملة. التفاصيل في `PROGRESS.md` و`technical/MIGRATIONS.md`، والقرارات ق-57 إلى ق-66 في `DECISIONS.md`.

**معلّق مهم — ت-01 غير مُغلق بالحرف الكامل:** آلية أكبر الباقي (ق-21) نُفّذت واختُبرت بمثال تركيبي لا بمثال حي من بئر حقيقي للمالك. **لا تُعتمد نهائيًا في الإنتاج قبل عرض مثال حقيقي وانتظار تأكيده أو تعديله** — انظر ت-09.

**التالي المقترح (لم يُقرر بعد):** رفع هذا العمل إلى GitHub، ثم الاختيار بين إغلاق ت-09، أو حارس تكرار الجلسات المفتوحة، أو تضييق م-15، أو الانتقال لبناء تطبيق Flutter الحقيقي.

**لم يبدأ بعد:** أي كود Flutter حقيقي (م-03 يخص فقط تطبيقًا تجريبيًا لإثبات البيئة).

## تحديث نقطة الاستئناف — 2026-08-13 (بعد إغلاق المرحلة 1)
**منتهٍ الآن:** المرحلة 1 (النواة) كاملة: tenants، persons، users (عبر profiles)، roles، permissions، wells، locations. التفاصيل في ق-68.
**التالي وفق الترتيب الموثّق:** المرحلة 2 (التشغيل) — water_lines، pump_line_links، farmer_profiles/farmer_well_accounts، bookings (ops.irrigation_bookings، booking_status_history، resource_reservations)، session_segments، price_schedules/price_rules — بجلب النص الحرفي من `03_implementation_schema.md` §13/14/15/18/19 قبل الكتابة.
