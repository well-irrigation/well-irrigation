# نقطة الاستئناف — 2026-09-04

**الحالة:** نافذ · **القرار الحاكم:** ق-93 · **السرد الزمني:** `RESUME_HISTORY.md`

هذا الملف **رأس قصير** يُقرأ في كل جلسة: الحالة الآن، والتالي، والحدود
الحيّة. السرد الزمني لكل الجولات السابقة انتقل حرفًا بحرف إلى
`docs/memory/RESUME_HISTORY.md` ويُقرأ عند الحاجة وحدها. ما بقي هنا ليس
تاريخًا — هو ما يُلزم الجولة القادمة.

## 1. أول أمر في الجلسة

    npm run c:state

يطبع تسعة أسطر: الفرع، والفارق عن `main`، والتغييرات غير المحفوظة، وعدد
الهجرات والرقم التالي، وطول هذا الملف، وأحجام الفهرس المولَّد. **لا
تستنتج حالة المستودع من ذاكرة محادثة سابقة قبل تشغيله**: في 2026-09-03
كشف أن مجلد عمل معزولًا كان متأخرًا خطوتين عن `main` وناقصًا `AGENTS.md`.

وبقيّة الأوامر: `npm run c:app` (فحص كود الهاتف) و`npm run c:db` (بناء
القاعدة + فحوصها + الفهرس) و`npm run cloud:verify`. كلها تطبع ملخّصًا
بأسطر معدودة وتكتب سجلها الكامل في `${TMPDIR:-/tmp}/wi-checks/`.

## 2. الحالة — ق-120 نافذة

**CURRENT = Stabilization / Audit Gate.**

المشروع Pre-Production: لا مستخدمين حقيقيين، ولا بيانات عملاء، ولا تشغيل
مالي حقيقي.

لا شاشة ولا Feature ولا وظيفة جديدة — ولا `W2-02d` — قبل إغلاق طابور
التدقيق. أي استثناء بطلب صريح من المالك ويُسجَّل معلنًا. السابقتان:
م-41D7 / هجرة 093 — هجرة سلطة لا شاشة؛ و**م-41E / ق-123** — الفريق
والتنشيط، استثناء معلَن للمراحل 2–4 لأن دور المشغّل **غير قابل للاستخدام
أصلًا** بلا مسار حساب.

## 3. خط الأساس المثبت — 2026-09-04

- **القاعدة محليًا (مثبت 2026-09-04 بعد 098):** 97 ملف
  هجرة (001–098 بفجوة عند 067)، و**37** ملف اختبار دائم، و
  `FILES=37 PASS=603 FAIL=0 ERROR=0`.
- **الهاتف:** `flutter analyze` على `lib` **و**`test` =
  `No issues found!`، و**354/354 PASS**، وحرس الحد **29 مقياسًا** — والمقياس
  السادس أُعيد في م-41G من «التسديد غير متاح» إلى «المُرسَل معرّفٌ من العقد
  ومبلغ مكتوب».
- **حدّ الـData API:** Internal-schema = 0، Bare-RPC = 0، Dotted-from = 0،
  وحرس الحد **28/28**. Direct DML = 0. API SECURITY DEFINER = 0. anon
  EXECUTE = 0. exposed schemas = `api` + `graphql_public` فقط.
- **دين الهوية = صفر** في `lib/`: `'well-1'` و`'tenant-1'` و
  `'active-user'` و`'بئر الخير الرئيسي'` و`'777123456'` و
  `placeholderAccountKey`. مقيس في حرس الحد، ولا يُسمح بعودة أيٍّ منها.
- **الفهرس المولَّد** `docs/technical/db`: columns **833**، constraints
  **501**، functions **460**، triggers 44 — بعد 098 (دالتان جديدتان).
- **السحابة (مثبت 2026-09-04 بعد نشر 098 / PR #35):** 97 محليًا مقابل 97
  سحابيًا، `MISSING_IN_CLOUD=0`، و`FUNCTIONS_INDEX=447` مقابل
  `FUNCTIONS_CLOUD=449` و`FUNCTIONS_MISSING_IN_CLOUD=0` (الزائدتان
  `public.max`/`public.min` ضجيج إضافة معلوم)، و`IAM_PERMISSIONS=43` /
  `IAM_ROLE_PERMISSIONS=79` **بلا تغيير** — و098 قراءة لا صلاحية. وأول
  قياس بعد الدمج بثوانٍ أظهر سطر الهجرة ناقصًا **ودوالّها كاملة**
  (`MISSING_IN_CLOUD=1` مع `FUNCTIONS_MISSING_IN_CLOUD=0` وزيادة +2 في
  الطرفين): تأخّر سجلّ لا عطب نشر، والقياس يُعاد بعد دقيقتين. والأداة تُعلن
  ذلك **فشلًا** — فشل كاذب ينتظر تمييزًا.
- **أداة الجلسة الأولى تُعلن عجزها:** إن عجز `git` عن العمل (مجلد عمل
  محجوب) فحقوله في `c:state` تُطبع `BLOCKED` والنتيجة `BLOCKED_GIT` — لا
  `0` ولا `SUCCESS`. أُصلح في 2026-09-04 بعد أن شهد بنظافة شجرة فيها
  تغيير غير محفوظ (الثابت 699).
- **الفحص التلقائي مكتوب لا مُثبت:** `.github/workflows/checks.yml` يجري
  أول مرة على أول طلب دمج، ولا **يمنع** الدمج حتى يُضاف إلى شرط حماية
  `main` في إعدادات GitHub — عملٌ في الموقع لا في المستودع.
- **غير مثبت ولا يُدَّعى:** لا تشغيل على جهاز أندرويد حقيقي، و
  `supabase/tests` لا تُشغَّل سحابيًا أبدًا — فالمُثبَت سحابيًا وجود
  الأهداف لا نجاح تحققاتها.

## 4. التالي بالترتيب

1. **م-41E — الفريق والهوية العملية** (ق-123 / ق-124). ✅ **مُنجزة كاملةً
   ومنشورة 2026-09-03** — أربع مراحل، كل واحدة سُلِّمت عاملة بذاتها،
   والتفصيل ومعايير القبول في `OPEN_ISSUES.md` قسم م-41E. **البند التالي
   فعليًّا هو رقم 2 أدناه:**
   - **1)** ✅ **مُنجزة 2026-09-03** — أربع كِذبات في المصادقة أُزيلت،
     والحزمة 317 والحرس 26.
   - **2)** ✅ **مُنجزة ومنشورة 2026-09-03** — هجرة 094 واختبارها 22/22،
     والقاعدة `FILES=32 PASS=522`، وسحابيًّا 93/93 و43/79.
   - **3)** ✅ **مُنجزة 2026-09-03** — شاشة الفريق ومسار التنشيط،
     والحزمة 328 والحرس 27.
   - **4)** ✅ **مُنجزة ومنشورة 2026-09-03** — هجرة 095 (نطاق قراءة
     الشريك) واختبارها 26 تحققًا، والقاعدة `FILES=33 PASS=548` والفهرس
     `functions 451`؛ وشاشة الشريك: الحزمة **342** والحرس **28** و
     `analyze` نظيف. وسحابيًّا 94/94 و`43`/`79` بلا تغيير (PR #29 مدموج).
   **الجذر المكتشَف:** دور المشغّل غير قابل للاستخدام أصلًا — لا مسار في
   التطبيق لإنشاء حساب له، فلا كلمة مرور أولى لأنه لا حساب. وفي المرحلة 4
   جذرٌ ثانٍ: **سياسات اطلاع الشريك المولَّدة في هجرة 050 كانت تكشف له
   الجلسة الجارية بأرقامها**، وكان التوجيه يضعه في شاشة العمليات.
2. **م-41F — استعادة كلمة المرور بإثبات بشري.** ✅ **مُنجزة ومنشورة
   2026-09-03** (هجرة **096** + دالة حافة `reset-password` + شاشتان).
   القاعدة `FILES=34 PASS=567` والفهرس `functions 457`؛ والهاتف **354**
   والحرس **29**؛ وسحابيًّا 95/95 و`43`/`79` بلا تغيير، والدالة الحافة
   منشورة. **وما لم يُثبت:** المسار من طرف إلى طرف لم يُجرَّب مرة واحدة على
   الإنتاج — منشور لا مُبرهن عمليًّا.
   **وما بقي من البند الموحَّد:** مزوّد الرسائل (ق-124) وإغلاق ثغرة احتلال
   الرقم وطابور الرسائل — كلها تنتظر المزوّد. **والمالك الوحيد الذي ينسى
   كلمة مروره يبقى بلا مسار** (لا أحد فوقه يُصدر تذكرة) — حالة معلنة.
3. **عقد قراءة سندات الرصيد المقدَّم.** ✅ **مُنجز ومنشور 2026-09-04**
   (هجرة **097** + نافذة تسديد حقيقية، PR #33): الإنسان يختار السند
   والفاتورة والمبلغ، والخادم يتحقق — والقرار 420 انتهى حاجزه.
4. **Integration / E2E tests** ثم **CI**. ✅ **الاختبار مُنجز ومُثبت
   محليًّا 2026-09-04**: ملف واحد يمشي في السلسلة كلها بدور
   `authenticated` وحده (16 تحققًا، كلها PASS من أول تشغيل)، فصار «مخرَج
   كل عقد يصلح مدخلًا للتالي» مقيسًا. والفحص التلقائي **مكتوب لا مُثبت**:
   يجري أول مرة على أول طلب دمج، ولا يمنع الدمج قبل إضافته إلى شرط حماية
   `main` في الموقع. **والتالي فعليًّا هو رقم 6 أدناه.**
5. **استثناء دوال الإضافات** من `cloud_verify.sh` و`db_index.sh` عبر
   `pg_depend` (`deptype = 'e'`) ليتطابق حدّ المجموعتين وينقطع ضجيج
   `public.max` / `public.min`. الشرط المسجَّل «أول جولة تمسّ هذين
   السكربتين» **تحقّق في 2026-09-03** ولم يُنفَّذ: يحتاج تشغيلًا سحابيًا
   لإثباته. وأولويته انخفضت لأن نصفه — ضجيج ترتيب الدوال المُحمَّلة —
   انقطع بإصلاح الترتيب الحاسم.
6. **عقد حدود اليوم / المنطقة الزمنية.** ✅ **مُنجز ومُثبت محليًّا
   2026-09-04** (هجرة **098** + ق-125): `core.well_timezone` و
   `core.period_bounds`، ثم `api.get_reports_summary` أُعيد تعريفه عليهما
   — الحدود بمنطقة الجهة، ويوم الجلسة **يوم انتهائها** (ق-27 بعد أن كان
   الكود على «يوم البداية» المرفوض)، والجارية خارج المجاميع (ق-37)، وعبور
   الشهر يتبع شهر النهاية (ق-39). وق-38 لم يُلمس. **10 تحققات**، ومنها أن
   النافذة **لا تتغيّر** حين تُبدَّل منطقة جلسة القاعدة. ولا تعديل في
   العميل: الشاشة ترسل رمز الفترة ولا تحسب حدودًا — فتوصيف البند القديم
   («النافذة يرسلها العميل») كان متقادمًا. **ومنشورة في PR #35** والتحقق
   السحابي 97/97 و43/79 بلا تغيير.
   **ودرسٌ ثالث في عائلة الثابت 699:** التحقق 28 في اختبار 092 كان يشترط
   أن يكون حدّ اليوم منتصف ليل الخادم — **حرسٌ يحرس الخطأ** ويشهد بالسلامة
   كل يوم. حُدِّث ليقيس الخاصية لا صيغة حسابها.
7. **تمريرة توحيد** لسقف الهجرات المختومة في الوثائق. ✅ **مُنجزة
   2026-09-04** — والعلاج لم يكن تصحيح الأرقام (تتقادم مجددًا بعد هجرتين)
   بل **إزالتها**: ستة مواضع حاكمة كانت تقول 084 أو 087 صارت تشير إلى
   `AGENTS.md` §4 بلا رقم، وهو المصدر الوحيد للسقف والتالي. ومعها قسم
   «baseline الحالي المثبت» في `PROJECT_MAP.md` — كان يُعلن نفسه «الحالي»
   بأرقام 2026-08-30 (86 هجرة، 25 اختبارًا، 362 نجاحًا) فيشهد بحالة تجاوزها
   الواقع أضعافًا: نجاح كاذب في وثيقة لا في كود. صار يحيل إلى §3 هنا،
   وقياساته القديمة معلَنة تاريخيةً بتاريخها. **والسجلات لم تُحرَّف:**
   `PROGRESS.md` و`DOC_CHANGELOG.md` و`RESUME_HISTORY.md` أرقامها صحيحة
   لحظة كتابتها فتبقى. **وما بقي بقصد:** صيغة `Migration 085+` حيث تعني
   «هجرة لاحقة» لا رقم بدء، و`Migration 083+084` حيث تشير إلى هجرتين
   بعينهما — وكلتاهما صحيحة دائمًا.
8. **تقرير إغلاق ق-120.**

**أُغلق في 2026-09-03:** جولة الهوية الحقيقية (م-41D8 / ق-122) — كانت
البند الأول موقوفًا بطلب المالك، ثم بدأت وأُنجزت بموافقته على مقايضتها.
التفصيل في `OPEN_ISSUES.md` قسم م-41D8.

**إدارة الفريق** لم تبقَ بندًا مستقلًا: صارت المرحلتين 2 و3 من م-41E.

`W2-02d` (شاشات الجاهزية وحالة المزامنة) وأي توسّع وظيفي يبقيان مؤجَّلين
حتى تُعالَج هذه القائمة.

## 5. حدود لا تُخترق

- **الهجرات 071–098 مختومة** (097 في PR #33 و098 في PR #35، وكلتاهما
  منشورة ومُتحقَّقة سحابيًّا). **وهذا السقف يُكتب في `AGENTS.md` §4 وحده**؛
  ما هنا نسخة عمل تُحدَّث معه.
  أي تغيير جديد بعدها يبدأ من **099**، مع اختبار
  دائم واحد في `supabase/tests` لكل هجرة.
- **الدمج في `main` = نشر على الإنتاج** عبر تكامل GitHub، وفرع `main`
  محميّ. التراجع يكون **بهجرة جديدة** لا بتعديل هجرة منشورة. والخطة
  المجانية بلا نسخ احتياطي وبلا فروع معاينة.
- **ق-78 / ق-79:** `api` هو السطح المكشوف وحده، ولا كتابة إلا عبر عقد
  `api`. **ق-99:** لا حساب مال ولا اختراع رقم في العميل.
- **ق-113 تسري على أدوات التحقق نفسها** (الثابت 699): اشتقّ التوقّع من
  مصدر مولَّد، ولا تكتب قائمة أسماء يدويًا في سكربت فحص.
- **قبل حجب أي إجراء على المستخدم** اقرأ صلاحية الخادم ووسائطه: منعُ عملٍ
  يقبله الخادم **فشل كاذب**، وهو مرآة النجاح الكاذب. والسلطة **طبقتان**
  (فحص مسمّى + RLS) تتحركان معًا وإلا فالنتيجة غياب كاذب.
- **لا تقرأ ملفات الهجرات كاملة** لإثبات وجود عمود أو قيد أو دالة:
  ابحث في `docs/technical/db/*.txt` المولَّد بـ`npm run db:index`.
- **`partner` و`accountant` و`viewer`** مقبولون في
  `core.well_assignments.role` لكن **بصفر منح بالتصميم**. و**`partner`
  خرج من هذا الحدّ بق-123**: نطاق قراءته مُقرَّر ومكتوب في
  `technical/ACCOUNT_SETTINGS_ARCHITECTURE.md` §26. أما `accountant` و
  `viewer` فيبقيان بصفر منح ولا تعرضهما أي واجهة قبل قرار صريح.
- **لا كلمة مرور يكتبها أحد لأحد** (الثابت 706)، و**الدعوة بصفر صلاحية
  حتى تُطالَب** مُثبتًا على الخادم (707)، و**حضور الجلسة الجارية يُعرض
  ورقمها لا** لمن لا يملك التشغيل (713).

## 6. ما لا يجوز إعادة عمله — دروس مدفوعة الثمن

**في المزامنة الخلفية (W2-02b):** لا جدولة من داخل العامل على نفس الاسم
الفريد، ولا `PeriodicWork` (أدناه 15 دقيقة)، ولا Foreground Service
دائم، ولا استعمال مؤشِّر الشبكة كدليل وصول (يُعلِّم عمليات مالٍ فاشلةً
وهي سليمة)، ولا سقف على **عدد** المحاولات — السقف على المدة فقط، فإسقاط
أمر من الطابور ضياع مال. وما ينتظر قرار إنسان لا يُوقظ الهاتف.

**في الجلسة النشطة (W2-02c):** لا جدول حالة جلسة محلي موازٍ (المصدر هو
الطابور)، ولا `Timer` في الذاكرة يُعتمد عليه، ولا قسمة على مجموع الثواني
— القسمة على كل مقطع وفق هجرة 066، ولا مطابقة م-26 بجمع الوقود على
المستحق المحلي.

**في إصلاح النجاح الكاذب (م-41B3B وما بعدها):** إذا لم يوجد عقد خادمي
يعيد ما تحتاجه العملية، فالحل حالة صريحة «غير متاح» لا بناء مدخلات في
العميل. وبناء المدخلات من رقم مُجمَّع تلفيقٌ لا توصيل.

**في وصف النطاق:** وصف البنود سطرًا سطرًا يُقصّر المحيط الحقيقي. م-41D4
أُعلنت ثلاثة مواضع وكانت خمسة عشر من العائلة نفسها؛ إصلاح الثلث كان
سيُنتج مسارًا أسوأ من الأصل.

**في الهوية (م-41D8 / ق-122):** لا تُعالَج القيمة المكتوبة وحدها. الشاشة
التي تستقبل `String wellId` مفردًا **تحتاج** قيمة عند غيابها فتُكتب
عندها — فالحقل المفرد هو المرض والقيمة عرضه. العلاج أن تستقبل الشاشة
هوية واحدة إلزامية، فتصير الحالة الكاذبة **غير قابلة للتمثيل** لا
محظورة بنصّ يُنسى. ولا يُعاد فتح مفتاح طابور ثابت: الكتابة بمفتاح
والقراءة بآخر تُظهر «لا جلسة» لجلسة جارية. وقيم الاختبار الجاهزة تبقى في
`test/support/` وحده، فوضعها في `lib/` يُفقد حرس الحد معناه.

**في أدوات التحقق:** انهيار الأداة ليس فشل الكود. `flutter` تسقط في مجلد
عمل معزول لأنها تكتب طابع نسخة داخل SDK للقراءة فقط، فيُقرأ الانهيار
`FAIL`. الوسيط `--no-version-check` يمنعه، والحالة البيئية تُسمّى
`BLOCKED` لا `FAIL` (ق-113 يسري على الأداة نفسها).

**والصمت لا يُقرأ صفرًا (2026-09-04):** أداة تعجز فتُخرج فراغًا، و
`wc -l` يحوّل الفراغ إلى `0` — فصار `git status` العاجز شهادةَ نظافةٍ
لشجرة فيها تغيير غير محفوظ. كل حقل مشتقّ من مصدر قد يُحجب يحتاج **علمًا
صريحًا** على صلاحية المصدر قبل قراءة قيمته. وفي المقابل: **مجلد العمل ليس
المستودع** — `cd` في أمر سابق يبقى ساريًا في التالي، فقُرئ ملفٌ موجود
غيابًا مرتين في جولة واحدة. المسارات المطلقة تُغلق هذا الباب.

**في الفريق والحسابات (م-41E / ق-123):** لا يكتب المالك كلمة مرور لأحد —
كلمة يعرفها شخصان تُفقد نسبة العملية النقدية إلى شخص واحد. والدعوة بصفر
صلاحية حتى يُطالَب بها، **مُثبتًا على الخادم** لا بإخفاء زرّ. والربط
يُحلّ عند كل دخول لا لحظة إنشاء البئر، وإلا بقي سجلّان لا يجتمعان.
ورمزٌ يُسلَّم باليد أقوى من رمز يُرسل: الرسالة تُثبت ملكية الرقم، والمالك
الواقف أمام مشغّله يُثبت أنه هو من سلّمه. ولا رمز في كل دخول: ربع دولار
للرسالة، ومشغّلٌ بلا تغطية = جلسة لا تبدأ. و«لحظي» يُعلَن برقم أو لا
يُعلَن.

**في نطاق الشريك (م-41E/4):** الحدّ الذي يُكتب في **عقد واحد** يسقط عند
كتابة العقد التالي؛ فحجب الجلسة الجارية عن الشريك تمّ في **سياسة الصفوف**
لتستفيد منه كل العقود. والسياسات تُجمع بـ`OR`، فمن له دور تشغيلي إلى جانب
شراكته يبقى يرى — والتضييق الذي يمسّه فشلٌ كاذب. وحين تحجب السياسة صفًّا
تحتاج الواجهة **حقيقته لا رقمه**، فعدّ الحضور انتقل إلى قارئ
`SECURITY DEFINER` غرضه العدّ وحده. وجدولٌ بلا `well_id` تقع سياسته
المولَّدة على `tenant_id` فتتسع إلى كل آبار المستأجر — تُراجع لا تُفترض.
وسلطة أي دالة في مخططات العمل **صلاحية مسمّاة أو حقيقة بيانات**، لا مصفوفة
أدوار نصية: حرس م-18 (اختبار 082 التحقق 5) يسقط الهجرة عندها، ونقل الفرع
إلى مخطط غير محروس إرضاءٌ للحرس ومخالفةٌ لقاعدته. **والحرس يقرأ نصّ التعريف
بتعليقاته** (`pg_get_functiondef`)، فذكرُ النمط الممنوع في تعليق داخل جسم
الدالة يُسقطه كالنمط نفسه — الشرح يُكتب **فوق** الدالة. وهذا رابع ظهور
لعائلة «المقياس النصّي يصطدم بالتعليق الذي يشرح ما أُلغي»، وأول ظهور لها
في ‎SQL‎ بعد ثلاثة في ‎Flutter‎.
وفي العميل: `ListView` الكسولة لا تبني الأقسام البعيدة، فيصير «لم يُعرض»
و«لم يُبنَ» شيئًا واحدًا لا يفرّق بينهما قياس.

## 7. مؤجَّل بقرار المالك

- ترقية خطة Supabase إلى `Pro` (نسخ احتياطي + فروع معاينة لكل طلب دمج).
- مصير `stash@{0}`، وتنظيف 14 فرعًا محليًا قديمًا.
- **ربط مزوّد رسائل نصية فعلي** (ق-124): مقرَّر ومؤجَّل. التطوير يمضي
  بالرمز اليدوي وبرمز ثابت للاختبار محليًّا. والكلفة المقيسة **≈ ربع
  دولار للرسالة** إلى اليمن عبر مزوّد دولي، والمحلي أرخص خمس مرات لكن بلا
  واجهة معلنة. **والإشعارات بالرسائل النصية نقاش مرحلة قادمة.**
- **قرار صلاحيات `accountant` / `viewer`** — يُطلب أول مرة تحتاجها واجهة.
- **صلاحية الشريك الكتابية** «أُقرّ باستلام حصتي» — مرحلة تالية.
- **الاشتراك المباشر للتحديث اللحظي** — يمسّ سطح الكشف وطبقة العزل.
- **تفويض إدارة الفريق لـ`well_manager`** — تفويض سلطة يستحق قرارًا.
- **إغلاق تسجيل المالك لنفسه** — قرار تجاري لا تقني.
- ~~**رؤية دفعة الرصيد المقدَّم**~~ — **سقط البند في 2026-09-04**: القياس
  أثبت أن هجرة **085** أعادت سياسة قراءة الدفعات بحالتين (بجلسة، أو بلا
  جلسة مربوطة بـ`well_id`) **للمالك والمشغّل**، فالمشغّل يراها فعلًا. سؤال
  م-29 (الفجوة 14) أُجيب عنه بالتنفيذ لا بقرار جديد، والوثيقة كانت
  متقادمة — نفس عائلة [[verification-tools-go-stale]].

## 8. عقد التنفيذ

من ينفّذ ماذا، وحدّ طول الأمر، وإيقاع الجلسة، وعمق التفكير المُعلَن، وشكل
التقرير: كلها في `AGENTS.md` — وهو باب الدخول الموحَّد لأي وكيل. لا
تُكرَّر هنا حتى لا تتناقض نسختان.

المالك ينفّذ كل ما يمسّ القاعدة أو الشبكة أو تاريخ المستودع. والوكيل
يشغّل بنفسه فحوص الهاتف وحدها.
